# Development History

제품 결정과 실제 변경 내용을 시간순으로 기록한다. 새 기능, 구조 변경, 문제 해결, 검증 결과를 포함한다.

## 2026-08-08 — 프로젝트 시작

### 결정

- 롱폼과 쇼츠를 별도 앱이 아닌 하나의 시퀀스 모델로 지원한다.
- Windows에서 개발을 시작하되 Windows/macOS 공통 구조를 유지한다.
- GUI는 Premiere Pro의 익숙한 배치를 참고하고 CapCut의 간결함과 Resolve의 단계적 작업 흐름을 조합한다.
- 첫 제품 방향을 한국어 유튜브 초벌 편집과 롱폼→쇼츠 전환으로 정한다.
- 원본을 수정하지 않는 비파괴 편집과 AI 제안의 완전한 되돌리기를 제품 원칙으로 정한다.

### 문서

- 제품 개요, 프로토타입 계획, 최종 제품 계획, 아키텍처, GUI 기준을 작성했다.
- 지속적으로 문제를 축적할 `troubleshooting.md`를 만들었다.

### 다음 작업

- React/TypeScript 기반 P0 GUI 프로토타입 구현
- typecheck 및 production build 검증

## 2026-08-08 — P0 GUI 프로토타입 완성

### 구현

- React 19, TypeScript 7, Vite 8 기반 프로젝트를 초기화했다.
- Premiere에 익숙한 4영역 구조로 미디어 패널, 프로그램 모니터, 인스펙터, 타임라인을 구현했다.
- 로컬 영상·오디오·이미지 가져오기와 가져온 미디어 미리보기를 구현했다.
- 16:9 YouTube, 9:16 Shorts, 1:1 Square 캔버스 전환을 구현했다.
- 비디오·오디오·자막 트랙과 클립 선택·드래그 이동·분할·삭제를 구현했다.
- 재생 헤드, 확대·축소, 트랙 음소거·잠금, 클립 속성 편집을 구현했다.
- 실행 취소·다시 실행과 주요 키보드 단축키를 구현했다.
- AI 초벌 편집의 방향을 확인할 수 있는 데모 자막 생성을 구현했다.
- 화면비별 캔버스 안전 영역과 타이포그래피를 적용했다.

### 검증

- `pnpm typecheck` 통과.
- `pnpm build` 통과.
- production bundle: JavaScript 약 220 KB, CSS 약 18 KB.
- 1280×720 화면에서 4영역 레이아웃을 시각적으로 확인했다.
- 분할 시 클립 7→8, 실행 취소 시 8→7 복구를 확인했다.
- 데모 자막 생성 시 클립 3개 추가를 확인했다.
- 삭제 후 실행 취소로 복구되는 것을 확인했다.
- 드래그로 첫 클립 시작점이 0초→5초로 변경되는 것을 확인했다.
- 브라우저 콘솔 오류가 없음을 확인했다.

### 현재 한계

- 미리보기 재생 헤드는 UI 검증용이며 실제 다중 트랙 A/V 합성이 아니다.
- MP4 렌더링과 프로젝트 저장은 P1에서 구현한다.
- AI 초벌 자막은 실제 음성 인식 결과가 아닌 인터랙션 데모다.

### 다음 작업

- P1 데스크톱 셸과 미디어 엔진 기술 검증
- 프로젝트 스키마와 명령 기반 실행 취소 코어 분리
- FFmpeg 미디어 분석, 썸네일, 파형, 프록시 생성

## 2026-08-08 — P1 실제 미디어 Developer Preview

### 결정

- 공통 H.264/AAC 경로는 Mediabunny와 WebCodecs/Web Audio를 사용한다.
- FFmpeg는 WebCodecs 미지원 전문 코덱과 실제 프록시를 위한 선택형 호환 계층으로 남긴다.
- React UI를 Tauri 2 데스크톱 셸에서 그대로 사용해 Windows/macOS 공통 구조를 유지한다.

### 구현

- Tauri 앱 셸, 파일 열기·저장 권한, 플랫폼 아이콘을 구성했다.
- 미디어 코덱·해상도·길이 분석, 영상 썸네일과 오디오 파형을 구현했다.
- 실제 영상/이미지 미리보기와 재생 헤드 동기화, 클립 변형을 구현했다.
- 프로젝트 `.editweave.json` 저장·열기, 오프라인 미디어 재연결, 자동 저장을 구현했다.
- WebCodecs/Mediabunny 기반 H.264/AAC MP4 합성, 진행률, 취소를 구현했다.

### 검증

- CC0 `flower.mp4`와 MP3를 분석하고 타임라인에 추가했다.
- 실제 영상 2초를 H.264/AAC MP4로 출력했다.
- 프로젝트 저장 후 이름·9:16 캔버스·타임라인 상태 재열기를 확인했다.
- `pnpm desktop:check`를 통과했다.

## 2026-08-08 — P2 한국어 대본·AI 초벌

### 구현

- SRT/VTT 가져오기, SRT 내보내기, 대본 편집과 자막 트랙 생성을 구현했다.
- Transformers.js Whisper tiny의 로컬 WebGPU/WASM 음성 인식 경로를 추가했다.
- 문장 삭제를 모든 트랙에 반영하는 리플 삭제를 구현했다.
- 군더더기, 반복 문장, 파형 기반 무음 후보와 적용·거절·실행 취소를 구현했다.
- 타임라인·대본·AI 제안을 한 묶음으로 되돌리는 편집 기록으로 확장했다.

### 검증

- 한국어 SRT 5개 구간을 가져와 자막 클립 5개를 생성했다.
- 군더더기와 반복 후보 2건을 찾고 제거 적용 후 실행 취소로 복원했다.
- 로컬 Whisper의 실제 모델 다운로드/장시간 인식은 Beta 검증 항목으로 남겼다.

## 2026-08-08 — P3 롱폼→쇼츠 파생 시퀀스

### 구현

- 프로젝트에 원본과 여러 파생 시퀀스를 함께 저장하는 스키마를 추가했다.
- 대본 훅 점수 기반 15/30/60초 구간·제목 후보를 구현했다.
- 선택 범위를 9:16 시퀀스로 트림 복제하고 가로 영상의 세로 채우기 배율을 자동 계산한다.
- 상단 시퀀스 전환, 쇼츠 안전 영역, 클립별 수동 구도 조정을 연결했다.
- 여러 쇼츠를 순차 H.264/AAC MP4로 출력하는 일괄 렌더 큐를 구현했다.

### 검증

- 쇼츠 파생 시퀀스 3개를 생성하고 원본으로 돌아와 원본 자막 보존을 확인했다.
- 실제 MP4가 포함된 9:16 쇼츠 2개를 일괄 렌더하고 두 번째 렌더 완료까지 확인했다.

## 2026-08-08 — Developer Preview 0.1.0 안정화·패키징

### 구현

- 자동 저장을 최근 5개 체크포인트로 확장했다.
- 렌더 오류와 별개로 UI 충돌을 복구할 오류 경계 화면을 추가했다.
- Vitest 회귀 테스트를 도입하고 자막, 리플 삭제, 쇼츠 파생 로직 6개 테스트를 작성했다.
- `pnpm check`에 타입 검사, 테스트, 웹 빌드, Rust 검사를 묶었다.
- Windows x64 MSI와 NSIS 설치 패키지를 구성했다.

### 검증

- `pnpm check` 통과: 테스트 파일 3개, 테스트 6개.
- `pnpm desktop:build` 통과.
- 릴리스 실행 파일: `src-tauri/target/release/editweave.exe` (7,952,896 bytes), SHA-256 `7EB1470DB42E340602A9BCAF88B1E0BD008EC551E59C050B413A0299F444E18C`.
- MSI: `EditWeave_0.1.0_x64_en-US.msi` (6,184,960 bytes), SHA-256 `4EBA98D4CCC495720EE8D1A7AB44BCE3FA1C48941ADE87DE017519EC44A6913F`.
- NSIS: `EditWeave_0.1.0_x64-setup.exe` (5,660,251 bytes), SHA-256 `32AEDB9FFF2AA173EB2358802FD55D1A2DA764B6F3FE3CCEF17CB5F7B5721C7D`.

### 출시 판단

- P0~P3 핵심 흐름은 Developer Preview 수준으로 완주했다.
- 실제 편집자 파일럿, 장시간/VFR·코덱 검증, macOS 서명/공증, 얼굴 추적·전문 편집 기능이 남아 있어 Alpha/Beta/V1 완료로 표시하지 않는다.

## 2026-08-08 — 버그 감사와 Beta 호환성 기반

### 버그 수정

- 프록시 변환 엔진의 취소 신호는 존재하지만 UI에서 누를 수 없던 문제를 수정해 생성 중 `취소` 버튼을 연결하고 정상 취소 상태로 복구한다.
- 빈 트랙의 첫 미디어가 1초에서 시작하던 문제를 수정해 0초에 삽입한다.
- 잠긴 트랙에서 분할·삭제·속성 편집을 막고 인스펙터 잠금 상태를 표시한다.
- 비디오·자막·오디오 트랙 음소거를 프로그램 모니터와 단일/일괄 렌더 입력에 반영한다.
- 대본 텍스트 수정 시작 전 스냅샷을 기록해 실행 취소·다시 실행이 가능하도록 했다.
- 쇼츠 시퀀스의 최소 타임라인 길이를 120초에서 15초로 줄여 짧은 편집의 불필요한 스크롤을 제거했다.

### Beta 진행

- 미디어 분석 결과에 영상·오디오 실제 디코딩 가능 여부를 기록한다.
- 미디어 카드에 `편집 준비`, `고해상도`, `장시간`, `오디오 제한`, `디코더 없음`, `오프라인` 진단을 표시한다.
- CC0 MP4(H.264/AAC, 960×540, 5.056초)와 MP3(44.1 kHz, stereo, 2.116초)를 자동 검증하는 호환성 픽스처 테스트를 추가했다.
- 로컬 Whisper tiny의 최초 다운로드·캐시·5초 MP4 인식을 실제 화면에서 완료했다.
- 모델 캐시 후 두 번째 인식은 같은 환경에서 약 1.7초에 완료됐다.

### 검증

- 첫 클립 0초 삽입 확인.
- 잠금 시 분할·삭제·인스펙터 입력 비활성화 확인.
- 비디오 음소거 시 프로그램 영상 제거와 출력 비활성화, 자막 음소거 시 오버레이 제거 확인.
- 대본 수정의 실행 취소·다시 실행 왕복 확인.
- 테스트 파일 5개, 테스트 11개 통과.

## 2026-08-08 — Beta VFR 진단·편집 프록시·장시간 기준선

### 구현

- 영상의 첫 240개 이내 인코딩 패킷을 표본으로 평균 프레임레이트와 프레임 지속 시간 변동계수를 계산한다.
- 변동계수 0.08 초과를 VFR 가능성으로 표시하고 미디어 카드와 호환성 진단에서 30fps 프록시를 안내한다.
- Mediabunny/WebCodecs 변환으로 긴 변 최대 960px, 고정 30fps, H.264/AAC MP4 편집 프록시를 생성한다.
- 프록시 생성 진행률·취소·오류·원본/프록시 미리보기 전환 UI를 추가했다. 최종 렌더는 프록시 상태와 무관하게 원본 파일을 사용한다.
- 30분 또는 1,000클립부터 경고, 60분 또는 5,000클립부터 고부하로 표시하는 타임라인 상태 진단을 추가했다.
- 10/30/60분 합성 타임라인의 리플 삭제와 프로젝트 직렬화 회귀 벤치마크를 추가했다.

### 검증

- CFR 29.97fps와 스마트폰형 혼합 프레임 지속 시간을 합성해 CFR/VFR 분류 회귀 테스트를 통과했다.
- CC0 `flower.mp4`를 29.97fps CFR로 분석했고, 960×540·30fps 프록시 263KB를 약 1.69초에 생성했다.
- 프록시 사용/원본 보기 전환 후 미리보기가 유지되고 출력 입력은 원본을 참조하는 것을 확인했다.
- 생성 직후 `취소`를 눌러 오류 없이 `생성` 상태로 복구한 뒤 프록시를 다시 생성해 취소·재시도 흐름을 확인했다.
- 합성 타임라인 측정: 10분·1,200클립 1.50ms, 30분·3,600클립 1.76ms, 60분·7,200클립 4.31ms.
- `pnpm typecheck`, 테스트 파일 7개·테스트 17개, `pnpm build`를 통과했다.
- `pnpm check`와 `pnpm desktop:build`를 다시 통과해 Windows x64 실행 파일·MSI·NSIS를 최신 코드로 재생성했다. 확정 크기와 SHA-256은 위 패키징 검증 항목에 기록했다.

### 남은 Beta 검증

- 실제 iPhone/Android VFR 샘플로 프레임 정확도와 A/V 동기화를 측정한다.
- 실제 30/60분 미디어에서 디코딩·파형·메모리·렌더 시간을 측정한다.
- 1.5GB 초과 원본을 위한 디스크 스트리밍 프록시를 구현한다. 데스크톱 영속 캐시·재열기 복원·개별 정리는 완료했다.

## 2026-08-08 — 데스크톱 프록시 영속 캐시와 간편 실행

### 구현

- `run.bat`을 추가해 프로젝트 루트에서 빌드된 `editweave.exe`를 더블클릭으로 실행할 수 있게 했다.
- Tauri 앱 캐시의 프로젝트/자산별 안전한 상대 경로에 완성된 프록시 MP4를 저장한다.
- 프로젝트 파일에는 프록시 캐시 경로·생성 시각·크기·해상도만 저장하고 Blob URL과 메모리 파일은 제외한다.
- 프로젝트 재열기 시 캐시 파일을 복원해 원본이 오프라인이어도 프록시 미리보기를 사용한다. 최종 출력에는 계속 원본 재연결이 필요하다.
- 미디어 패널에 `디스크 캐시`/`세션 전용` 상태와 프록시 개별 삭제를 추가했다. 삭제해도 원본은 유지된다.
- 파일 시스템 권한을 `$APPCACHE/proxies/**`로 제한하고 캐시 경로의 상위 디렉터리 이탈을 거부한다.

### 버그 수정

- 프로젝트 파일을 새로 열자마자 이전 자산 참조를 검사해 캐시 복원을 건너뛸 수 있던 경쟁 조건을 수정했다.
- 오프라인 원본의 캐시 복원 오류가 숨겨지고 동작하지 않는 생성 버튼이 보일 수 있던 상태 표시를 수정했다.
- `run.bat`의 LF 줄바꿈을 Windows CRLF로 변환하고 UTF-8 코드 페이지를 설정해 명령 이름의 첫 글자가 유실되던 실행 오류를 수정했다.

### 검증

- 프로젝트 직렬화에서 디스크 캐시 메타데이터만 남고 세션 전용 프록시 값은 제거되는 회귀 테스트를 추가했다.
- 안전한 캐시 경로 생성과 `../` 경로 거부, 오프라인 원본의 `프록시만` 진단을 테스트했다.
- 웹 화면에서 960×540 프록시 생성, `세션 전용` 표시, 개별 삭제와 원본 미디어 유지를 확인했다.
- `pnpm check` 통과: 테스트 파일 9개, 테스트 22개.
- `pnpm desktop:build`로 최신 Windows x64 EXE·MSI·NSIS를 생성했다.
- `run.bat` 실행으로 새 `editweave.exe` 프로세스가 생성되는 것을 PID로 확인하고 테스트 프로세스만 종료했다.

## 2026-08-08 — V1 Development Candidate 전체 개발 패스

### 편집 코어

- 임의 개수의 V/A/T 트랙, 트랙 선택·삭제·표시·음소거·솔로·잠금과 트랙 믹서를 구현했다.
- 뒤에 추가·삽입·덮어쓰기, 일반·리플·롤·슬립 트림, 그룹 이동/삭제, 링크·그룹, 마커를 구현했다.
- 속도·역재생·정지 프레임, 전환, 변형 키프레임과 easing을 프로젝트 스키마에 추가했다.
- 여러 활성 비디오 레이어를 프로그램 모니터와 최종 캔버스에서 같은 순서로 합성한다.

### 색상·오디오·자막

- 노출·대비·채도·색온도·틴트·하이라이트·그림자와 시네마틱/웜/쿨/모노 LUT를 추가했다.
- 클립/트랙 게인·팬·페이드, 솔로, 피크 미터·클리핑 경고, 정규화, 노이즈 게이트, 음성 강조와 BGM 덕킹을 추가했다.
- 오디오 렌더를 10초 청크 스테레오 믹스로 바꾸고 속도·역재생 타임 매핑을 반영했다.
- Whisper 단어 타임코드 그룹화, 로컬 화자 라벨, 화자 수동 수정, 교정 사전과 단어 강조 자막 프리셋을 구현했다.

### 쇼츠·미디어·프로젝트

- 4:5 캔버스를 추가하고 지원 WebView에서 다섯 표본 프레임 얼굴 중심을 찾아 쇼츠 키프레임을 생성한다.
- 세로 안전 영역과 큰 하단 자막의 충돌 경고를 추가했다.
- 미디어 이름·태그 검색, 폴더, 태그, 사용 여부와 프로젝트 프록시 총량/전체 정리를 추가했다.
- 최근 5개 자동 저장 체크포인트를 화면에서 골라 복원할 수 있게 했다.
- 데스크톱 프록시를 Mediabunny `StreamTarget`으로 앱 캐시에 직접 기록하고, 1.5GB 초과 또는 WebCodecs 미지원 원본은 시스템 FFmpeg를 사용하는 보조 경로를 추가했다.

### 출력·운영

- H.264/H.265, 24/30/60fps, 비트레이트, 하드웨어/소프트웨어 인코더 선호 옵션을 추가했다.
- FCPXML과 CMX 3600 EDL 교환 파일 생성을 추가했다.
- 렌더 진행 상태를 로컬 복구 레코드로 남겨 비정상 종료 다음 실행에서 중단 사실을 안내한다.
- 사용자 요청형 업데이트 확인, 명시적 동의형 충돌 보고와 로컬 오류 보고서 저장을 추가했다.
- `run.bat` 기본 동작은 최신 소스 개발 앱, `run.bat release`는 기존 배포 EXE로 분리했다.
- `run.command`와 Windows/macOS GitHub Actions 빌드 구성을 추가하고 Apple 서명·공증 및 Tauri 서명 secret 자리를 마련했다.

### 검증 정책

- 사용자가 하나씩 직접 검증하겠다고 요청했으므로 이 개발 패스에서는 `pnpm typecheck`, `pnpm test`, `pnpm build`, `cargo check`, 브라우저 조작, 앱 실행과 패키징을 실행하지 않았다.
- 기존 0.1.0 EXE/MSI/NSIS의 크기와 해시는 최신 소스 산출물로 갱신하지 않았다.

## 2026-08-09 — Windows 배치 실행 인코딩 재발 수정

- `run.bat` 재작성 과정에서 LF 전용 줄바꿈과 한글 안내가 다시 들어가 CMD가 `TALLER`, `ules" goto release` 같은 잘린 문자열을 명령으로 실행하는 문제가 사용자 테스트에서 발견됐다.
- 배치 내용을 ASCII 전용으로 단순화하고 `pnpm`을 `call pnpm desktop:dev`로 호출하도록 수정했다.
- 줄바꿈을 Windows CRLF로 다시 정규화했다.
- 사용자가 검증을 담당하므로 배치 파일 자체 실행과 앱 실행은 Codex가 수행하지 않았다.

## 2026-08-09 — 편집 화면 기본 설정 런타임 오류 수정

- 사용자 테스트에서 편집 화면이 `defaultCaptionStyle is not a function` 오류로 복구 모드에 진입하는 문제를 확인했다.
- 기본 자막·색상·오디오 설정을 객체 상수에서 새 객체를 반환하는 팩토리 함수로 변경해 기존 호출 규약과 일치시켰다.
- 자막과 동일한 구조였던 색상·오디오 기본값도 함께 수정해 후속 런타임 오류를 예방했다.
- 사용자 요청에 따라 앱 실행과 기능 테스트는 수행하지 않았다.

## 2026-08-09 — 실무용 V1 Production Hardening 전환

- 완료 후보 판정을 철회하고 `docs/08-production-completion-plan.md`를 추가해 실제 작업 완주 기준과 남은 제품 개발을 분리했다.
- 브라우저 즉시 파일 선택, Tauri 네이티브 드롭, MIME 없는 파일 확장자 판별, 패널 전체 드롭과 선택기 폴백을 구현했다.
- 저장 프로젝트의 데스크톱 원본 경로 자동 재연결과 사용 중 미디어 보호형 제거를 추가했다.
- 새 프로젝트의 가짜 영상·오디오·자막 클립을 제거하고 빈 V1/A1/T1 트랙으로 시작하게 했다. 기존 자동 저장의 알려진 데모 클립 ID도 로드 시 제거한다.
- 크롭, 타원/둥근 마스크, 블러, 그림자, 감지 얼굴 모자이크를 인스펙터·미리보기·최종 캔버스 렌더에 연결했다.
- 로컬 Transformers.js MODNet 배경 제거를 추가해 선택 이미지를 투명 PNG 새 미디어로 비파괴 등록한다.
- 공식 ComfyUI 로컬 API 경로에 맞춰 선택 이미지 업로드, API 워크플로 입력 교체, 큐 등록, 실행 이력 폴링, 결과 다운로드와 새 미디어 재등록을 구현했다.
- 조정 레이어를 추가하고 합성 미리보기와 최종 렌더에서 색보정·시각 필터가 적용되게 했다.
- 미리보기 오디오에 StereoPanner, 노이즈 하이패스와 음성 대역 강조를 연결했다.
- SRT에 더해 VTT 내보내기를 추가하고, 쇼츠 파생 화면비를 9:16·4:5·1:1로 확장했다.
- 데스크톱 단일 및 일괄 MP4 출력을 지정 파일로 직접 스트리밍해 장시간 출력의 전체 파일 메모리 적재를 제거했다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않았다.

## 2026-08-09 — Top 3 벤치마크 정교화와 팀 오디오 템플릿

- Premiere의 정밀 타임라인·대본 편집, DaVinci Resolve의 Color/Fusion/Fairlight 통합, CapCut의 롱폼→쇼츠·자동 자막·템플릿 중심 흐름을 공식 제품 설명 기준으로 다시 비교했다.
- GUI 기준을 Premiere형 미디어/소스·프로그램·타임라인·인스펙터 뼈대, Resolve형 선택적 전문 화면, CapCut형 빠른 크리에이터 진입의 혼합 구조로 확정했다.
- ComfyUI는 핵심 편집 엔진이나 필수 설치 조건이 아니라 사용자가 명시적으로 허용할 때만 동작하는 선택적 외부 생성·보정 브리지로 범위를 고정했다.
- 오디오 믹서에서 네 역할 버스의 게인·뮤트·솔로·안전 리미터와 모든 순차 삽입 효과를 이름 있는 팀 템플릿으로 저장·갱신·삭제·적용할 수 있게 했다.
- 템플릿에 ADR 큐 기본 길이, 0/1/2/3/5초 카운트다운과 선호 마이크 이름을 포함하고, 적용한 기본값을 다음 ADR 세션의 장치 선택·큐 길이·녹음 시작에 연결했다.
- 팀 템플릿을 `.editweave-audio.json`으로 공유·가져오기하며, 기존 ADR 필드가 없는 템플릿은 안전한 기본값으로 보완한다. 잘못된 JSON은 믹서를 중단시키지 않고 화면 오류로 표시한다.
- 템플릿의 버스 삽입 인스턴스에는 새 ID를 발급하고 전체 버스 적용은 편집 기록 한 건으로 남겨 Undo로 되돌릴 수 있게 했다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않고 정적 소스 연결만 확인했다.

## 2026-08-09 — 자유형 속도 곡선과 팀 속도 템플릿

- 속도 키프레임에 사용자 cubic Bézier 제어점 두 개를 저장하고 인스펙터의 소형 곡선 편집기에서 직접 끌어 구간 가속 형태를 조절하도록 했다.
- Bézier의 시간축 x를 역산한 속도 보간과 `y·dx` 적분을 공통 소스 시간 계산에 연결해 프로그램 미리보기, 오디오, 최종 출력이 같은 자유 곡선을 사용하도록 했다.
- 기본 재생 속도·역재생·모든 속도 지점·보간·Bézier 제어점을 사용자 속도 템플릿으로 로컬 저장·적용·삭제할 수 있게 했다.
- `.editweave-speed.json` 공유와 가져오기를 추가하고, 다른 길이의 대상 클립에서는 지점 시간을 길이 비율로 재배치하며 새 키프레임 ID를 발급한다.
- 템플릿은 최대 120개 지점과 5%~1600% 범위를 정규화하고 잘못된 버전·숫자·보간 형식은 적용 전에 거부한다.
- 사용자 요청에 따라 실행·타입 검사·자동 테스트·패키징은 수행하지 않았다.

## 2026-08-09 — 영상 전체 로컬 배경 제거와 시간축 마스크 안정화

- 이미지 전용 MODNet 로더를 재사용해 선택 영상 클립의 실제 소스 구간을 Mediabunny로 디코딩하고 최대 60개 프레임을 로컬 추론하는 영상 전경 마스크 경로를 추가했다.
- 각 알파 결과를 256px 분석 프레임에서 읽고 전경이 존재하는 12개 높이의 좌·우 끝점을 찾아 24점 닫힌 다각형으로 축약한다. 전경 면적이 1% 미만이거나 유효 높이 표본이 6개 미만이면 해당 프레임을 거부한다.
- 연속 다각형 점을 이전 28%·현재 72%로 평활화해 프레임별 가장자리 떨림을 줄이고, 각 표본에 평균 알파 기반 신뢰도를 저장한다.
- 속도 램프·자유 곡선·역재생의 공통 소스 시간 역매핑으로 분석 프레임을 클립 로컬 시간에 배치한다.
- 기존 시각 효과와 모든 키프레임 시간을 함께 재표본화하고 동일 ID의 `AI 영상 전경` intersect 마스크를 병합해 기존 크롭·블러·다른 마스크·크로마키 애니메이션을 보존한다. 최대 8마스크 한도에서는 기존 마스크를 자동 삭제하지 않고 실행 전 차단한다.
- 인스펙터에 실행·취소 버튼, 진행 단계와 완료 신뢰도 알림을 연결했으며 결과는 기존 Canvas 마스크 처리 덕분에 프로그램 미리보기와 최종 MP4에 동일하게 적용된다.
- 사용자 요청에 따라 실행·타입 검사·자동 테스트·패키징은 수행하지 않았다.

## 2026-08-09 — QuickTime tmcd 트랙 자체 타임코드 판독

- Rust 데스크톱 계층에 의존성 없는 QuickTime atom 판독기를 추가해 FFprobe 실행 여부와 별개로 MOV/MP4의 `moov`와 각 `trak`을 검사한다.
- `hdlr=tmcd` 트랙에서 timecode sample description의 flags, time scale, frame duration, nominal frame count와 source reference를 읽는다.
- `stco`/`co64`의 첫 chunk offset과 `stsz`의 sample size를 따라 파일의 실제 첫 32-bit timecode sample frame count를 범위 읽기한다. 대용량 원본 전체를 메모리에 올리지 않고 `moov`만 최대 256MB 한도로 읽는다.
- Apple QTFF의 drop-frame flag `0x0001`, 24-hour wrap `0x0002`, counter flag `0x0008`을 반영하고 29.97/59.94 계열의 누락 프레임 번호를 SMPTE 문자열로 복원한다.
- FFprobe가 성공하면 색공간/HDR 등 결과를 유지하면서 비어 있는 TC·릴·fps만 네이티브 판독값으로 보충하고, FFprobe 자체가 없으면 네이티브 `tmcd` 결과만 반환한다.
- 손상 atom, 외부 data reference, 256MB 초과 `moov`, 음수 TC처럼 안전하게 해석할 수 없는 구조는 추측하지 않고 기존 원시 태그 또는 수동 입력 경로로 넘긴다.
- 사용자 요청에 따라 Cargo 검사·실행·자동 테스트·패키징은 수행하지 않았다.

## 2026-08-09 — 물체 추적 장기 재획득과 수동 교정 재시작

- 네 코너의 적응형 단기 패치와 별도로 최초 전체 마스크 영역 및 각 코너의 변경되지 않는 identity template을 보존한다.
- 평균 로컬 오차가 0.19를 넘거나 두 표본 이상 대상을 잃으면 전체 프레임에서 0.7/0.85/1/1.15/1.3배 다중 스케일 identity 후보를 거친 간격으로 검색하고 최상 후보 주변을 다시 정밀 탐색한다.
- 전체 영역 오차가 0.175 미만일 때만 재획득으로 인정하고, 새 중심·스케일에 네 코너 배치를 옮긴 뒤 원본 코너 패치로 재검증한다. 낮은 신뢰도 프레임은 적응형 템플릿을 갱신하지 않아 가림 물체로 외형이 오염되지 않게 했다.
- 각 추적 표본에 `reacquired` 상태를 기록하고 완료 알림에 평균 신뢰도와 가림 뒤 재획득 횟수를 표시한다.
- 기존 마스크 추적 키프레임이 있으면 재생 헤드에서 사용자가 수정한 현재 마스크를 새 기준으로 삼고 이전 키프레임을 유지한 채 이후 구간만 재추적한다.
- 사용자 요청에 따라 실행·타입 검사·자동 테스트·패키징은 수행하지 않았다.

## 2026-08-09 — 중첩 시퀀스 완전 시간 재매핑과 합성 우선순위

- 중첩 시퀀스를 펼칠 때 부모의 첫 고정 배속만 곱하던 경로를 적분형 소스 시간·역함수 기반 매핑으로 교체했다.
- 부모 속도 램프와 자유 Bézier 곡선, 역재생을 이진 탐색으로 출력 시간에 역매핑하고 자식 자체 속도·역재생과 결합한 소스 속도를 최대 120개 표본으로 보존한다.
- 합성된 속도 표본의 적분값을 실제 자식 미디어 시작·끝 프레임 차이에 맞게 보정해 중첩 경계에서 영상·오디오 소스 시간이 어긋나지 않도록 했다.
- 자식 변형·시각 효과·클립 오디오·트랙 오디오 키프레임, 전환 길이와 ADR 컴프 구간도 같은 부모 시간 역매핑으로 출력 타임라인에 배치한다.
- 중첩 클립 인스펙터에 -1000~1000 합성 우선순위를 추가하고, 펼친 트랙을 우선순위로 정렬해 값이 높은 중첩 영상이 실제 프로그램 합성과 출력에서 나중에 그려지도록 했다.
- 사용자 요청에 따라 실행·타입 검사·자동 테스트·패키징은 수행하지 않고 정적 연결만 확인했다.

## 2026-08-09 — FFprobe 미설치 환경의 컨테이너 TC·릴 태그 대체 판독

- 미디어 분석 단계에서 Mediabunny의 컨테이너 설명 메타데이터를 FFprobe 호출과 병렬로 읽도록 했다.
- MP4/MOV `udta/meta`, Matroska Tags, ID3, Vorbis와 RIFF INFO의 원시 태그를 재귀 순회하고 문자열과 UTF-8 바이트 값을 안전하게 텍스트로 변환한다.
- timecode/time_code/start_tc 계열 키와 `HH:MM:SS:FF`·드롭프레임 `HH:MM:SS;FF` 패턴, reel/reel_name/tape 계열 키를 찾아 FFprobe가 없거나 해당 값이 null일 때 대체한다.
- FFprobe가 제공한 색공간·HDR·프레임레이트와 TC/릴 값은 계속 우선하며, 대체 판독은 없는 필드만 채운다.
- QuickTime `tmcd` 전용 트랙 안의 이진 샘플 프레임 카운트 판독은 별도 남은 항목으로 유지해 태그 대체를 과장하지 않았다.
- 사용자 요청에 따라 실행·타입 검사·자동 테스트·패키징은 수행하지 않았다.

## 2026-08-09 — 비파괴 장면 전환 감지

- 선택 영상 구간을 최대 1,800개 축소 프레임으로 로컬 디코딩하고, 휘도 표본과 히스토그램 차이의 적응형 임계값·인접 피크 억제로 하드 컷 후보를 찾는 장면 감지 엔진을 추가했다.
- 분석은 원본 또는 활성 편집 프록시를 사용하며 진행률 표시와 취소를 지원한다.
- 일정 속도·속도 램프·역재생 클립 모두 감지된 소스 시간을 공통 시간 모델로 타임라인 시간에 역산한다.
- 감지 결과를 즉시 타임라인에 강제 적용하지 않고 후보별 신뢰도와 타임코드를 고르는 검토 창을 추가했다.
- 선택 결과는 원본을 바꾸지 않는 마커로 추가하거나, 선택 클립과 연결·그룹 클립을 같은 지점에서 분할할 수 있다. 분할은 편집 이력의 실행 취소 대상이다.
- 장면 분할 시 각 조각의 속도 곡선·정방향/역방향 소스 오프셋을 보존하고, 시작/끝 전환은 바깥 경계 조각에만 남긴다.
- 프록시 메모리 파일을 분석할 때 원본 데스크톱 경로를 잘못 재사용하던 모션 추적 입력 경로도 함께 수정했다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않았으며 정적 소스 연결만 점검했다.

## 2026-08-09 — 속도 램프와 공통 소스 시간 매핑

- 클립 기본 속도와 재생 헤드 위치별 속도 키프레임을 함께 저장하고 선형·가속·감속·부드러운 보간을 선택하는 인스펙터 편집 UI를 추가했다.
- 로그 스케일 속도 그래프에서 두 번 눌러 지점을 만들고 드래그해 시간·속도를 함께 조정하며 두 번 눌러 삭제할 수 있게 했다.
- 속도 곡선의 면적을 해석적으로 적분해 소스 시간을 구하는 공통 모델을 만들고, 정방향·역방향·정지 프레임의 프레임 조회가 같은 계산을 사용하게 했다.
- 클립별 정렬 속도 지점과 누적 적분값을 WeakMap에 캐시하고 이진 탐색해 48kHz 오디오 출력의 샘플별 시간 조회 비용을 줄였다.
- 프로그램 영상과 오디오의 순간 재생률을 재생 헤드 위치에서 갱신하고, 타임라인 클립에 속도 지점 수를 표시했다.
- 최종 오디오를 출력 샘플 기준으로 다시 매핑해 느린 재생·역재생·가변 속도에서 빈 샘플 없이 영상과 같은 소스 시간을 조회하도록 바꿨다.
- 일반·리플·롤·슬립·슬라이드 트림, 범위 삭제, 덮어쓰기, 일반 분할과 멀티캠 컷이 속도 곡선의 경계 속도와 소스 오프셋을 보존하게 했다.
- 쇼츠 파생, 얼굴 모션 추적, 무음 초벌 제안, 중첩 시퀀스 펼치기에도 가변 속도 시간 역산을 연결했다.
- FCPXML에는 속도 시간 맵을 기록하고 CMX 3600 EDL에는 읽을 수 있는 EditWeave 속도 램프 주석을 남긴다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않았으며 정적 소스 연결만 점검했다.

## 2026-08-09 — Top 3 벤치마크·중첩 시퀀스·영속 렌더 큐

- 공식 기능표를 기준으로 Premiere Pro, DaVinci Resolve, CapCut Desktop의 미디어·편집·합성·색상·오디오·자막·AI·쇼츠·출력·협업 기능 매트릭스를 `docs/09-top3-benchmark-and-differentiation.md`에 추가했다.
- EditWeave의 차별성을 롱폼과 파생 결과물을 연결하는 Creator Source Graph, 승인형 대본 편집, 납품 사전 검사, 로컬 우선 AI, 복구 중심 렌더로 정의했다.
- ComfyUI는 핵심 편집 기능이 아니라 설치를 요구하지 않는 선택적 외부 생성·보정 연동으로 범위를 축소했다.
- 재생 헤드에 겹친 잠금 해제 클립들을 새 중첩 시퀀스로 묶고, 상단 시퀀스 선택기에서 내부를 편집하며 프로그램 미리보기와 최종 출력에서 재귀적으로 펼치는 합성 경로를 구현했다.
- 렌더 작업을 로컬에 보존하고 실행 중 일시정지·재개·취소, 창을 닫은 백그라운드 진행, 실패·취소·비정상 종료 작업의 재시도와 개별 정리를 추가했다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않았다.
## 2026-08-09 — Top 3 벤치마크 구현 패스와 EditWeave 차별화

### 벤치마크와 범위

- 유튜브 롱폼·쇼츠 기준 Top 3를 Premiere Pro, DaVinci Resolve, CapCut Desktop으로 정하고 공식 기능 페이지 기반 비교표와 D1~D4 로드맵을 `docs/09-top3-benchmark-and-differentiation.md`에 기록했다.
- ComfyUI는 핵심 영상 편집 기능이 아닌 선택적 로컬 이미지 워크플로 연동으로 분리했다.

### 편집 코어

- 독립 소스 모니터에 인·아웃, 구간 해제와 3점 삽입을 연결했다.
- 내장 오디오를 연결된 별도 클립으로 분리하고 독립 트림해 J/L 컷을 만들 수 있게 했다.
- 슬라이드 트림, 중첩 시퀀스의 재귀 합성, 파형 상관관계 동기화, 멀티캠 시퀀스와 1~4 각도 라이브 컷을 추가했다.
- 키보드 단축키 캡처·중복 해소·기본값 복원 편집기를 추가했다.
- 크롭·마스크·블러·그림자·모자이크·크로마키의 효과 키프레임을 미리보기와 출력에 연결하고 중첩 경계에서도 샘플링해 합성한다.

### 색상·합성·텍스트

- 합성 모드, 크로마키, 자유 다각형 마스크와 실제 픽셀 키잉을 미리보기·출력에 적용했다.
- Lift/Gamma/Gain, 톤 커브형 암부·중간톤·명부 제어, 비브런스·페이드·비네트와 조정 레이어 색보정을 추가했다.
- 선택 소스의 실제 프레임을 읽는 파형, RGB 퍼레이드, 히스토그램 스코프를 추가했다.
- Master·Red·Green·Blue 채널의 점을 직접 추가·드래그·삭제하고 채널별 초기화하는 RGB 곡선 편집기를 추가했다.
- 곡선을 256단계 채널 LUT로 변환해 일반 클립은 처리 캔버스, 조정 레이어는 SVG Component Transfer 미리보기와 출력 합성 캔버스에 적용한다.
- RGB 곡선은 크로마키 뒤, 기존 노출·대비·LUT 필터 앞에 적용하고 중첩 시퀀스에서는 자식과 부모 곡선을 17점 합성 곡선으로 보존한다.
- 곡선이 기본 직선인 클립은 픽셀 읽기 경로를 사용하지 않아 기존 빠른 미리보기·출력 경로를 유지한다.
- HSL Qualifier를 추가해 색상 중심·허용 범위, 채도/명도 밴드와 부드러움으로 픽셀을 선택하고 선택 부분에만 노출·채도·색상 이동을 적용한다.
- Qualifier는 크로마키·RGB 곡선 뒤에 적용되며 일반 클립의 프로그램 미리보기와 최종 출력, 중첩 시퀀스에 보존된다. 조정 레이어에는 현재 Qualifier 편집 UI를 노출하지 않는다.
- `선택 매트 보기`를 켜면 프로그램 모니터만 흰색/검정 가중치 매트로 전환하고, 출력 처리에는 미리보기 플래그를 전달하지 않아 매트가 최종 파일에 들어가지 않게 분리했다.
- 헤드라인, 로어 서드, 인용문, 구독 CTA, 포인트의 다섯 모션 텍스트 템플릿을 추가했다.
- 마스크 페더와 반전을 효과 모델·키프레임·중첩 합성에 추가하고, 미리보기와 MP4 출력이 공통 마스크 형상 계산을 사용하도록 했다.
- 크로마키·얼굴 모자이크·소프트 마스크를 하나의 처리 캔버스로 합쳐 조합 효과에서도 마스크 알파가 유지되도록 했다.
- 선택한 클립의 원본 구간을 최대 120개 프레임까지 로컬 분석하고 얼굴 중심을 부드럽게 보간한 위치 키프레임으로 적용하는 온디맨드 모션 추적을 추가했다.
- 모션 추적 진행 중 같은 버튼으로 취소할 수 있고, 원본 디코더가 제한된 경우 사용 중인 편집 프록시를 추적 입력으로 재사용한다.
- 여러 얼굴이 검출되면 이전 프레임 위치와 얼굴 크기를 함께 점수화해 같은 인물을 우선 추종하고 장면 내 인물 전환 점프를 줄인다.
- 얼굴 감지 API와 별개로 사용자가 지정한 다각형 마스크의 휘도 템플릿을 주변 영역에서 추적하고, 이동량을 마스크 점 시각 효과 키프레임으로 기록하는 일반 물체 추적을 추가했다.
- 일반 물체 추적은 원본·프록시, 일정 속도·속도 램프·역재생을 지원하며 진행률·취소와 기존 시각 키프레임 교체 확인을 제공한다.
- 분할·트림·범위 삭제 시 변형과 시각 효과 자동화를 경계 포함 최대 120개 선형 표본으로 다시 배치해 오른쪽 조각의 키프레임 시간이 어긋나지 않게 했다.

### 오디오·납품·협업

- 하이패스, 3밴드 EQ, 컴프레서, 리미터를 미리보기와 최종 믹스에 연결했다.
- 트랙 믹서에서 게인·팬·뮤트·솔로를 한 화면에 모으고 분석 파형 기반 LUFS/True Peak 예상 및 -14 LUFS 일괄 맞춤을 추가했다.
- 클립 게인·팬을 재생 헤드 위치에 키프레임으로 기록하고 네 가지 보간 방식으로 자동화하는 인스펙터 흐름을 추가했다.
- 미리보기의 Web Audio GainNode·StereoPanner와 최종 48kHz 샘플 믹스가 같은 자동화 값을 사용하며 양의 게인도 HTML 볼륨 상한에 잘리지 않게 했다.
- 분할·트림·범위 삭제·중첩 시퀀스·쇼츠 파생에서 오디오 자동화 시간을 다시 배치하고, Delivery Guard의 LUFS/True Peak 예상은 자동화 평균 에너지와 최대 게인을 반영한다.
- 오프라인·디코더·VFR·검은 구간·중첩·오디오·자막 안전 영역을 검사하는 Delivery Guard를 추가했다.
- 타임코드 검토 코멘트, 작성자, 열림/해결 상태와 UTF-8 CSV 내보내기를 추가했다.
- 720p·1080p와 함께 2160급 4K MP4 출력을 선택할 수 있게 했다.
- YouTube 1080p·Shorts 1080p·YouTube 4K·HEVC 4K 마스터 기본 프리셋을 추가하고, 현재 해상도·코덱·fps·비트레이트·인코더·오디오 설정을 이름 붙여 로컬 저장·적용·삭제할 수 있게 했다.
- 대화·음악·효과음·환경음의 4개 역할 버스를 추가하고 버스별 게인·뮤트·솔로·리미터를 오디오 믹서에서 제어하도록 했다.
- 클립의 기본 역할 라우팅과 단일 보조 센드/센드 레벨을 인스펙터에 추가했다. 최종 출력은 클립 처리 후 역할별 버퍼를 합산하고 버스 게인·리미터를 거쳐 마스터로 모은다.
- 버스 상태를 시퀀스별 프로젝트 저장·자동 저장·전환·중첩·멀티캠·쇼츠 파생과 편집 Undo/Redo에 포함하고, 미리보기 및 빠른 LUFS/True Peak 예상에도 버스 솔로·뮤트·게인과 센드를 반영했다.
- 중첩 시퀀스를 상위 타임라인으로 펼칠 때 내부 역할 버스의 솔로·뮤트·게인·리미터를 먼저 적용하고, 자식·부모 클립의 게인/팬 자동화를 공통 표본 키프레임으로 합성한 뒤 상위 시퀀스 버스로 보내도록 했다.
- 중첩 클립이 부모 또는 자식 시퀀스 경계와 맞닿을 때 디졸브·딥 전환 종류와 환산 길이를 펼친 합성 클립에 보존하도록 했다.
- 활성 시퀀스의 중첩 그래프를 따라 누락 참조, 자신으로 돌아오는 순환 경로, 16단계 초과 중첩을 찾고 Delivery Guard의 출력 차단 사유와 전체 경로로 표시한다.
- 클립의 기본 변형·변형 키프레임·시각 효과·시각 키프레임을 사용자 모션으로 로컬 저장하고, 다른 길이의 클립에 적용할 때 키프레임 시간을 비례 재배치한다.
- 선택 모션 템플릿을 사람이 읽을 수 있는 `.editweave-motion.json`으로 공유하거나 다시 가져오고, 가져온 템플릿은 새 ID로 저장해 기존 항목을 덮어쓰지 않도록 했다.
- 미디어 정보에 `HH:MM:SS:FF` 소스 시작 타임코드를 입력·저장하고, 선택 클립을 기준으로 다른 잠금 해제 클립을 같은 절대 소스 시각에 정렬하는 TC 동기화를 추가했다.
- 전체 파형 상관 동기화 외에 파형의 중앙값·MAD 대비 가장 뚜렷한 상승 피크를 찾아 맞추는 클랩 동기화를 추가했다. 세 동기화 방식 모두 클립 속도 램프·역재생의 공통 소스 시간 역매핑을 사용한다.
- 내보내기 창에서 현재 시퀀스 또는 예약 당시의 쇼츠 시퀀스 묶음을 실제 `queued` 작업으로 추가하고, 렌더 작업 창에서 현재 프로젝트의 대기 작업을 순서대로 시작·중지하도록 했다.
- 큐 실행 시 데스크톱 출력 폴더를 한 번만 선택하며, 비활성 시퀀스도 화면 전환 없이 해당 화면비·트랙·오디오 버스로 렌더한다. 쇼츠 묶음은 예약한 시퀀스 ID를 보존한다.
- 큐 안의 중복 파일명에는 시퀀스명·번호를 붙이고, 출력 폴더에 같은 MP4가 이미 있으면 `-2`, `-3` 접미사를 찾아 기존 파일을 덮어쓰지 않도록 했다.
- 앱 재실행 후 대기 작업은 그대로 유지하고 실행/일시정지 작업만 `중단`으로 바꾼다. 사용자는 새 출력 폴더를 선택해 남은 대기열을 다시 순차 실행할 수 있다.
- 오디오·비디오 트랙에 절대 타임라인 시간 기준 음량·팬 키프레임을 추가하고 선형·가속·감속·부드러운 보간을 공통 함수로 계산한다.
- 트랙 인스펙터에서 재생 헤드 위치의 자동화 값을 기록·수정·삭제하며, 자동화 트랙은 타임라인 헤더와 오디오 믹서에 `A/AUTO`로 표시한다. 믹서의 -14 LUFS 맞춤은 모든 트랙 자동화 음량도 같은 비율로 조절한다.
- 프로그램 미리보기의 Web Audio 게인·팬과 최종 48kHz 샘플 믹스가 같은 트랙 자동화 값을 사용하고, Delivery Guard의 빠른 에너지·피크 예상은 자동화 RMS와 최대 음량을 반영한다.
- 중첩 시퀀스를 펼칠 때 자식 트랙 시간과 부모 타임라인 시간을 변환해 두 자동화의 음량을 곱하고 팬을 합산한 표본 키프레임을 만든다.
- 상단 보이스오버 도구에서 마이크 장치를 선택하고 입력 레벨을 확인한 뒤 3초 카운트다운, 녹음 일시정지·재개, 완료를 처리하는 로컬 ADR 흐름을 추가했다.
- 녹음 완료 파일은 데스크톱 앱 데이터의 프로젝트별 폴더에 저장하고 다시 열 때 원본 경로로 자동 연결되며, 녹음 시작 당시 재생 헤드에 `대화` 역할 오디오 클립으로 자동 배치한다. 잠금 해제 오디오 트랙이 없으면 새 트랙을 만든다.
- macOS 패키지에 마이크 사용 목적 문구와 오디오 입력 entitlement를 추가해 서명된 앱에서도 권한 요청 근거를 포함했다.
- ADR 대사 큐를 테이크마다 고정하고 여러 번 연속 녹음한 뒤 각 결과를 창 안에서 미리듣기·삭제·선택할 수 있게 확장했다. 채택한 테이크 번호와 대사 큐는 타임라인 클립에 저장되어 인스펙터에서 확인·수정할 수 있다.
- 데스크톱 미디어 분석에서 FFprobe의 영상·데이터 스트림 및 포맷 태그를 조회해 컨테이너 시작 타임코드와 릴 이름을 자동 등록하도록 했다. 29.97/59.94 세미콜론 타임코드는 드롭프레임 누락 프레임 수를 반영해 절대 소스 시간으로 변환한다.
- 미디어 패널에 컨테이너 TC 출처와 릴 메타데이터를 표시하고, 사용자가 수정한 수동 TC는 재연결·재분석 시 컨테이너 값보다 우선하도록 보존했다.
- FFmpeg 프록시에 원본 포맷·영상 스트림 메타데이터를 전달하고 완성 프록시를 다시 FFprobe로 읽어 원본 시작 TC와 반 프레임 허용치로 비교한다. 불일치하면 미디어 상태와 프록시 카드에서 경고하고 원본 편집 또는 재생성을 안내한다.
- 변형 인스펙터에 위치 X/Y·크기·회전·불투명도 값 그래프를 추가했다. 보간된 곡선을 시간축에서 확인하고 빈 곳 더블클릭으로 키프레임 추가, 점 드래그로 시간·값 동시 변경, 더블클릭으로 삭제할 수 있다.
- 데스크톱 MP4 저장 완료 후 FFmpeg `ebur128=peak=true`로 완성 파일의 통합 LUFS, Loudness Range, True Peak를 스캔하고 영속 렌더 작업에 기록한다.
- 쇼츠 일괄 출력은 각 파일을 개별 실측해 LUFS 최저~최고와 최대 True Peak를 묶음 작업에 요약한다. FFmpeg/오디오 문제로 실측이 실패해도 이미 완성된 영상은 성공으로 유지하고 측정 오류만 별도로 표시한다.
- 클립별 단일 보조 센드를 최대 4개의 Aux 센드로 확장하고 각 센드에 활성화, 대상 역할 버스, -60~+12dB 레벨, 프리/포스트 페이더 위치를 저장한다. 기존 프로젝트의 단일 센드는 포스트 센드로 자동 호환한다.
- 최종 48kHz 믹스는 프리 센드를 클립·트랙 게인/팬 전의 처리 신호에서, 포스트 센드를 메인 페이더 뒤 신호에서 역할 버스로 분기한다. 미리보기·Delivery 예상·중첩 시퀀스도 같은 다중 라우팅 목록을 사용한다.
- ADR 세션을 프로젝트 데이터로 승격해 시퀀스 ID, 큐 시작·끝, 대사, 상태, 전체 테이크 자산·클립·트랙과 채택 테이크를 자동 저장·복원한다.
- 현재 세션의 모든 녹음을 프로젝트별 앱 데이터에 저장하고 각각 별도 오디오 트랙의 같은 큐 시작점에 배치한다. 채택 테이크만 활성화하고 나머지는 흐리게 표시되는 대기 레인으로 보존한다.
- ADR 창의 대본 큐 목록에서 기존 테이크를 미리듣고 즉시 채택 전환하며, 큐 구간 반복 재생과 큐·클립·미디어·앱 데이터 녹음 원본의 확인 후 일괄 삭제를 추가했다.
- 프로젝트에 저장된 ADR 큐와 테이크 레인 참조가 어긋나지 않도록 ADR 클립의 개별 삭제·분할·이동·트림을 일반 타임라인에서 제한하고, 큐 단위 관리 경로를 안내하도록 보강했다.
- 역할 버스마다 최대 6개의 하이패스·3밴드 EQ·컴프레서·리미터를 순서대로 추가하고 우회·재정렬·삭제할 수 있는 삽입 체인 랙을 오디오 믹서에 추가했다. 체인과 파라미터는 시퀀스 프로젝트 데이터 및 Undo에 보존된다.
- 프로그램 미리보기는 메인과 각 프리/포스트 Aux 신호를 별도 Web Audio 버스 체인으로 분기하고, 최종 48kHz 믹스는 역할별 신호를 먼저 합산한 뒤 같은 삽입 순서·파라미터와 출력 안전 리미터를 적용하도록 연결했다.
- 새 ADR 큐는 같은 Take 번호의 기존 ADR 트랙과 시간이 겹치지 않으면 해당 레인을 재사용하고, 겹치는 경우에만 새 전용 트랙을 만들어 장기 세션의 트랙 증가를 줄였다.
- ADR 테이크 인스펙터에 컴프 인·아웃과 `이 구간에 Take 채택` 작업을 추가했다. 기존 컴프 맵을 좌우로 비파괴 분할하고 인접한 같은 테이크 구간은 자동 병합해 프로젝트에 저장한다.
- 각 테이크 클립에 실제 채택 절대 시간 범위를 기록하고 타임라인에 청록 오버레이로 표시한다. 프로그램 Web Audio의 메인·프리 Aux와 최종 48kHz 샘플 믹스가 범위 밖 신호를 차단한다.
- ADR 컴프 경계 전후에 12ms 등가 크로스페이드 가중치를 공유 적용해 즉시 테이크 전환에서 생기는 클릭과 순간 무음을 줄였다.
- 시각 효과에 최대 8개 `EffectMask` 레이어를 추가하고 타원·둥근 사각형·다각형·베지어 도형, 더하기·빼기·교차 결합, 개별 페더·불투명도·반전·활성 상태와 순서를 프로젝트에 저장하도록 확장했다.
- 베지어 마스크 편집기에서 앵커 추가·이동·삭제와 인/아웃 핸들 곡률 조절을 제공하고, 같은 마스크 구조의 시각 효과 키프레임 사이에서 앵커·핸들·페더·불투명도를 보간한다.
- 프로그램 Canvas와 최종 출력이 같은 다중 마스크 알파 합성기를 사용하며 크롭을 최종 교차 영역으로 적용한다. 중첩 시퀀스는 부모 마스크 묶음을 자식 결과에 교차하고 모션 템플릿은 핸들을 포함해 깊은 복사한다.
- 일반 물체 추적이 기존 단일 다각형뿐 아니라 첫 활성 다각형·베지어 마스크를 이동 키프레임으로 기록하고, 나머지 마스크 레이어와 비활성 상태를 보존하도록 확장했다.
- 일반 물체 추적기를 단일 중심 패치에서 마스크 영역 네 코너 패치의 독립 적응형 추적으로 확장했다. 각 코너 변위·평균 신뢰도·추정 회전·크기를 계산하고 코너 변위를 쌍선형 보간해 앵커와 베지어 핸들에 적용하므로 이동뿐 아니라 크기·회전·제한적 원근 변화를 기록한다.
- FFprobe 컨테이너 판독에 color primaries·transfer·matrix·range를 추가하고 PQ·HLG·BT.2020 광색역 분류를 미디어 자산과 검색·상세 정보에 저장했다.
- FFmpeg 대용량 HDR 프록시는 선형화 후 Hable 톤매핑을 거쳐 Rec.709 제한 범위로 태그하며, Delivery Guard는 PQ/HLG 원본이 현재 SDR MP4 납품 경로에 들어오면 경고한다.
- 색보정 인스펙터에 최대 16개의 Primary·Curves·Qualifier·Look/LUT·HDR Tone Map 노드를 추가하는 그래프 편집기를 연결했다. 각 노드는 SOURCE 또는 앞선 복수 노드를 입력으로 선택하고 평균·더하기·곱하기·스크린 합성, 개별 Mix, 우회, 이름 변경, 재정렬과 명시적 PROGRAM OUTPUT을 프로젝트에 저장한다.
- 노드 삭제·재정렬로 존재하지 않거나 뒤쪽 노드를 참조하는 입력은 가장 가까운 유효한 이전 출력으로 정규화해 순환과 끊어진 그래프를 막는다.
- 프로그램 미리보기와 최종 프레임 출력이 크로마키→기본 색보정→RGB 곡선→HSL Qualifier→색상 노드 그래프의 같은 Canvas 처리 순서를 사용하도록 연결했다. 조정 레이어도 같은 노드 출력 경로를 사용한다.
- 중첩 시퀀스 합성 시 자식·부모 색상 노드 ID에 별도 접두어를 붙이고 모든 입력 및 출력 참조를 다시 매핑해 충돌 없이 직렬 보존한다.
- Tone Map 노드는 Hable·Reinhard·Mobius 방법과 원본/대상 피크 nit를 제공하지만 현재 8-bit SDR Canvas에서 수행되는 표시·납품용 처리다. 네이티브 10-bit PQ/HLG 출력으로 과장해 기록하지 않았다.
- 프로그램 모니터를 개별 HTML 영상 레이어의 CSS 합성에서 최종 출력과 동일한 `drawVisual`·조정 레이어·자막 Canvas 합성 경로로 전환했다. 작업 화면은 최대 960px로 제한하고 위치 변형은 시퀀스 해상도 비율로 보정한다.
- 조정 레이어의 RGB 곡선·HSL Qualifier·색상 노드가 프로그램 화면에서는 누락되고 출력에만 적용될 수 있던 경로 차이를 제거했다. 자막도 최종 출력과 같은 줄바꿈·위치·애니메이션 그리기 함수를 사용한다.
- 완성 프로그램 캔버스를 재생 중 최대 초당 4회 인스펙터에 공유하고 Waveform·RGB Parade·Histogram이 선택 원본 대신 합성 결과를 읽도록 변경했다. 정지 상태에서는 같은 편집 상태의 중복 전달을 막는 프레임 지문을 사용한다.
- FFprobe의 영상 스트림 side data에서 Content light level metadata와 Mastering display metadata를 읽어 MaxCLL·MaxFALL, RGB 원색/백색점 좌표와 mastering display 최소·최대 휘도를 프로젝트 미디어에 저장하도록 확장했다.
- 미디어 상세 정보와 검색 문맥에 HDR 정적 메타데이터를 표시하고 Delivery Guard의 SDR 납품 경고에 원본 MaxCLL·MaxFALL을 함께 제시한다.
- Canvas에서 이미 8-bit SDR로 변환된 픽셀을 단순히 10-bit PQ/HLG로 태그하는 경로는 구현하지 않았다. 네이티브 HDR은 부동소수점 합성·HDR 모니터링·10-bit 인코더 입력을 함께 갖춘 뒤에만 완료로 판정한다.
- 상단 도구에 `AI 데이터 설정`을 추가해 전사·자막, 장면 감지·초벌 제안, 배경 제거·추적, 쇼츠 리프레임별 입력 데이터와 로컬 처리·프로젝트 보관 범위를 표시한다.
- 선택적 ComfyUI는 EditWeave 밖의 별도 프로세스로 분류하고 기본 전송 동의를 꺼 두었다. 전역 설정과 실제 워크플로 창의 동의 체크가 모두 같은 영속 설정을 사용하며, 동의가 없으면 실행 함수에서도 한 번 더 차단한다.
- ComfyUI 주소가 localhost여도 워크플로 내부 노드가 외부 API를 호출할 수 있음을 고지하고, 선택 이미지·워크플로 전송과 마지막 결과 이미지의 프로젝트 보관 범위를 명시했다.
- 전사 문장의 RMS와 zero-crossing 두 값으로 화자 1/2를 나누던 임계값 휴리스틱을 제거했다.
- 각 전사 구간에서 Hann window 음성 프레임을 최대 96개 표본화하고 12개 주파수대 Goertzel 에너지, log RMS, zero-crossing, 70~320Hz 자기상관 피치와 주기성의 평균·표준편차를 정규화한 `acoustic-v1` 임베딩을 계산한다.
- 임베딩 코사인 유사도로 최대 4개 화자 중심을 온라인 갱신하고 짧은 고립 전환을 양쪽 화자에 맞춰 평활화한다. 화자 번호·유사도 신뢰도·임베딩 버전은 대본 구간에 저장하며 화자 이름은 계속 사용자가 직접 수정할 수 있다.
- `creator-feedback-v1` 프로젝트 프로필을 추가해 침묵·군더더기·반복·하이라이트 유형별 적용/유지 횟수와 누적 구간 길이를 저장한다.
- AI 초벌 후보의 기본 점수에 유형별 베타 사전분포 수용률을 반영하고 피드백이 3건 이상이면 표시 임계값도 조절한다. 이유 문구에는 반영 건수와 평균 적용 길이를 표시한다.
- AI 패널에 현재 프로젝트 피드백 건수와 `학습 초기화`를 추가했다. 프로필은 자동 저장·프로젝트 파일 열기·새 프로젝트 초기화 경로에 포함하며 외부 서버로 전송하지 않는다.
- 대본 문장의 질문, 수치·목록, 핵심어, 느낌표·인용 강조와 길이·초반 위치를 조합해 설명 가능한 하이라이트 후보를 생성하고 카드에 개인화 후 점수를 표시한다.
- 하이라이트의 `적용`은 타임라인을 삭제하지 않고 시작 시간에 노란 챕터 마커를 추가한다. `유지`와 챕터 적용 결과는 다른 초벌 유형과 동일하게 프로젝트별 피드백에 반영한다.
- 검토 코멘트를 프로젝트 ID·이름·시퀀스 ID·내보낸 시각과 함께 `.editweave-review.json`으로 저장하고 다시 병합하는 오프라인 검토 패키지를 추가했다.
- 가져온 코멘트는 동일 ID의 내용·상태·시간·작성자 변경을 갱신하고, ID가 달라도 시간·작성자·내용·상태가 같은 항목은 중복 추가하지 않는다. 다른 프로젝트 패키지는 사용자 확인 뒤 현재 타임라인 시간 기준으로 병합한다.
- 기존 CSV는 읽기 쉬운 전달 형식으로 유지하고, 재병합 가능한 검토 패키지는 별도 버튼으로 분리했다.
- 업데이트 확인 코드가 운영 도메인에서 HTTP 매니페스트·다운로드 URL을 거부하고 HTTPS만 허용하도록 보강했다. 개발용 localhost는 예외로 유지하고 SHA-256 값이 제공되면 64자리 16진수 형식을 검사한다.
- `release/update-manifest.example.json`과 릴리스 입력 설명을 추가하고 Tauri 번들 게시자·저작권 메타데이터를 명시했다. 자동 설치는 서명된 updater 산출물과 운영 서버가 없는 상태에서 완료로 표시하지 않는다.

### 미디어·복구

- 데스크톱의 256MB 초과 영상은 빈 메모리 자리표시자와 파일 범위 읽기로 분석하고 자동 프록시를 생성하도록 바꿨다. 대용량 오디오는 영상 스트리밍 조건에서 제외해 정상 파일로 읽는다.
- 대용량 영상은 프록시가 준비되기 전 타임라인 추가를 막고, 준비 후 프로그램 영상과 내장 오디오가 모두 같은 프록시를 사용하게 했다. 최종 출력은 계속 원본 경로를 읽는다.
- 디스크 프록시 복원 시 전체 파일을 `readFile`로 메모리에 적재하던 경로를 앱 캐시 범위로 제한한 Tauri asset protocol 재생으로 교체했다. 전사는 실행 시점에만 프록시 바이트를 읽는다.
- 렌더 시작 직후 선택한 출력 파일·폴더 경로를 복구 레코드와 작업 목록에 기록해 비정상 종료 뒤 남은 부분 파일의 위치를 찾을 수 있게 했다.
- 프로젝트 파일 모듈에서 누락됐던 `readFile` 가져오기를 복구했다.
- Tauri persisted scope를 등록해 사용자가 선택한 원본 경로 권한을 재실행 뒤 복원하도록 했다.
- 미디어 검색에 메모, 코덱, 방향, 해상도, 얼굴, VFR·프록시 진단 문맥을 포함했다.
- 자동 저장 실패를 복구 가능한 상태로 처리하고 이력 체크포인트를 30초 간격으로 보존한다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않았다.

## 2026-08-09 — 유지율 기반 의미 하이라이트와 영상 마스크 안정화

- 영상 전경 실루엣의 각 목표 높이에서 가장 가까운 유효 알파 행을 찾아 항상 좌·우 12쌍, 총 24개 점을 만들도록 보강했다. 시간 표본마다 점 개수가 달라 보간이 끊기는 경우를 제거했다.
- 따옴표 포함 CSV, 초·시각 문자열·0~1 위치 비율·백분율 위치와 0~1/백분율 유지율을 판독하는 YouTube 유지율 가져오기를 추가했다. 현재 시퀀스 실사용 길이에 정렬한 표본과 원본 파일명을 프로젝트 프로필에 저장한다.
- 로컬 `Xenova/multilingual-e5-small`로 대본을 16문장씩 임베딩하고 핵심 결론·호기심·실행 팁 의미 앵커, 유지율 상위 문장의 의미 중심, 기존 설명형 점수를 결합하는 하이라이트 추천을 추가했다.
- 의미 분석으로 생성한 하이라이트에는 벡터를 함께 보존한다. 챕터로 적용한 벡터와 유지한 벡터를 각각 누적 중심으로 학습해 다음 분석의 양성·음성 의미 점수에 반영한다.
- AI 패널에 유지율 CSV 상태, 모델 다운로드·대본 분석 진행률과 단계 문구를 추가했다. 모델 준비가 실패하면 기본 초벌 후보를 버리지 않고 결과로 남긴다.
- AI 데이터 설정에 유지율 CSV와 의미 임베딩의 입력·로컬 처리·프로젝트 저장 범위를 명시했다. ComfyUI는 이 경로와 무관하며 계속 선택적 외부 이미지 워크플로로만 유지한다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않았다.

## 2026-08-09 — 앱 종료 후 렌더 체크포인트 재개

- 데스크톱 단일 시퀀스 렌더를 30초 단위 독립 MP4 구간으로 나누고 각 구간이 완전히 finalize된 뒤에만 완료 개수를 복구 레코드에 기록하도록 변경했다.
- 작업 ID·프로젝트·시퀀스·출력 설정·펼친 타임라인·오디오 버스를 fingerprint로 묶어 동일 작업일 때만 체크포인트를 재사용한다. 편집 내용이나 출력 설정이 달라지면 오래된 구간을 정리하고 처음부터 안전하게 렌더한다.
- 재실행 시 복구 레코드의 완료 개수와 앱 캐시에 실제 존재하는 구간 파일을 교차 확인한다. 중간 번호가 없거나 미완성 파일이면 그 지점부터 다시 만들며 이후 확정 구간을 잘못 신뢰하지 않는다.
- 모든 구간의 H.264/H.265와 AAC encoded packet을 Mediabunny의 packet source/sink로 시간 오프셋해 최종 MP4에 재다중화한다. FFmpeg 재인코딩이나 전체 파일 메모리 적재 없이 결합하고 성공 뒤 렌더 세션 캐시를 삭제한다.
- 결합 중 앱이 종료되면 구간 캐시는 남고 최종 파일만 다시 만들 수 있다. 취소·실패 작업을 렌더 목록에서 제거할 때 해당 작업 캐시도 정리한다.
- 쇼츠 일괄 출력은 완료된 시퀀스 ID와 파일 경로를 복구 레코드에 저장한다. 재시도 시 파일 존재를 확인한 완료 쇼츠는 건너뛰고 미완료 시퀀스부터 이어가며, 건너뛴 파일도 러드니스를 다시 수집한다.
- Tauri 앱 캐시 권한에 `render-sessions` 범위를 추가하고 세션 경로 생성·완료 구간 검사·정리 명령을 연결했다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않았다.

## 2026-08-09 — 실제 HEVC Main10 HDR 출력 기반

- 내보내기 설정에 SDR Rec.709 8-bit, HDR10 PQ 10-bit, HLG 10-bit를 추가했다. HDR 선택 시 H.264 선택값을 HEVC로 강제하고 `hvc1.2.4.L153.B0` Main10 인코더 capability가 없으면 태그만 붙인 파일을 만들지 않고 오류로 중단한다.
- WebGPU compute shader가 현재 합성 프레임의 sRGB 값을 선형화하고 Rec.709→BT.2020 행렬 변환한 뒤, PQ는 203 nit reference white·10,000 nit absolute scale, HLG는 scene reference OETF를 적용한다.
- shader가 BT.2020 non-constant luminance YCbCr를 limited-range 10-bit 코드값으로 계산하고 Y 2픽셀, U/V 2개 chroma sample을 각각 32-bit word에 GPU에서 패킹해 `I420P10` 평면을 만든다. 4K 프레임 전체를 JavaScript 픽셀 반복문으로 변환하지 않는다.
- 10-bit 평면을 Mediabunny raw `VideoSample`과 `VideoSampleSource`로 HEVC Main10 인코더에 직접 전달하고 샘플 색공간을 BT.2020/PQ 또는 BT.2020/HLG/BT.2020-NCL/limited range로 기록한다.
- PQ↔PQ 또는 HLG↔HLG, BT.2020 `I420P10`, 출력과 동일한 크기·회전 0인 straight-cut 프레임은 변형·색상·효과·전환·조정 레이어·자막·복수 레이어가 전혀 없을 때 원시 Y/U/V 평면과 color space를 새 출력 타임스탬프로 복사한다. 이 경로는 기존 Canvas와 SDR 톤매핑을 실제로 우회한다.
- YouTube HDR10 4K와 HLG 4K 기본 출력 프리셋, 렌더 큐의 SDR/HDR 표시, WebGPU·Main10 요구사항 안내를 추가했다. 체크포인트 구간 결합은 decoder configuration까지 비교해 Main10 초기화 데이터가 다른 파일을 잘못 연결하지 않는다.
- 이 단계는 10-bit YUV 생성·인코딩과 조건부 원본 10-bit straight-cut 보존을 실제로 수행한다. 크기 변경이나 효과가 있는 HDR 원본은 아직 기존 SDR Canvas 결과를 PQ/HLG로 매핑하므로, 전체 색상 노드·마스크를 RGBA16F에서 처리하는 것은 완료로 표시하지 않고 다음 작업으로 유지한다.

## 2026-08-09 — HDR10 정적 마스터링 메타데이터 보존

- 실제 렌더 타임라인이 참조하는 PQ 원본에서 완전한 SMPTE ST 2086 mastering display와 존재하는 MaxCLL·MaxFALL을 수집하는 출력 메타데이터 계층을 추가했다. 서로 다른 소스의 광량 정보는 최댓값을 사용하고, 누락 필드는 임의의 추정값으로 채우지 않는다.
- 데스크톱 네이티브 계층이 완성 MP4의 HEVC `hvc1`/`hev1` visual sample entry에 ISO BMFF `mdcv`·`clli` box를 스트리밍 삽입한다. 삽입으로 커진 sample entry와 모든 상위 atom의 크기, 삽입점 뒤의 `stco`·`co64` chunk offset을 함께 보정하고 임시 파일 교체 실패 시 원본을 복구한다.
- 단일 렌더는 체크포인트 구간을 최종 파일로 결합한 뒤 메타데이터 기록에 성공해야 구간 캐시와 복구 기록을 정리한다. 쇼츠 일괄 렌더도 각 파일의 메타데이터 기록이 끝난 뒤에만 해당 시퀀스를 완료 체크포인트로 저장한다.
- HLG에는 HDR10 정적 메타데이터를 억지로 기록하지 않으며, PQ 원본에도 값이 없으면 컨테이너 색공간 표지만 유지한다. 전체 HDR 다중 레이어 RGBA16F 합성은 계속 미완료 범위로 남긴다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사, Rust 검사와 패키징은 수행하지 않고 정적 소스 연결만 확인했다.

## 2026-08-09 — 업로드 없는 LAN 검토 링크

- 기존 CSV와 `.editweave-review.json` 교환에 더해, 데스크톱 앱 안에서 임의 포트로 실행되는 검토 HTTP 서버와 128-bit 세션 토큰을 추가했다. 사용자가 선택한 완성 MP4만 동일 네트워크에 제공하며 원격 서버나 ComfyUI로 영상을 전송하지 않는다.
- 검토 브라우저는 MP4 byte range를 지원해 탐색 가능한 영상을 재생하고, 현재 재생 시점에 작성자·코멘트를 추가하며 텍스트 수정과 열림/해결 상태를 저장한다. 별도 EditWeave 설치나 계정은 필요하지 않다.
- 편집 앱은 3초마다 현재 시퀀스 코멘트와 세션을 양방향 병합한다. 코멘트 ID와 `updatedAt` ISO 시각으로 최신 변경을 선택하고, 편집자 삭제 ID는 서버 확인 전까지 tombstone으로 재전송해 진행 중 동기화와 겹쳐도 삭제 항목이 부활하지 않도록 했다.
- 서버 입력은 토큰 형식, MP4 존재·최소 크기, 요청 본문 4.2MB, 세션 16개, 코멘트 10,000개와 필드 길이를 제한한다. 검토 페이지는 코멘트를 DOM 텍스트와 input value로만 표시하고 영상 외 로컬 경로는 제공하지 않는다.
- 현재 링크는 편집 앱이 실행 중인 동일 LAN 전용이다. TLS·공용 인터넷 릴레이·영구 검토 이력과 팀 미디어 잠금은 운영 인프라가 필요한 다음 단계로 명시했다.
- 사용자 요청에 따라 앱 실행, 브라우저 접속, 자동 테스트, 타입 검사, Rust 검사와 패키징은 수행하지 않고 정적 소스 연결만 확인했다.

## 2026-08-09 — 프로젝트 버전 비교와 비파괴 분기 병합

- 프로젝트 버전 기록에서 과거 자동 저장을 현재 작업과 비교해 추가·제거된 미디어/시퀀스 수, 내용이 달라진 시퀀스, 현재→과거 클립 수와 ID 기준 코멘트 차이를 표시한다.
- 기존 전체 복원과 별도로 `새 분기로 가져오기`를 추가했다. 선택한 과거 시퀀스가 참조하는 source sequence와 nested sequence를 재귀 수집해 현재 시퀀스를 덮어쓰지 않고 나란히 추가한다.
- 분기 과정에서 시퀀스·트랙·클립 ID와 ADR 큐·테이크·컴프 ID를 새로 만들고 nested/source, clip-track, ADR cue/take/clip/track/comp 내부 참조를 새 ID로 다시 연결한다. 현재 프로젝트에 없는 미디어 항목만 과거 버전에서 보충하고 기존 미디어 경로·조직 정보는 우선 보존한다.
- 가져온 루트 시퀀스를 즉시 활성화하고 현재 프로젝트 ID, 다른 시퀀스, 크리에이터 학습 프로필과 현재 사용자 사전을 유지한다. 원본 경로가 없는 과거 미디어는 기존 재연결 흐름에서 오프라인으로 명시된다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않고 정적 소스 연결만 확인했다.

## 2026-08-09 — raw 10-bit HDR 선형 변형 경로

- Mediabunny가 디코드한 PQ/HLG·BT.2020 `I420P10` 원본 plane을 JavaScript RGBA나 Canvas로 변환하지 않고 WebGPU storage buffer에 직접 올리는 `HdrRawFrameTransformer`를 추가했다.
- Compute shader는 plane offset/stride와 visible rect를 사용해 10-bit limited-range YCbCr를 bilinear 표본화하고 BT.2020 RGB로 복원한 뒤 PQ ST 2084 또는 HLG 역함수로 선형화한다. 위치·크기·회전을 역매핑하고 불투명도/검정 배경을 선형 광에서 합성한 다음 원래 transfer와 10-bit I420P10으로 다시 패킹한다.
- 단일 HDR 영상 레이어에 색상·마스크 효과와 자막/조정 레이어가 없으면 원본·출력 해상도가 달라도 이 경로를 사용한다. 변형 키프레임, crossfade와 dip-black의 시간축 opacity도 공통 `resolveClipTransform` 결과를 사용하며, 완전한 무변형·동일 크기 프레임은 기존 무복사에 가까운 raw retimestamp 경로를 유지한다.
- dip-white, 색상 보정·노드, 크롭·마스크·블러·모자이크·크로마키, 복수 영상 레이어와 자막은 아직 SDR Canvas 합성 결과를 HDR로 배치한다. 전체 RGBA16F 다중 레이어 효과 파이프라인은 완료로 표시하지 않았다.
- 사용자 요청에 따라 앱 실행, GPU 셰이더 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않고 정적 소스 연결만 확인했다.

## 2026-08-09 — 권한 없는 선언형 Creator Pack

- 사용자 모션 템플릿, cubic Bézier 속도 템플릿, 역할 버스·삽입 체인·ADR 기본값이 포함된 팀 오디오 템플릿, 사용자 출력 프리셋을 하나의 `editweave-creator-pack-v1` JSON으로 내보내고 설치하는 확장 계층과 전용 UI를 추가했다.
- Pack manifest는 실행 코드, 네트워크 접근, 파일시스템 접근을 모두 명시적 `false`로 요구한다. 형식이 다르거나 권한을 요구하면 설치하지 않으며 파일 크기 2MB, 모션/속도/출력 각 100개, 오디오 50개와 각 기존 템플릿 스키마의 필드·범위를 다시 검증한다.
- 가져온 모션·속도·오디오·출력 항목은 새 ID로 인스턴스화한다. 이름과 ID/생성시각을 제외한 설정 지문이 동일하면 중복 추가하지 않으며, 설치 이벤트로 현재 열린 인스펙터·오디오 믹서·내보내기의 목록을 즉시 다시 읽는다.
- 사용자 출력 프리셋 자체의 로컬 저장/읽기에도 해상도·코덱·HDR 모드·fps·비트레이트·하드웨어 선호 검증과 HDR→HEVC 강제를 적용해 Pack 이외의 손상된 저장 데이터도 걸러낸다.
- 네이티브 바이너리나 임의 JavaScript 플러그인은 허용하지 않는다. 향후 코드형 확장은 서명, 권한 프롬프트, 프로세스 격리가 설계되기 전까지 제품 범위 밖이다.
- 사용자 요청에 따라 앱 실행, Pack 설치, 자동 테스트와 타입 검사는 수행하지 않고 정적 소스 연결만 확인했다.

## 2026-08-09 — 동의형 충돌 큐와 재전송

- 기존 React Error Boundary 단일 전송을 앱 전역 충돌 파이프라인으로 확장했다. React 렌더 오류, window error, unhandled Promise rejection을 종류와 함께 기록하고 동일 message·상위 stack이 5초 안에 반복되면 중복 큐 적재를 억제한다.
- 최대 10개 리포트의 message/stack/component stack/user agent 길이를 제한해 로컬 저장하고, 사용자가 동의한 경우에만 전송한다. 미디어·프로젝트 데이터와 로컬 파일 경로는 수집 구조 자체에 없다.
- endpoint는 HTTPS만 허용하고 개발용 localhost만 HTTP 예외로 둔다. 성공 HTTP 응답을 받은 리포트만 큐에서 삭제하고 실패하면 30초부터 최대 24시간까지 지수 backoff와 시도 횟수를 저장해 앱 재시작 뒤 이어서 전송한다.
- 충돌 직후 동의를 켜면 현재 큐를 즉시 전송하며, 앱 시작 때도 동의와 endpoint가 모두 있을 때 미전송 큐를 비동기로 비운다. 사용자가 선택하는 JSON 수동 저장은 동의·서버 설정과 무관하게 유지했다.
- 실제 운영 endpoint와 서버 보존 정책은 아직 제공되지 않았으므로 외부 운영 준비 항목으로 유지한다. 사용자 요청에 따라 네트워크 전송, 오류 유발, 자동 테스트와 타입 검사는 수행하지 않았다.

## 2026-08-09 — 공유 프로젝트 파일 lock과 heartbeat

- 데스크톱 프로젝트 경로 옆에 `.<파일명>.editweave-lock`을 두는 네이티브 잠금 계층을 추가했다. 새 잠금은 `create_new`로 원자 생성하고 instance UUID, OS 사용자·장비 이름, process ID, 획득 시각과 heartbeat 시각을 JSON으로 기록한다.
- 앱은 잠긴 프로젝트를 열거나 첫 저장 경로로 사용할 때 소유자와 활성/만료 상태를 표시한다. 사용자 확인 없이는 인수하지 않으며, 확인한 경우에만 기존 잠금을 교체한다. 잠금 파일이 손상됐어도 자동 삭제하지 않고 명시적 인수를 요구한다.
- 소유 앱은 15초마다 heartbeat를 갱신하고 45초가 지난 잠금을 만료로 판정한다. 다른 장비가 강제 인수하면 이전 앱의 다음 heartbeat가 instance ID 불일치를 감지해 현재 프로젝트 경로와 잠금 상태를 해제하고 다른 파일 저장을 안내한다.
- 프로젝트를 바꾸거나 새 프로젝트를 만들거나 앱을 닫을 때 자신이 소유한 잠금만 삭제한다. 동일 경로의 일반 저장은 파일 선택창을 다시 열지 않고 잠금을 갱신한 뒤 기록하며, 브라우저 파일 열기는 기존 데스크톱 잠금을 먼저 해제한다.
- 이 잠금은 SMB/NAS/동기화 폴더가 원자적 파일 생성을 보장한다는 전제의 동시 저장 방지 장치다. 프록시 서버와 동일 시퀀스의 실시간 병합은 별도 운영 인프라 범위다.
- 사용자 요청에 따라 복수 장비, 네트워크 공유 드라이브, 자동 테스트, Rust/TypeScript 검사는 수행하지 않고 정적 소스 연결만 확인했다.
- 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 패키징은 수행하지 않았다.

## 2026-08-09 — HDR 다중 레이어 RGBA16F 선형 합성

- `HdrLinearCompositor`를 추가해 PQ/HLG·BT.2020 limited-range `I420P10`의 Y/U/V plane을 직접 읽고 역전송 함수로 선형화한 뒤, 두 개의 `rgba16float` WebGPU texture를 ping-pong하며 복수 영상 레이어를 source-over 합성한다.
- raw HDR 클립은 각 레이어의 위치·크기·회전·불투명도 키프레임과 crossfade/dip-black을 선형 광에서 처리한다. 동일 해상도·무변형·무효과 straight cut은 기존 raw retimestamp 빠른 경로를 유지한다.
- 이미지·비 HDR 영상·모션 그래픽·자막은 전체 프로그램 화면을 8-bit로 내리지 않고 투명 Canvas 레이어 하나만 GPU에 올린다. sRGB/Rec.709를 BT.2020으로 변환하고 PQ는 203 nit, HLG는 75% diffuse white에 해당하는 선형 값으로 매핑해 아래 HDR 원본의 하이라이트와 10-bit 정밀도를 보존한다.
- 최종 RGBA16F 프레임은 PQ/HLG OETF, BT.2020-NCL limited range와 4:2:0 chroma 표본화를 거쳐 `I420P10`으로 직접 패킹한다. 최초 연결 뒤 기본 그레이딩·마스크·크로마키를 같은 경로로 확장했으며, 사용자 RGB 곡선·Qualifier·색상 노드·블러·그림자·모자이크·조정 레이어는 후속 RGBA16F 효과 단계로 남겼다.
- raw HDR 레이어의 노출·대비·채도·색온도·틴트·하이라이트·섀도·Hue/Vibrance/Fade·Lift/Gamma/Gain·3구간 톤을 선형 BT.2020에서 계산하고, 크롭·비네트와 Normal/Multiply/Screen/Overlay/Darken/Lighten 합성을 같은 shader 경로에 추가했다.
- 기존 ellipse/rounded/polygon/Bezier 마스크 렌더러로 최대 8개 add/subtract/intersect·반전·불투명도·페더 결과의 알파만 출력 크기 `rgba8unorm` texture에 전달하고 raw shader의 레이어 alpha에 곱한다. 색상 채널은 Canvas를 거치지 않는다. 사용자 RGB 곡선·Qualifier·색상 노드와 블러·그림자·모자이크는 레이어별 Canvas 대체, 조정 레이어는 전체 Canvas 대체를 유지한다.
- 이미지·비 HDR 영상처럼 투명 Canvas에서 그리는 레이어도 GPU에 올린 뒤 선형 BT.2020 버퍼의 아래 레이어와 합성 모드를 계산하도록 `addCanvas`에 동일한 여섯 blend mode를 연결했다. 빈 투명 Canvas에서 미리 blend한 결과를 일반 합성하는 오류를 피한다.
- raw HDR chroma key를 추가했다. 사용자가 고른 RGB 키 색상과 tolerance/softness를 PQ/HLG 역함수 전의 인코딩 신호에서 비교해 alpha를 만들고, spill 값은 반투명 경계의 색만 무채색 방향으로 줄인다. 키잉 뒤 남은 HDR RGB는 그대로 선형 그레이딩·RGBA16F 합성으로 전달된다.
- 합성된 RGBA16F 프로그램 전체에 기본 색상·톤·비네트를 적용하는 adjustment compute pipeline과 96-byte uniform layout을 추가했다. 여러 조정 레이어는 타임라인 순서대로 ping-pong texture에 누적한다.
- 내보내기는 활성 조정 레이어가 기본 그레이딩·블러 범위일 때 HDR 선형 경로를 유지한다. 사용자 RGB 곡선·Qualifier·색상 노드·LUT 또는 그림자가 있는 조정 레이어만 기존 전체 Canvas 대체 경로로 제한한다.
- raw HDR 레이어는 원본 Y/U/V plane의 중심·축·대각 9개 표본을 출력 변형 배율에 맞춰 읽는 10-weight blur kernel을 적용한 뒤 크로마키·PQ/HLG 역함수·그레이딩으로 전달한다. 블러가 0이면 단일 표본 빠른 분기를 유지한다.
- 조정 레이어도 현재 RGBA16F 프로그램 텍스처의 중심·축·대각 9개 표본을 경계 clamp와 함께 읽어 블러 뒤 기본 그레이딩을 적용한다. 따라서 클립 또는 기본 조정 레이어 블러만으로 전체 HDR 프레임이 Canvas로 내려가지 않는다.
- Master/R/G/B 사용자 곡선을 각 256개 float 표본으로 정규화해 4KB WebGPU storage LUT에 기록하고, 채널 곡선 뒤 Master 곡선을 적용하는 기존 편집 계약을 raw HDR와 조정 레이어 shader에 연결했다. 모든 곡선이 identity이면 LUT 표본을 생략해 1.0을 넘는 중간 HDR 선형 값이 불필요하게 clamp되지 않는다.
- HSL Qualifier는 순환 hue 거리, saturation/luminance band와 softness를 계산하고 선택 영역에 exposure·saturation·hue shift를 가중 적용한다. raw 클립과 조정 레이어가 같은 파라미터 범위와 처리 순서를 사용하며 최종 출력에서는 편집용 selection mask 표시를 강제로 굽지 않는다.
- 최대 16개 색상 노드를 위한 고정 크기 meta/value/curve storage buffer와 RGBA16F 노드 실행기를 추가했다. SOURCE·앞선 노드 복수 입력, 평균·Add·Multiply·Screen, 우회, 개별 Mix와 명시적 PROGRAM OUTPUT을 순서 보존해 실행하며 Primary·Curves·Qualifier·Look·Hable/Reinhard/Mobius Tone Map을 raw HDR 클립 및 조정 레이어에 같은 그래프로 적용한다.
- 노드 다중 입력은 기존 Canvas 계약과 같은 누적 가중치를 사용하고, Look 노드는 기본 색상 조정 뒤 내장 look을 적용하며 Qualifier 비활성 상태도 보존한다. 클립 자체의 기본 LUT는 노드 Look과 별도이므로 아직 Canvas 대체로 유지한다.
- 기본 Cinematic/Warm/Cool/Mono Look을 선형 BT.2020 밝기·대비·채도·색상 행렬 연산으로 이식하고, 클립 기본 LUT와 조정 레이어 LUT를 색상 노드 전에 적용한다. 노드 Look도 같은 연산을 공유해 HDR 클립의 LUT 때문에 Canvas로 내려가던 조건을 제거했다.
- raw 클립의 crop·다중 마스크·크로마키 결과 알파를 화면 좌표에서 다시 표본화하는 9점 그림자 커널을 추가했다. 오프셋·blur·opacity를 반영한 검정 그림자를 원본 레이어 아래에서 premultiplied alpha로 합친 뒤 기존 선형 blend mode에 전달해 원본 HDR RGB를 8-bit로 변환하지 않는다.
- `faceTrack`의 현재 소스 시간 최근접 좌표를 raw 합성기로 전달하고, 추적 중심의 원본 YUV 영역을 16×16 셀로 양자화해 위치·크기·회전 뒤에도 얼굴 모자이크가 같은 소스 영역을 가리키도록 했다. 모자이크 셀도 PQ/HLG 역함수와 색상 파이프라인을 그대로 통과한다.
- 현재 `ColorAdjustment`·`VisualEffects` 범위는 raw HDR 셰이더에서 처리하므로 효과 존재 여부에 따른 Canvas 대체 제한을 제거했다. raw 형식·색공간·회전 메타데이터가 맞지 않거나 별도 흰색 전환인 레이어만 기존 투명 Canvas 대체를 유지한다.
- 사용자 요청에 따라 앱 실행, GPU 셰이더 실행, 자동 테스트, 타입 검사, Rust 검사와 패키징은 수행하지 않고 정적 소스 연결과 문서만 갱신했다.

## 2026-08-09 — Ed25519 서명 업데이트 매니페스트

- 운영 업데이트 확인은 `editweave-update-v1` schema와 고정 순서 canonical payload를 사용하고, 앱 빌드에 포함된 32-byte Ed25519 공개 키로 Base64 서명을 검증한 뒤에만 새 버전과 다운로드 URL을 표시하도록 변경했다. localhost 개발만 공개 키·서명 없는 매니페스트를 허용한다.
- Windows/macOS 데스크톱은 `ed25519-dalek` 기반 Tauri 명령으로 서명을 우선 확인해 WebView의 Ed25519 지원 여부에 의존하지 않는다. 일반 브라우저 개발 화면은 같은 payload의 WebCrypto 검증을 사용한다.
- 매니페스트 schema·SemVer·channel·날짜·최소 지원 버전·안내 길이·SHA-256·keyId를 검증하고, 64KB 초과 응답·HTTP 운영 URL·리다이렉트·URL 자격 증명·운영 매니페스트의 localhost 다운로드를 거부한다. fetch에는 자격 증명과 referrer를 보내지 않는다.
- Node 내장 crypto만 사용하는 `release/sign-update-manifest.mjs`를 추가했다. 저장소 밖의 Ed25519 PEM 개인 키로 canonical payload를 서명하고, 공개 키 지문의 keyId와 앱 빌드용 Base64 raw 공개 키를 출력한다. 개인 키는 앱·매니페스트·소스에 기록하지 않는다.
- `.env.example`, 매니페스트 예시와 릴리스 절차에 공개 키·keyId, 키 생성·서명·교체 순서를 반영했다. 설치 파일 자동 교체는 별도 Tauri updater `.sig`, Windows/macOS 코드 서명·공증과 운영 서버가 준비되기 전까지 활성화하지 않는다.
- 릴리스 빌드는 Cargo 환경의 `EDITWEAVE_UPDATE_PUBLIC_KEY`를 Rust 바이너리에 고정한다. 네이티브 검증·다운로드 명령은 렌더러가 보낸 공개 키가 고정 키와 다르면 거부하고, release 바이너리에 키가 없으면 실패 폐쇄한다. debug 빌드만 전달 키를 허용한다.
- 사용자가 저장 경로를 고르면 Rust가 서명 payload를 다시 검증하고 그 안의 URL·SHA-256만 사용해 설치 파일을 PID별 `.part`로 스트리밍한다. 최대 2GB, 연결 20초·전체 30분, 5회 안전 리다이렉트, 빈 파일과 쓰기 실패를 제한하며 해시 불일치 시 임시 파일을 제거한다.
- 검증 성공 파일은 최종 경로로 옮긴 뒤 프로세스 내부에 30분·1회용 실행 토큰으로 등록한다. 사용자가 다시 실행을 승인하면 토큰을 소비하고 같은 파일의 SHA-256을 재계산한 뒤 Windows EXE/MSI 또는 macOS DMG/PKG만 연다. 네이티브 실행 명령은 임의 경로·해시를 받지 않는다.
- 서명 payload에 `windows-x86_64`·`windows-aarch64`·`macos-x86_64`·`macos-aarch64`·`macos-universal` platform을 포함하고 프런트와 Rust 양쪽에서 현재 OS·CPU와 일치하는지 검사한다. 플랫폼도 Ed25519 canonical payload에 포함되어 다른 대상 설치 파일로 바꿀 수 없다.
- Windows 다운로드는 숨김 PowerShell의 `Get-AuthenticodeSignature` 상태가 `Valid`이고 인증서 Subject가 빌드 시 `EDITWEAVE_UPDATE_SIGNER_SUBJECT`와 일치해야 등록한다. macOS DMG는 `codesign --verify --deep --strict`와 Gatekeeper open assessment, PKG는 `pkgutil --check-signature`와 Gatekeeper install assessment를 통과하고 `EDITWEAVE_UPDATE_APPLE_TEAM_ID`가 서명 정보에 있어야 한다.
- OS 서명·게시자 판정은 다운로드 직후와 실행 직전 두 번 수행한다. 첫 판정에 실패하면 최종 다운로드 파일을 제거하고 토큰을 발급하지 않으며, release 빌드에 예상 Subject 또는 Team ID가 없으면 실패 폐쇄한다.
- 검증 완료 설치 파일의 canonical payload·서명·경로·이전/목표 버전과 실행 세션을 `editweave-update-attempt-v1`으로 최대 7일 보존한다. 실행을 승인하기 전에 상태를 기록하고 명령 실패 시 ready 상태로 되돌리므로 설치 프로그램이 현재 앱을 빠르게 종료해도 시도 기록이 남는다.
- 새 앱 프로세스는 실제 번들 버전을 읽어 목표 버전 이상이면 적용 성공으로 기록을 제거한다. 다른 세션에서 10분 유예 뒤에도 이전 버전이면 적용되지 않은 것으로 알리고 ready 상태로 되돌려 같은 경고를 매 시작마다 반복하지 않는다.
- 다음 업데이트 확인에서 서버의 현재 서명 payload와 저장 기록이 정확히 일치하면 기존 파일 재사용을 제안한다. Rust `prepare_existing_update_installer`가 빌드 고정 공개 키의 Ed25519 서명, OS/CPU, 파일 SHA-256과 Authenticode/Developer ID를 다시 확인한 뒤에만 새 1회 실행 토큰을 반환한다. 로컬 저장소의 경로·상태는 신뢰 경계로 사용하지 않는다.
- 사용자 요청에 따라 서명 도구 실행, 네트워크 확인, 자동 테스트, 타입 검사와 패키징은 수행하지 않고 정적 소스 연결과 문서만 갱신했다.

## 2026-08-09 — 운영형 충돌 수집 서버

- `services/crash-collector/server.mjs`에 외부 런타임 의존성이 없는 Node 수집 서비스를 추가했다. `POST /api/editweave/crashes`는 `editweave-crash-v1`의 허용 필드, UUID, 발생 시각, 오류 종류와 문자열 상한을 엄격히 검사하고 `/healthz`를 제공한다.
- 기본 Tauri origin 허용 목록, 선택형 신뢰 프록시, IP별 분당 속도 제한과 32KB 요청 상한을 적용했다. IP는 속도 제한용 메모리에만 두고 JSONL·서버 로그에는 저장하지 않으며 쿠키와 요청 헤더도 영속화하지 않는다.
- 리포트를 날짜별 JSONL에 단일 쓰기 큐로 직렬 append하고, 프로세스 재시작 시 보존 파일의 최근 ID를 복원해 같은 ID를 중복 저장하지 않는다. 기본 30일·512MB 한도를 초과하면 가장 오래된 날짜 파일을 정리하고 현재 파일만으로 상한을 넘으면 507로 실패 폐쇄한다.
- 클라이언트는 설치된 Tauri 앱 버전을 읽고 `schema`, runtime, 오류 이름을 전송한다. 자격 증명·referrer·redirect를 제외한 12초 제한 요청만 사용하며 서버 성공 응답 뒤에만 로컬 큐에서 제거한다.
- 로컬 큐 적재와 전송 직전, 서버 수신 뒤의 세 단계에서 로컬/UNC 경로, 홈 사용자명, URL query/hash, 이메일, UUID와 따옴표 안 동적 값을 제거한다. 프로젝트·미디어·렌더 설정 필드는 수집 스키마에 존재하지 않는다.
- 비루트 Node 22 Dockerfile, 읽기 전용 컨테이너 실행 예시, Caddy 자동 TLS를 포함한 Compose, 환경 변수·reverse proxy·암호화 볼륨·백업 보존 정책을 `services/crash-collector/README.md`에 기록하고 루트 `pnpm crash:serve` 명령을 연결했다.
- 현재 남은 출시 조건은 실제 HTTPS 도메인과 운영 볼륨 배포, 동의/비동의·재시도·보존 정책의 사용자 환경 확인이다. 사용자 요청에 따라 서버 실행, 네트워크 요청, 자동 테스트, 타입 검사와 빌드는 수행하지 않았다.

## 2026-08-09 — 공개 키 검증형 업데이트 배포 서버

- `services/update-server/server.mjs`에 stable/beta와 Windows x64/ARM64, macOS x64/ARM64/universal 플랫폼별 서명 매니페스트를 제공하는 무의존성 Node 서비스를 추가했다.
- 서버 시작과 SIGHUP reload는 빌드에 설정된 32-byte Ed25519 공개 키와 key ID로 앱과 같은 고정 순서 canonical payload를 검증한다. 매니페스트 디렉터리의 channel/platform 이름, 64KB 상한, HTTPS public origin, `/editweave/artifacts` 경로와 OS별 설치 확장자도 함께 검사한다.
- 실제 EXE/MSI/DMG/PKG를 스트리밍 SHA-256으로 읽어 매니페스트와 일치하고 2GB 이하인 catalog만 활성화한다. 개인 키·코드 서명 인증서는 서버에 두지 않으며 검증 실패 reload는 제공 중인 이전 catalog를 유지한다.
- 매니페스트는 no-store, 설치 파일은 압축 없는 immutable 응답으로 제공하고 GET·HEAD·단일 byte range, SHA-256 ETag/header, 전체/클라이언트별 동시 다운로드 상한을 지원한다. 시작 시 확인한 파일 크기·수정 시각이 요청 시 달라지면 503으로 실패 폐쇄한다.
- `release/create-update-manifest.mjs`를 추가해 설치 파일·버전·platform·channel·운영 origin에서 SHA-256과 플랫폼별 무서명 JSON을 만들고 기존 파일은 덮어쓰지 않도록 했다. 기존 `sign-update-manifest.mjs`로 저장소 밖 개인 키 서명을 이어간다.
- 공개 포트 없이 읽기 전용 채널 볼륨을 마운트하는 비루트 Dockerfile, Caddy 자동 TLS Compose와 환경 템플릿, 릴리스 채널 구조·키 격리·원자 교체 절차를 문서화했다. 앱 빌드의 매니페스트 예시도 플랫폼별 운영 경로로 바꿨다.
- 남은 출시는 실제 DNS/TLS·공개 키·OS 코드 서명/공증 자격 증명과 채널 artifact를 연결하고 설치·재시도·업데이트 결과를 사용자 장비에서 확인하는 단계다. 사용자 요청에 따라 서버/도구 실행, 파일 해시 계산, 네트워크 요청, 자동 테스트, 타입/Rust 검사와 패키징은 수행하지 않았다.

## 2026-08-09 — 릴리스 버전·플랫폼 업데이트 설정 동기화

- 태그 이름만 바뀌고 package/Tauri/Cargo가 `0.1.0`으로 남아 업데이트 적용 판정이 틀릴 수 있던 CI 공백을 수정했다. `release/set-version.mjs`는 `v` 접두어를 제거한 SemVer를 세 메타데이터 파일에 함께 기록하고 GitHub output도 제공한다.
- `release/build-desktop.mjs`는 CI matrix의 Windows x64 또는 macOS ARM64 target과 stable/beta channel을 받아 설치된 앱 version, 플랫폼별 manifest URL, Ed25519 공개 키·key ID를 Vite 빌드에 설정하고 같은 공개 키를 Cargo 환경에 유지한 채 데스크톱 빌드를 실행한다.
- update origin·공개 키·key ID는 셋을 함께 설정해야 하며 HTTPS origin, raw 32-byte Base64 키와 key ID 형식을 패키징 전에 확인한다. 충돌 endpoint도 자격 증명·query/hash가 없는 HTTPS만 빌드에 허용한다.
- GitHub Actions 수동 빌드에 version/channel 입력을 추가하고 태그 빌드는 태그 SemVer를 사용한다. 저장소의 공개 운영 변수와 기존 Apple 인증서·공증 secret을 빌드 래퍼에 전달하도록 연결했다.
- 사용자 요청에 따라 버전 변경 스크립트, GitHub Actions, 데스크톱 빌드와 패키징은 실행하지 않았으며 정적 연결만 검토했다.

## 2026-08-09 — 실제 sample peak 기반 오디오 정규화

- 인스펙터의 `피크 정규화`가 원본 분석값과 무관하게 1.12배를 적용하던 임시 처리를 제거했다. 미디어 분석은 모든 오디오 채널의 모든 PCM sample을 순회해 source-wide sample peak를 계산하고 파형과 별도로 미디어·프로젝트 문서에 저장한다.
- 정규화는 -1 dBFS 목표 gain을 최대 ±24 dB 범위에서 계산한다. 분석값이 없거나 사실상 무음이면 unity gain으로 실패 안전 처리하며, 인스펙터에서 소스 peak·적용 gain·예상 peak 또는 재분석 안내를 표시한다.
- 프로그램 미리보기, 프리 페이더 Aux, 최종 스테레오 출력과 Delivery Guard 빠른 음량 예상에 같은 계산 함수를 연결했다. 프로젝트 열기에서는 비정상·과도한 외부 `audioPeak` 값을 제거한다.
- 이는 intersample True Peak 정규화가 아니라 디코딩 PCM의 sample peak 정규화다. 완성 MP4의 납품 수치는 기존 FFmpeg EBU R128 측정으로 별도 보존한다.
- 사용자 요청에 따라 실행·재생·출력·자동 테스트·타입 검사·빌드는 수행하지 않았으며 정적 연결만 검토했다.

## 2026-08-09 — 미리보기·출력 공통 soft-open 노이즈 게이트

- `노이즈 감소`가 최종 출력에서는 저레벨 sample을 0으로 만들지만 프로그램 미리보기에서는 전체 음량을 최대 20% 낮추고 하이패스 주파수까지 45~185Hz로 바꾸던 불일치를 제거했다.
- `noiseGateThreshold`와 `applyNoiseGate`를 공통 오디오 효과 계약으로 추가했다. 0~100%는 0~0.035 linear amplitude 임계값이며, 임계값부터 2배 amplitude까지 smoothstep으로 열어 하드 게이트 경계의 클릭을 완화한다.
- 미리보기는 65,537점 Web Audio WaveShaper curve로 같은 함수를 적용하고, 최종 PCM 믹스는 sample별로 같은 함수를 호출한다. 게이트는 EQ·Dynamics·클립 페이더·정규화 전 신호에 적용되어 프리/포스트 Aux 경로도 일치한다.
- Delivery Guard의 빠른 LUFS/peak 예상도 분석 파형과 source peak에 같은 게이트를 먼저 적용한다. 인스펙터에는 현재 게이트 임계값(dBFS)과 6 dB soft-open 구간을 표시한다.
- 사용자 요청에 따라 실행·청취·출력·자동 테스트·타입 검사·빌드는 수행하지 않았으며 정적 연결만 검토했다.

## 2026-08-09 — 공유 오디오 버스 미리보기와 공통 Dynamics curve

- 프로그램 미리보기의 각 활성 클립이 자기 AudioContext 안에 역할 버스 삽입 체인과 안전 리미터를 별도로 만들던 구조를 제거했다. 이 구조에서는 같은 대화 버스의 여러 클립이 먼저 합산되지 않아 compressor/limiter가 최종 출력보다 덜 작동하고, Aux가 많은 구간의 master 합산도 달랐다.
- PreviewPanel이 하나의 AudioContext와 대화·음악·효과음·환경음 summing input을 소유한다. 각 AudioLayer는 처리된 primary·pre/post Aux 신호를 해당 input에 연결하고, 버스별 합산 뒤 순차 inserts→bus gain→안전 ceiling→공유 master tanh→destination 순서로 재생한다.
- 클립 compressor의 amplitude-domain 출력식을 제거하고 threshold/ratio를 dB에서 계산하는 `applyStaticCompressor`, 공통 `applyBrickwallLimiter`와 Web Audio transfer curve 생성기를 추가했다. 버스 compressor makeup, 삽입 limiter와 버스 안전 limiter도 같은 함수의 curve를 사용한다.
- WaveShaper 입력은 0.25배로 축소하고 curve에서 4배 범위를 복원해 ±4 linear input까지 최종 PCM 함수와 같은 결과를 낸다. master도 합산 신호를 같은 방식으로 `tanh` 처리하며, 클립이 사라질 때 모든 source/send/node 연결을 명시 해제한다.
- Delivery Guard 빠른 예상은 클립 gate·compressor·limiter, 정규화·fader, primary/pre/post route, 버스 EQ의 보수적 최대 gain·compressor·insert limiter·bus gain·안전 limiter와 master tanh 순서를 반영한다. 실제 시간 중첩 및 주파수별 에너지는 완성 MP4 EBU R128 실측이 최종 기준이다.
- 사용자 요청에 따라 앱 실행·청취·렌더·자동 테스트·타입 검사·빌드는 수행하지 않았으며 정적 연결만 검토했다.

## 2026-08-09 — 채널 인지 StereoPanner 출력과 speaker downmix

- 최종 PCM의 기존 pan은 모든 원본을 mono로 가정해 좌우 각각 `cos/sin((pan+1)π/4)`를 곱했기 때문에 stereo 원본의 center pan에서도 두 채널이 각각 약 -3dB가 됐다. 미리보기 StereoPanner는 center에서 stereo L/R을 unity 통과하므로 레벨과 스테레오 폭이 달랐다.
- W3C Web Audio 1.1 StereoPanner 알고리즘을 `stereoPanSample`로 구현했다. mono는 -1~1을 0~1로 정규화한 equal-power L/R 분배를 사용하고, stereo는 center unity를 유지하며 왼쪽 pan에서는 R을 L로, 오른쪽 pan에서는 L을 R로 equal-power crossfeed한다.
- 디코딩 span에 mono 여부를 보존하고 primary와 post-fader Aux에만 pan을 적용한다. pre-fader Aux는 미리보기처럼 pan 전 stereo를 유지한다. 클립·트랙 pan 자동화의 합을 -1~1로 제한한다.
- Quad는 Web Audio speaker layout의 `0.5 × (L+SL/R+SR)`, 5.0/5.1은 `L/R + √0.5 × (C+SL/SR)`로 stereo downmix하고 LFE는 제외한다. 미리보기의 3채널 이상 MediaElement도 첫 DSP 노드 앞에서 `explicit` stereo/`speakers`로 downmix해 게이트·EQ·Dynamics의 비선형 처리 순서를 최종 PCM과 맞춘다. 7.1 이상은 앞쪽 5.1 bed만 사용하므로 Delivery Guard가 추가 후방·높이 채널 미보존을 경고한다.
- Delivery Guard의 peak 예상은 clip/track pan 자동화 조합별 최대 channel factor를 반영해 hard-panned stereo crossfeed의 최대 2배 합산 가능성을 보수적으로 계산한다.
- 사용자 요청에 따라 앱 실행·청취·렌더·자동 테스트·타입 검사·빌드는 수행하지 않았으며 정적 연결만 검토했다.

## 2026-08-09 — Web Audio와 최종 출력의 RBJ Biquad EQ 통합

- 미리보기는 BiquadFilterNode의 high-pass·low shelf·peaking·high shelf를 사용하지만 최종 PCM은 1차 high-pass와 두 개 low-pass 차분으로 3개 대역을 근사하던 음색 불일치를 제거했다. `src/editor/audioDsp.ts`에 정규화된 RBJ cookbook 계수와 Direct Form I 상태 처리를 추가했다.
- 공통 파라미터는 high-pass Q 1, low shelf 220Hz, mid peaking 1.2kHz/Q 0.75, high shelf 4.2kHz다. 클립의 음성 강조는 이전 sample 차분 증폭을 제거하고 미리보기와 같은 2.4kHz/Q 0.8/+4.5dB peaking filter로 출력한다.
- 역할 버스의 순차 high-pass와 3밴드 EQ inserts도 같은 계수로 처리한다. 좌우 채널은 독립 state를 사용하고 클립 및 버스 filter state는 2초/10초 PCM 렌더 청크가 바뀌어도 유지해 경계에서 필터가 재시작하는 클릭·톤 변화를 방지한다.
- 계수 생성은 sample rate와 Nyquist 범위를 제한하고 출력이 비정상 수치가 되면 해당 filter state를 0으로 복구한다. Delivery Guard는 주파수 스펙트럼이 없는 축약 파형의 한계 때문에 활성 EQ/voice boost 중 가장 큰 양의 gain을 보수적으로 peak 예상에 반영한다.
- 사용자 요청에 따라 앱 실행·청취·렌더·자동 테스트·타입 검사·빌드는 수행하지 않았으며 정적 연결만 검토했다.

## 2026-08-09 — 실제 pre/post-master 프로그램 오디오 미터

- 프로그램 모니터의 기존 미터가 활성 미디어의 파형 bucket 평균을 사용해 EQ·pan·Aux·역할 버스 합산·Dynamics와 master 처리를 반영하지 못하던 표시를 제거했다.
- 공유 프로그램 AudioContext의 역할 버스가 합쳐지는 master sum과 `tanh` soft clip 출력에 각각 stereo splitter·L/R AnalyserNode tap을 연결했다. tap은 0 gain sink로 destination에 연결하므로 분석 graph는 유지하면서 가청 신호를 중복 합산하지 않는다.
- 재생 중 실제 처리 후 L/R time-domain peak를 읽어 -60~0 dBFS 로그 눈금과 최대 채널 수치로 표시한다. soft clip 직전 합산 peak가 1.0 이상이면 빨간 과부하 표시를 1.5초 유지하고, 정지 시에는 peak hold를 감쇠한 뒤 정확히 0으로 복귀한다.
- 사용자 요청에 따라 앱 실행·청취·자동 테스트·타입 검사·빌드는 수행하지 않았으며 실제 채널 신호와 hold 동작은 사용자 검증 항목으로 남겼다.

## 2026-08-09 — 단일 재생 시계와 프레임 정확 미디어 동기화

- 기존 프로그램 시계는 rAF 간 delta를 React state에 계속 더했다. 이를 `performance.now()`의 monotonic 표본과 마지막 확정 위치를 보존하는 clock으로 바꾸고, 소스/프로그램 모드 전환 또는 타임라인 클릭·프레임 이동처럼 현재 state가 1/120초보다 달라지면 그 위치에서 자동 재기준화한다.
- 소스·프로그램의 모든 video/audio 동기화를 재생/정지 정책으로 분리했다. 정지·역재생·정지 프레임은 1/240초 이상 차이면 즉시 원하는 소스 시각으로 seek해 이전 180ms 허용 오차 때문에 30fps 한 프레임 이동이 화면에 반영되지 않던 문제를 제거했다.
- 정방향 재생 중 audio drift는 75ms를 넘으면 재탐색한다. video는 1/60초 이상 차이에 의도 속도의 최대 ±2%로 부드럽게 따라잡고 120ms를 넘으면 hard seek하며, 동기화되면 원래 속도로 복귀한다.
- metadata가 아직 없는 미디어는 마지막 time/options를 WeakMap에 보존하고 `loadedmetadata` 직후 한 번 적용해 초기 non-zero 탐색의 InvalidStateError와 잘못된 첫 프레임을 방지한다. ADR loop는 긴 rAF 공백에서도 전체 경과 시간을 modulo로 보존한다.
- 환각성 완료 판정을 막기 위해 `docs/10-implementation-evidence-matrix.md`를 추가하고 소스 연결, 부분 구현, 검증 대기, 외부 조건을 분리했다. 역재생 오디오 미리보기와 pitch-preserving time stretch를 현재 출시 차단 공백으로 명시했다.
- 사용자 요청에 따라 앱 실행·재생·자동 테스트·타입 검사·빌드는 수행하지 않았다. 장시간 실제 A/V drift와 각 경계는 사용자 검증 전이다.

## 2026-08-09 — 범위 PCM 기반 가변 속도 역재생 오디오 미리보기

- 최종 출력은 `clipSourceTime`으로 역방향 PCM을 만들지만 프로그램 미리보기는 HTMLMediaElement를 pause해 역재생 클립이 무음이던 불일치를 제거했다.
- `src/media/audioPcm.ts`로 mono/stereo/Quad/5.0/5.1 speaker downmix, decoded span 끝과 선형 sample 보간을 분리해 최종 출력과 미리보기 디코더가 같은 PCM 채널 규칙을 사용한다.
- 이후 `src/media/timeMappedAudioPreview.ts`로 일반화한 부분 디코더는 로컬 원본 또는 편집 프록시를 Mediabunny 범위 source로 열고, 250ms 타임라인 구간의 시작/끝 source 범위만 디코딩한다. 각 AudioContext sample에서 공통 속도 곡선 적분 시간을 다시 조회하므로 일정 역재생뿐 아니라 가변 속도 램프도 최종 출력과 같은 방향·시각으로 재표본화한다.
- 프로그램은 역재생 클립 1.25초 전부터 디코더를 준비한다. AudioBufferSource 청크를 200ms lead와 최대 0.9초 horizon으로 예약하고, raw PCM을 기존 gate→RBJ EQ/voice→compressor→limiter→fader/pan→pre/post Aux→역할 버스→master graph의 입력에 연결한다.
- 재생 중 탐색 또는 ADR loop jump가 80ms 이상이면 예약 source를 중단하고 새 playhead로 재기준화한다. 디코딩이 세 번 연속 예약 시각을 놓치면 틀린 구간을 재생하지 않고 프로그램 화면에 편집 프록시 사용 오류를 표시한다. pause/unmount/엔진 교체에서는 timer·AudioBufferSource·Mediabunny Input을 정리한다.
- 사용자 요청에 따라 실행·청취·자동 테스트·타입 검사·빌드는 수행하지 않았다. 긴 GOP, 25~400% 역방향 램프, 반복 탐색, clip/track automation과 preview/export 음질은 사용자 검증 전이다.

## 2026-08-09 — 정·역방향 공통 pitch-preserving time stretch

- `AudioAdjustment.preservePitch`와 인스펙터의 `속도 변경 시 음정 유지`를 추가하고 기본값을 켰다. 이전 `.editweave.json`의 audio adjustment에 필드가 없으면 열 때 `true`로 정규화하고, 사용자가 끄면 기존 source-time 선형 재표본화를 유지한다.
- `src/media/audioPcm.ts`에 40ms Hann grain, 10ms hop의 granular overlap-add 처리기를 구현했다. 각 grain은 공통 `clipSourceTime`의 기대 중심에서 ±6ms 후보를 1ms 간격으로 비교하고, 이미 합성된 겹침 파형과 normalized cross-correlation이 가장 높은 위치를 선택해 대사·음악의 위상 겹침을 줄인다.
- grain 중심은 clip start 기준 전역 hop 격자에 고정된다. 각 요청은 앞 50ms 합성을 다시 수행해 correlation state를 안정화한 뒤 해당 history를 버리므로 프로그램의 250ms rolling buffer와 최종 출력의 2/10초 chunk가 같은 위치에서 독립 렌더되어도 경계 선택이 재현된다. 최대 속도에 비례한 source padding을 추가해 16배속 grain history도 범위 디코딩 안에 포함한다.
- 정방향 speed ramp도 pitch 유지가 켜져 있으면 HTMLMediaElement가 아니라 역재생과 같은 Mediabunny 부분 PCM scheduler를 사용한다. 미리보기와 출력은 같은 time-stretch 배열을 gate·RBJ EQ·Dynamics·normalization·fade·pan·Aux·역할 버스 전에 전달한다. 정상 1배속은 원본 HTML media/direct PCM 경로를 유지한다.
- Delivery Guard는 음정 유지가 켜진 25% 미만 또는 400% 초과 클립을 음질 확인 경고로 표시한다. 사용자 요청에 따라 실행·청취·자동 테스트·타입 검사·빌드는 수행하지 않았으며 기본음·transient·stereo image·청크 경계는 사용자 검증 전이다.

## 2026-08-09 — 동일 프로젝트 클립 단위 3-way 공동 작업 병합

- 프로젝트 버전 기록에서 공통 자동 저장 체크포인트를 선택하고 다른 편집자의 `.editweave.json`을 가져오는 공동 작업 병합 경로를 추가했다. 공통 기준·현재·상대 프로젝트 ID가 다르면 적용 전에 거부한다.
- 시퀀스·트랙·클립과 대본·제안·마커·오디오 버스, 미디어·ADR 큐·사용자 교정 사전을 stable ID 기준으로 3-way 비교한다. 한쪽만 바꾼 값, 서로 다른 ID의 추가/삭제, 서로 다른 클립 수정은 자동 반영하고 같은 ID를 양쪽에서 다르게 바꾼 경우 현재 편집본을 유지한다.
- 같은 클립·트랙·시퀀스 충돌이 있으면 상대 시퀀스와 source/nested 시퀀스, ADR 큐/테이크/컴프 및 clip group/link group 참조를 새 ID로 복제해 `병합 충돌 (상대)` 분기에 보존한다. 같은 클립 충돌 위치에는 열림 상태의 `EditWeave 병합` 검토 마커를 추가해 수동 비교 지점을 남기고 Delivery Guard가 미해결 개수를 납품 경고로 표시한다.
- 적용 전 자동 반영 클립 수·충돌 수·생성 분기 수와 “선택한 체크포인트가 실제 공통 기준인지”를 확인한다. 취소하면 현재 프로젝트를 변경하지 않는다. 세 문서의 시퀀스/트랙/클립/대본/제안/마커/미디어/ADR ID 중복과 고아 track 참조를 먼저 검사하고 이상하면 적용 전 거부한다. 활성 시퀀스의 legacy `sequence` 필드가 `sequence-main`/30fps로 고정되던 저장 불일치도 실제 활성 ID·규격으로 수정했다.
- `ProjectMergeSession`과 충돌 결정 레코드를 프로젝트·자동 저장에 최대 20건 보존한다. 기준/상대 시각, 상대 프로젝트, 원본/분기 entity ID, 마커, 현재 클립·트랙 보존 사본, 상태, 현재/상대 결정과 해결 시각을 저장하며 이전 프로젝트는 빈 목록으로 호환한다.
- 버전 기록에 미해결 충돌 목록과 현재 위치·상대 분기 바로가기, `현재 유지`, `상대 클립 적용`, `상대 삭제 적용`을 추가했다. 상대 클립은 충돌 분기의 nested/ADR/group/link 참조를 보존하고 원본 clip ID로 현재 시퀀스에 복귀하며, 같은 ID 트랙의 종류가 다르면 별도 병합 트랙을 만든다. 모든 결정을 마친 상대 분기는 `병합 기록 (해결됨)`으로 남기고 해결된 세션의 결정 통계를 다시 볼 수 있으며, 저장된 현재 클립 사본과 상대 분기를 사용해 결정을 다시 전환할 수 있다.
- 검토 마커를 직접 해결하거나 삭제해도 병합 세션이 열려 있으면 Delivery Guard가 프로젝트 전체 미해결 충돌 수를 계속 경고한다. resolver로 결정하면 대응 마커도 함께 해결된다.
- 자동 저장 버전 비교 통계에도 병합 세션별 충돌 상태·현재/상대 결정·해결 시각 차이를 추가해, 과거 버전을 복원할 때 해결 결정이 되돌아가는 범위를 확인할 수 있게 했다.
- 사용자 요청에 따라 프로젝트 병합 실행·자동 테스트·타입 검사·빌드는 수행하지 않았다. 두 장비에서 복제한 실제 프로젝트의 비충돌/충돌/삭제·수정/nested/ADR 조합과 저장·재열기는 사용자 검증 전이다.

## 2026-08-09 — 공동 작업 충돌 종류별 직접 resolver 확대

- 클립만 상대 적용할 수 있던 병합 기록을 시퀀스 메타, 트랙, 대본, 편집 제안, 마커, 오디오 버스, ADR 큐, 사용자 교정 사전·크리에이터 학습 프로필과 이전 병합 결정까지 현재/상대 스냅샷으로 선택할 수 있게 확장했다. 상대가 삭제한 항목은 삭제 결정으로 적용하며 해결 기록에서 다시 현재 값으로 전환할 수 있다.
- 트랙 설정을 바꿀 때 현재 트랙의 클립과 배열 위치를 보존하고, 대본·제안·마커는 시간순으로 다시 정렬한다. ADR 큐는 적용 전에 시퀀스·미디어·트랙·클립 참조를 확인해 손상된 상대 값을 거부한다.
- 시퀀스 전체 삭제/수정 충돌과 미디어 원본 항목은 자동 적용 시 의존 파일이나 연결이 손상될 수 있어 상대 분기 확인 후 현재 상태로만 완료하도록 차단했다. 시퀀스 양쪽 설정 수정은 콘텐츠 배열을 보존한 채 메타데이터만 선택한다.
- 버전 기록 UI는 충돌 종류를 한국어로 표시하고 종류별 상대 적용/삭제 버튼, 현재·상대 위치 이동, 해결된 모든 충돌의 결정 전환을 제공한다. resolver 결과의 ADR·사전·학습 프로필도 React 상태에 즉시 동기화한다.
- 사용자 요청에 따라 실행·자동 테스트·타입 검사·빌드는 수행하지 않았다. 각 충돌 종류의 실제 A/B 프로젝트 왕복과 저장·재열기는 사용자 검증 전이다.

## 2026-08-09 — 공동 작업 미디어 충돌 비파괴 해결

- 병합 기록의 미디어 충돌에도 `현재 유지`, `상대 미디어 적용`, `상대 미디어 삭제` 결정을 연결했다. 선택 결과는 프로젝트의 영속 메타데이터만 바꾸며 원본·프록시 파일을 복사하거나 디스크에서 삭제하지 않는다.
- 이름·종류·확장자·바이트 크기·길이가 같은 미디어 ID는 상대의 태그·분석·타임코드 같은 저장 값을 반영하면서 현재 장비에서 이미 열린 File/Object URL, sourcePath, 파형·썸네일과 프록시 핸들을 유지한다. 다른 원본으로 판정되면 이전 Object URL을 해제하고 오프라인 미디어로 전환해 캐시 프록시 복원과 sourcePath 자동 재연결을 다시 시도한다.
- 상대 삭제를 선택할 때 전체 시퀀스의 클립과 모든 ADR 테이크를 검사한다. 하나라도 해당 asset ID를 사용 중이면 삭제하지 않고 참조 개수를 포함한 오류를 표시해, 연관 클립/ADR 충돌을 먼저 해결하도록 했다.
- 이전 소스에서 `canApplyIncoming: false`로 저장된 미디어 충돌도 현재/상대 스냅샷이 있으면 프로젝트 열기 정규화 과정에서 새 resolver를 사용할 수 있게 승격한다. 선택 미디어가 실제로 제거된 경우 패널 선택도 함께 해제한다.
- 사용자 요청에 따라 실제 병합·재연결·자동 테스트·타입 검사·빌드는 수행하지 않았다. 같은 파일의 다른 장비 경로, 다른 파일로 교체, 참조 중/미참조 삭제와 결정 재전환은 사용자 검증 전이다.

## 2026-08-09 — 자동 저장 복원·버전 분기의 정상 미디어 연결 보존

- `applyProject`가 프로젝트 버전 적용 때마다 모든 원본/프록시 Object URL을 해제하고 `restoreAssets`로 전부 오프라인화하던 경로를 수정했다. 같은 프로젝트의 자동 저장 전체 복원과 과거 버전 새 분기는 stable asset ID를 신뢰해 현재 장비의 File, Object URL, sourcePath, 파형·썸네일과 프록시 핸들을 유지한다.
- 다른 프로젝트 파일 열기와 공동 작업 3-way 병합은 stable ID만으로 파일을 같다고 가정하지 않는다. 이름·종류·확장자·바이트 크기·분석 길이가 같은 항목만 기존 핸들을 재사용하고 바뀐 원본이나 새 항목은 오프라인 상태로 복원해 프록시 캐시와 sourcePath 재연결을 시도한다.
- 프로젝트 적용 전후 URL 집합을 비교해 다음 상태에서 실제로 재사용하지 않는 원본/프록시 Object URL만 해제한다. 현재 자산 참조를 React state보다 먼저 갱신한 뒤 캐시 복원과 원본 재연결을 시작해 비동기 로더가 이전 프로젝트 자산으로 오판하지 않게 했다.
- 자동 저장 복원과 새 분기 호출은 `trustStableMediaIds`를 명시하고, 외부 프로젝트 열기와 병합은 보수적 원본 지문 비교를 사용하도록 호출 경로를 분리했다.
- 사용자 요청에 따라 실제 복원·분기·병합·자동 테스트·타입 검사·빌드는 수행하지 않았다. 데스크톱과 브라우저에서 정상 재생을 유지하는지, 제거된 항목의 URL만 해제되는지는 사용자 검증 전이다.

## 2026-08-09 — 미디어 가져오기 부분 실패 진단과 중복 원본 방지

- `readMediaFilesFromPaths`를 성공 File 배열만 반환하는 경로에서 `files + failures(path/name/message)` 결과로 확장했다. 네이티브 다중 선택, Tauri 경로 드롭과 저장 경로 자동 재연결은 일부 파일의 stat/read가 실패해도 성공 파일을 계속 등록하고 최종 토스트에 경로 읽기 실패 개수와 첫 파일명을 남긴다. HTML/경로 드롭에 지원하지 않는 형식이 섞이면 성공 미디어는 계속 분석하고 미지원 개수와 첫 파일명을 별도로 표시한다.
- 브라우저 선택·HTML 드롭은 기존 FileList 경로를 유지하고, 네이티브 선택은 동일한 `handleFiles` 분석 경로로 실패 정보를 전달한다. 사용자가 시스템 선택기를 취소한 경우에는 오류나 기본 선택기 중복 호출 없이 아무 작업도 하지 않는다.
- 데스크톱은 정규화한 sourcePath+실제 바이트 크기, 브라우저는 File 이름+크기+lastModified+MIME으로 같은 열린 원본을 식별한다. Windows 드라이브/역슬래시·UNC 경로만 대소문자를 접고 POSIX/macOS 경로와 브라우저 파일명은 원래 대소문자를 유지한다. 이미 ready/analyzing인 원본 또는 같은 선택 안의 중복은 새 asset ID를 만들지 않고 기존 항목을 선택하며 건너뛴 수를 표시한다.
- 오프라인 또는 분석 오류 항목에 같은 경로/이름+크기의 파일을 다시 선택하면 기존 stable ID, 메타데이터와 프록시 연결을 바탕으로 재분석한다. 오류 상태의 이전 원본 Object URL은 새 URL로 교체될 때 해제한다.
- Tauri drag 이벤트와 HTML drop이 가까운 시점에 겹쳐도 첫 등록의 analyzing 자산을 다음 이벤트가 즉시 볼 수 있도록 `assetsRef`를 React state와 동시에 갱신했다.
- 사용자 요청에 따라 실제 선택·드롭·부분 권한 실패·자동 테스트·타입 검사·빌드는 수행하지 않았다. Windows/macOS 경로 대소문자와 브라우저 동일 파일 재선택은 사용자 검증 전이다.

## 2026-08-09 — ADR 녹음 자산 삭제 무결성과 납품 진단

- 프로젝트 미디어의 `사용됨` 표시에 모든 시퀀스 클립뿐 아니라 ADR 큐의 take asset ID를 포함했다. 일반 미디어 제거는 활성 시퀀스 최신 상태와 비활성 시퀀스, 모든 ADR 테이크의 참조 수를 합산하고 하나라도 남으면 구체적인 클립/테이크 개수와 함께 삭제를 차단한다.
- ADR 큐 삭제 전에 큐의 테이크 asset을 다른 시퀀스 클립 또는 다른 ADR 큐가 재사용하는지 검사한다. 전용 녹음만 프로젝트 자산·Object URL·프록시 캐시·앱 데이터 원본에서 삭제하고 공유 녹음은 프로젝트와 디스크에 보존한다. 확인창과 완료 토스트에 삭제/보존 원본 수를 분리했다.
- 큐 삭제와 일반 미디어 제거는 React state보다 먼저 `assetsRef`를 갱신해 연속 삭제나 비동기 원본 로더가 이미 제거한 자산을 다시 참조하지 않게 했다. 제거된 자산이 미디어 패널에서 선택 중이면 선택도 해제한다.
- Delivery Guard에 현재 활성 시퀀스와 실제 nested 의존 그래프의 ADR 큐를 추가했다. 큐 시퀀스, 선택 테이크, 컴프 take ID, 녹음 원본 ready/File 상태, 테이크 트랙/클립, 클립 asset ID 일치를 검사해 손상 항목을 납품 blocker로 표시한다. 해결된 병합 감사 분기처럼 출력 그래프 밖의 ADR은 현재 납품을 차단하지 않는다.
- 사용자 요청에 따라 ADR 삭제·재사용·자동 테스트·타입 검사·빌드는 수행하지 않았다. 다른 타임라인에 녹음 자산을 재사용한 뒤 큐 삭제와 Delivery Guard 항목은 사용자 검증 전이다.

## 2026-08-09 — 의존성 보호 시퀀스 라이브러리

- 상단 시퀀스 선택기 옆에 현재 캔버스·fps와 기본 영상/오디오 트랙을 가진 빈 일반 시퀀스를 만들고, 롱폼·쇼츠·중첩·멀티캠을 한 화면에서 열고 이름 변경·복제·삭제하는 시퀀스 관리 창을 추가했다. 해상도·fps·길이·트랙·ADR 수와 현재 열림 상태, 삭제를 막는 실제 참조 이유를 함께 표시한다.
- 이름은 공백을 정규화하고 80자·프로젝트 내 대소문자 무시 중복을 검사한다. 중첩 시퀀스의 이름과 같던 부모 타임라인의 중첩 클립 표시명도 함께 바꾸며, 관리 작업 직전 활성 타임라인 최신 상태를 라이브러리에 materialize해 마지막 편집이 이전 저장본으로 되돌아가지 않게 했다.
- 시퀀스 복제는 시퀀스·트랙·클립·그룹·링크와 변형/속도/시각/오디오/트랙 키프레임, 마스크, 색상 노드, Aux·버스 insert, 대본·제안·마커, ADR 큐·테이크·컴프 ID를 새로 발급하고 내부 참조를 재작성한다. 미디어 asset과 외부 중첩/source 의존성만 공유하므로 복제본의 후속 편집이 원본 객체를 함께 바꾸지 않는다. 중복 ID, 실제 배열 소속과 다른 trackId, 순환·누락 nested/source, 프로젝트에 없는 일반/ADR 미디어, 손상된 ADR 선택·컴프·트랙·클립 참조는 복제 전에 거부한다.
- 마지막 시퀀스 또는 다른 시퀀스의 nested clip, 쇼츠/중첩 source provenance, 다른 시퀀스 클립의 ADR 큐/테이크, 공동 작업 병합 감사 기록, 미완료 렌더 작업, 진행 중 직접 출력, LAN 검토, 열린 ADR 창이 참조하는 시퀀스는 삭제하지 않는다. 허용된 삭제는 해당 시퀀스의 ADR 큐 기록만 제거하고 녹음 asset을 포함한 프로젝트 미디어·프록시·디스크 원본을 보존한다. 활성 시퀀스를 지우면 source 부모, main, 나머지 첫 시퀀스 순으로 안전하게 전환한다.
- 사용자 요청에 따라 앱 실행·자동 테스트·타입 검사·빌드는 수행하지 않았다. 원본/복제본 독립 편집, 각 차단 조건, ADR 기록 제거와 저장·재열기는 사용자 검증 전이다.

## 2026-08-09 — 선택과 분리된 V/A/T 소스 트랙 대상

- 기존 미디어 추가는 선택된 트랙의 종류가 맞으면 그곳, 아니면 첫 트랙을 암묵적으로 사용해 인스펙터 선택과 소스 목적지가 섞였다. `TimelineTrack.sourceTarget`과 전용 `trackTargeting.ts`를 추가해 비디오·오디오·자막마다 최대 한 개의 명시적 소스 목적지를 시퀀스에 저장한다.
- 트랙 머리글에 순서 기반 `V1/A1/T1` 대상 버튼을 추가했다. 켜면 같은 종류의 이전 대상이 꺼지고, 다시 누르면 해당 종류를 전부 해제한다. 미디어 추가, 조정 레이어, 모션 텍스트, 중첩 결과는 선택 트랙이 아니라 이 대상으로만 이동하며 대상이 없거나 잠겨 있으면 잘못된 fallback 대신 구체적인 안내를 표시한다.
- 새 트랙을 만들면 그 종류의 새 대상이 되고, 활성 대상 트랙을 삭제하면 같은 종류의 잠금 해제 트랙, 없으면 남은 첫 트랙으로 대상을 이전한다. 단순 트랙 선택과 클립 선택은 목적지를 바꾸지 않는다.
- 이전 `.editweave.json`처럼 `sourceTarget`이 전혀 없는 종류는 첫 잠금 해제 트랙을 기본 대상으로 한 번 정규화한다. 하나라도 명시적 boolean이 있는 종류는 사용자의 전체 해제 상태를 보존하고, 여러 `true`가 들어온 손상/구형 데이터는 첫 대상만 유지한다. 프로젝트 루트와 모든 시퀀스, 자동 저장·복원·복제에서 같은 정규화를 사용한다.
- 대상 버튼과 기존 자동화·가시성·음소거·솔로·잠금·삭제 버튼이 함께 표시되도록 트랙 머리글 폭과 최대 제어 열을 조정했다.
- 사용자 요청에 따라 앱 실행·자동 테스트·타입 검사·빌드는 수행하지 않았다. 종류별 라우팅, 전체 해제, 잠금, 새 트랙/삭제 복구, 시퀀스 저장·전환·재열기는 사용자 검증 전이다.

## 2026-08-09 — 경계 분할과 프로젝트 메타데이터를 포함한 실제 리플 삽입

- 기존 삽입 모드는 모든 잠금 해제 트랙에서 삽입점 이후에 시작한 클립과 마커만 이동해, 삽입점을 가로지르는 클립 위에 새 클립이 겹치고 대본·편집 제안·트랙 자동화·ADR 세션 시간이 원래 위치에 남았다. `timelineOps.ts`의 insert 경로를 경계 분할 기반으로 바꿔 좌우 조각의 소스 시간, 속도 램프, 변형·시각·오디오 자동화와 단어 자막을 각 구간으로 다시 계산했다.
- 오른쪽 조각은 새 clip ID를 받고, 같은 그룹·링크에 속해 경계에서 갈라지거나 이후에 있는 클립들은 오른쪽 전용 그룹·링크 ID를 공유한다. 따라서 삽입 뒤 오른쪽 묶음만 이동해도 왼쪽 원본 묶음이 따라오지 않는다. 내부 분할 경계의 트랜지션과 오디오 fade는 제거하고 원래 바깥쪽 전환은 각 조각에 보존한다.
- 이후 클립의 절대 ADR 컴프 범위와 잠금 해제 트랙의 절대 믹스 키프레임을 삽입 길이만큼 이동한다. 자동화 곡선이 삽입 공백에서 임의로 늘어나지 않도록 삽입 직전 값과 삽입 끝의 같은 값을 경계 키프레임으로 남긴 뒤 기존 곡선을 이어간다. 새 `rippleInsert.ts`는 마커·LAN 검토 코멘트, 대본 구간과 단어, 편집 제안, 활성 시퀀스의 ADR 큐·컴프 구간을 같은 규칙으로 이동하며 삽입점을 가로지르는 대본·제안 범위는 끝을 늘려 시간 공백을 보존한다.
- ADR 큐나 테이크 클립 내부에 삽입하면 녹음 자산·take clip ID·컴프 참조가 분리될 수 있으므로 큐 경계 밖으로 이동하라는 오류로 차단한다. 삽입점 이후 ADR 테이크 트랙이 없거나 잠겨 있어 큐와 클립을 같이 이동할 수 없는 경우도 원인과 함께 차단한다.
- ADR 큐를 `EditorHistorySnapshot`과 즉시 동기화 ref에 포함하고 기존 ADR 생성·채택·컴프·삭제·시퀀스 복제/삭제·프로젝트 적용 경로도 같은 갱신 함수로 통합했다. 리플 삽입의 트랙·대본·제안·마커·ADR 변경은 한 번의 Undo/Redo로 함께 왕복한다.
- 사용자 요청에 따라 앱 실행·자동 테스트·타입 검사·빌드는 수행하지 않았다. 경계의 링크 영상/오디오, 가변 속도·키프레임, 잠금 트랙, 단어 대본, 검토 코멘트, ADR 큐 앞/안/뒤 삽입과 저장·재열기는 사용자 검증 전이다.

## 2026-08-09 — ADR·자동화·단어 타임코드를 보존하는 원자적 리플 삭제

- 기존 대본 구간 삭제와 AI 초벌편집 적용은 `removeTimelineRange`로 일반 클립만 재단하고 별도 함수로 대본·마커를 이동했다. 트랙 믹스 키프레임·대본 단어·다른 편집 제안·ADR 큐와 컴프가 남았으며, 삭제 범위와 겹친 ADR 테이크는 일반 클립처럼 ID가 바뀌거나 일부만 남아 cue→take→clip 참조가 손상될 수 있었다.
- 새 `rippleDelete.ts`에 범위 정규화와 timed range 삭제 규칙을 모았다. 범위 전 항목은 유지하고, 범위 안 항목은 제거 또는 적용/기각 감사 상태로 접으며, 뒤 항목은 삭제 길이만큼 당기고, 한 구간이 범위를 가로지르면 남은 앞뒤 시간을 맞붙인다. 대본의 단어 타임코드도 같은 규칙으로 제거·이동하고 제거된 단어가 있으면 남은 단어로 표시 텍스트를 다시 만든다.
- `removeTimelineRange`는 모든 잠금 해제 트랙에서 삭제 범위와 클립의 네 교차 형태를 구분한다. 좌우 소스 offset, 속도 램프, 변형·시각·오디오 자동화와 단어 자막을 다시 슬라이스하고 내부 전환/fade를 제거한다. 범위를 양쪽으로 가로지른 클립의 오른쪽 조각은 새 clip ID와 오른쪽 전용 group/link ID를 받고, 같은 관계의 후속 클립도 그 ID로 이동해 왼쪽 조각과 독립된다.
- 절대 트랙 믹스 자동화는 범위 안 키프레임을 제거하고 뒤 키프레임을 당긴다. 삭제 직전 값을 1/240초 앞에, 삭제 끝의 값을 새 경계에 남겨 기존 왼쪽 곡선을 보존하면서 오른쪽 값으로 짧게 전환한다. 대본 및 AI 삭제 경계의 일반 오디오/영상 클립에는 80ms 최소 fade를 적용하되 ADR 테이크의 믹스 설정은 건드리지 않는다.
- ADR 사전 검사는 활성 시퀀스의 큐와 삭제 범위가 조금이라도 겹치면 적용 전에 차단한다. 이동 대상 큐의 take track·clip·cue/take ID와 큐/클립의 이동 방향을 확인하고, 누락·불일치 또는 잠긴 레인이 있으면 구체적인 오류를 표시한다. 허용된 범위 뒤 큐는 큐·comp segment·take clip·clip comp range를 같은 길이만큼 당긴다.
- `commitRippleDelete`가 대본 삭제와 AI 제안 적용의 트랙·대본·제안·마커·ADR 변경을 하나의 `EditorHistorySnapshot`에 기록한다. 차단된 AI 제안은 크리에이터 학습 통계를 먼저 증가시키지 않는다. 활성 LAN 검토 중 삭제된 comment ID는 commit/Undo/Redo의 before/after 마커 집합으로 재계산해 원격 삭제가 복원 뒤 다시 전송되지 않게 했다.
- 이전 `removeMarkerRange`와 `removeTranscriptRange` 공개 함수는 기존 테스트/호출 호환을 위해 새 공통 구현의 얇은 위임 함수로 유지했다. 사용자 요청에 따라 앱 실행·자동 테스트·타입 검사·빌드는 수행하지 않았다. 링크된 V/A·잠금 트랙·속도/효과/트랙 자동화·단어 대본·AI 제안·LAN 검토·ADR 앞/겹침/뒤와 Undo/Redo·저장 재열기는 사용자 검증 전이다.

## 2026-08-09 — 프로젝트 시간축과 연결된 양방향 리플 트림·ADR 보호 덮어쓰기

- 기존 `trimTimelineClipAdvanced`의 리플 모드는 끝 경계를 줄이거나 늘릴 때 일부 후속 클립의 start만 바꾸고, 시작 경계 리플은 일반 트림처럼 gap/겹침을 남겼다. 대본·단어·제안·마커·트랙 자동화·ADR은 움직이지 않았다. 순수 track 연산도 공통 `removeTimelineRange`/`insertTimelineGap`과 clip 확장 함수를 사용하도록 교체하고, App의 실제 리플 트림 경로는 같은 track 연산에 프로젝트 시간 메타데이터와 ADR 사전 검사를 원자적으로 더했다.
- 시작을 안쪽으로 줄이면 `[oldStart,newStart]`, 끝을 안쪽으로 줄이면 `[newEnd,oldEnd]`를 원자적으로 제거한다. 선택 클립은 기존 clip ID를 유지하고 새 바깥 경계에 원래 transitionIn/Out과 fadeIn/Out 값을 복원하며, 다른 경계 교차 클립과 모든 프로젝트 시간 엔티티는 공통 삭제 규칙을 따른다.
- 시작/끝을 바깥으로 늘리면 기존 경계에 `insertTimelineGap`을 적용한다. 모든 잠금 해제 트랙의 경계 클립 분할·후속 이동·트랙 자동화 hold와 대본/단어·제안·마커·ADR 이동을 먼저 수행한 뒤 선택 클립을 추가 길이만큼 확장한다. 시작 확장은 `sliceClipSpeed`의 음수 로컬 범위로 source offset/속도 곡선을 다시 만들고 기존 변형·시각·오디오 키프레임과 단어 자막을 추가 길이만큼 뒤로 옮겨 원래 콘텐츠 시각에 유지한다.
- 삽입 ADR 사전 검사를 cue→take→track/clip과 adrCueId/adrTakeId, 큐/클립 이동 방향까지 검증하도록 강화했다. orphan ADR clip, 큐 내부 경계, 누락·불일치, 이동할 잠긴 레인이 하나라도 있으면 리플 확장과 일반 삽입을 적용 전에 중단한다. 축소는 공통 ADR 리플 삭제 검사를 사용한다.
- 기존 덮어쓰기는 대상 트랙 클립의 duration/source offset만 잘랐고 자동화·단어 자막·내부 전환과 좌우 link/group 관계를 보존하지 못했다. `removeTrackRange`를 네 범위 교차 형태로 확장해 속도/변형/시각/오디오 자동화와 captionWords를 슬라이스하고, 내부 transition/fade를 제거하며 오른쪽 조각과 같은 관계의 후속 클립에 새 group/link ID를 부여했다. 타임라인 길이와 프로젝트 메타데이터 시간은 그대로 유지한다.
- 새 `overwrite.ts` 사전 검사는 소스 대상 트랙의 덮어쓰기 범위와 활성 ADR cue/take clip이 겹치는지 양쪽에서 확인하고, 누락 cue 또는 손상된 take clip ID도 차단한다. 덮어쓰기는 녹음 원본이나 ADR 레인을 자동 절단하지 않으며 다른 대상 트랙 또는 ADR 세션 정리를 안내한다.
- 사용자 요청에 따라 앱 실행·자동 테스트·타입 검사·빌드는 수행하지 않았다. 시작/끝 각각의 축소·확장, 잠금 트랙, 링크 V/A, 속도 램프와 세 종류 로컬 자동화·단어 자막·트랙 자동화, ADR 앞/내부/뒤, 덮어쓰기 네 교차 형태와 Undo/Redo·저장 재열기는 사용자 검증 전이다.

## 2026-08-09 — 원본 핸들과 링크 관계를 보존하는 일반·롤·슬립·슬라이드 트림

- 기존 일반 트림은 미디어 끝을 넘어 클립을 늘릴 수 있었고, 슬립은 source offset의 상한이 없었다. 롤은 실제로 맞닿지 않은 이웃도 변경했으며 롤·슬라이드는 선택 트랙만 수정해 링크된 V/A가 어긋날 수 있었다. `trimConstraints.ts`에 속도 곡선 적분 소스 길이, 영상/오디오 분석 길이, 중첩 시퀀스 길이와 이미지·정지/생성 레이어 구분을 사용하는 공통 원본 핸들 모델을 추가했다.
- 일반 트림은 같은 링크 ID와 편집 경계를 공유하는 클립을 모아 시작/끝 delta 허용 구간을 교집합으로 제한한다. 원본 이전/이후 핸들과 같은 트랙의 빈 간격까지만 확장하고, 안쪽 재단은 속도·변형·시각·오디오 자동화와 단어 자막을 같은 로컬 구간으로 슬라이스한다. 바깥 transition/fade는 유지하되 새 길이를 넘지 않게 제한한다.
- 슬립은 선택 경계의 실제 속도와 역재생 방향으로 source offset 이동량을 계산하고, 모든 링크 클립의 `[0, source duration - consumed span]`을 동시에 만족하는 delta만 적용한다. 정지 이미지·정지 프레임·생성 레이어는 바뀌지 않는 source offset을 임의로 이동하지 않고 구체적인 안내를 표시한다.
- 롤은 링크된 각 트랙에 실제로 맞닿은 반대편 클립이 모두 있을 때만 양쪽 경계를 같은 delta로 변경한다. 슬라이드는 각 링크 트랙의 앞·선택·뒤 세 클립을 원자적으로 변경한다. 양쪽 원본 핸들, 최소 50ms, 타임라인 0초, 중복 링크, 잠긴 링크 트랙 또는 ADR 테이크/컴프 중 하나라도 위반하면 일부 트랙만 수정하지 않는다.
- 리플 확장도 같은 소스 한계와 링크 경계 집합을 사용한다. 이전에는 선택 영상만 늘고 같은 시작/끝의 링크 오디오가 timeline gap 뒤로 밀릴 수 있었지만, 이제 공통 확장량을 먼저 계산한 뒤 프로젝트 대본·제안·마커·ADR 이동과 함께 링크 V/A 전체를 확장한다.
- App은 고급 트림 결과의 적용 여부·실제 제한 경계·차단 사유를 받아 변경이 없는 Undo 항목을 만들지 않고 사용자에게 원본 핸들·인접 관계·잠금·ADR 원인을 표시한다. 사용자 요청에 따라 앱 실행, 자동 테스트, 타입 검사와 빌드는 수행하지 않았다. 실제 정/역방향 V/A, 가변 속도, 중첩, J/L 경계, 이미지와 ADR 조합은 사용자 검증 전이다.

## 2026-08-09 — 23개 실무 흐름 유한 정적 소스 연결 감사

- 장시간 대화의 완료 보고에 의존하지 않고 `docs/10-implementation-evidence-matrix.md`의 실제 행을 다시 세어 M1~R2 총 23개임을 확인했다. 이전 보고의 22개는 계산 오류로 정정했으며, 이후 진행률은 문서 행 수를 직접 근거로 사용한다.
- 입력/프로젝트/편집·후반, 출력/납품, 교환/협업/배포 세 묶음으로 UI 명령→App 조정 함수→편집·미디어 엔진→저장/네이티브 서비스의 정적 호출을 한 번씩 감사했다. 근거표에서 인용한 62개 경로는 현재 작업 폴더에 모두 존재한다.
- M1~S1은 미디어 가져오기·분석·프록시, 프로젝트/시퀀스, 공통 source-time 편집·미리보기, 색상/마스크/오디오/ADR, 전사·자막·쇼츠 파생 호출이 연결되어 있다. O1~D1은 H.264/H.265·HDR·구간 복구 출력과 Delivery Guard/완성 파일 R128 측정이 연결되어 있다. X1~R1은 교환·검토·병합·lock, 오류 수집과 서명 업데이트 클라이언트/서비스가 연결되어 있다.
- 이번 감사에서 고급 트림 수정 이후 추가로 끊어진 UI→엔진 연결은 정적으로 발견되지 않았다. 이를 런타임·품질 검증으로 간주하지 않으며 현재 상태는 `V1 소스 개발 후보`다. 23개 행 모두 사용자 검증 전이고, R1 운영 배포 및 R2 설치본 서명·공증은 외부 조건이다. 사용자 요청에 따라 앱 실행·자동 테스트·타입 검사·빌드·패키징은 수행하지 않았다.

## 2026-08-09 — 컴파일 차단 제거와 최신 Windows 패키지 재빌드

- TypeScript 7과 React 19 타입 정의에서 드러난 76개 오류를 수정했다. 오디오 조정 분할 시 필수 기본값 보존, `captureActiveSequence` hook 선언 순서, Hugging Face 진행 이벤트 union narrowing, React ref 초기값, 속도·쇼츠 keyframe literal, 색상 노드 tone-map 속성, CSS blend mode, ArrayBuffer 소유권, Map iterator 호환성, WebCodecs HDR 색공간·visible rectangle 변환을 정리했다.
- Rust의 HDR MP4 metadata 복사 `Read::by_ref` 모호성과 LAN 검토 HTTP request header의 immutable/mutable borrow 충돌을 수정했다.
- `pnpm check`가 TypeScript 검사, Vitest 9개 파일·22개 테스트, Vite production build와 Rust `cargo check`를 모두 통과했다. 실제 `flower.mp4` H.264/AAC와 `t-rex-roar.mp3` 스트림 metadata를 Mediabunny로 읽는 픽스처 테스트가 포함된다.
- `pnpm desktop:build`로 최신 Windows x64 `editweave.exe`, MSI와 NSIS를 재생성했다. 새 EXE를 8초간 기동해 즉시 종료나 crash가 없음을 확인한 뒤 테스트 프로세스만 종료했다.
- 이 검증은 자동 컴파일·단위 테스트·기본 기동 범위다. 실제 GUI 가져오기/편집/출력, 장시간 VFR/HDR·HEVC Main10, 설치·제거·업데이트, 코드 서명, macOS와 운영 서버는 계속 출시 게이트로 남는다.

## 2026-08-09 — 전체 출시 기준 런타임·회귀·설치 검증 확대

- MP4/MP3/PNG 실제 파일을 로컬 앱 UI로 가져와 분석 완료를 확인하고, 파일 입력이 실제 지원 목록보다 넓어 SVG를 받아 실패 자산으로 만들던 문제를 수정했다. 브라우저와 네이티브 선택기는 PNG/JPEG/WebP만 이미지로 허용하며 SVG는 프로젝트에 추가하기 전에 미지원으로 거부한다.
- 빈 시퀀스를 생성해 5.06초 H.264/AAC MP4를 타임라인에 배치하고 720×1280 H.264 SDR 30fps로 실제 합성·인코딩했다. 약 21초 뒤 영속 렌더 큐에서 시퀀스 3 작업의 100% 완료를 확인했다.
- 소스 트랙 대상, 리플 삽입·삭제의 마커/대본 단어/제안 동기화, 소스 길이·역재생 기반 트림/슬립, 오디오 DSP·역할 버스·Aux, Delivery Guard, 프로젝트 JSON 왕복·원본 재연결, 시퀀스 ID 안전 복제·삭제 보호 회귀를 추가했다. `pnpm check`가 Vitest 12개 파일·41개 테스트, TypeScript, production build와 Rust `cargo check`를 모두 통과했다.
- 별도 장시간 데이터 성능 측정에서 10분/1,200클립 2.14ms, 30분/3,600클립 3.05ms, 60분/7,200클립 4.46ms에 1.25초 리플 삭제와 JSON 직렬화를 완료했다. 이 수치는 편집 데이터 연산 기준이며 실제 장시간 미디어 디코딩·재생의 A/V sync와 GPU·메모리 검증은 남아 있다.
- 최신 Windows x64 EXE/MSI/NSIS를 다시 빌드했다. release EXE와 NSIS 설치본 EXE가 각각 8초 기동을 통과했고, NSIS를 작업 폴더 아래 격리 경로에 무인 설치·제거해 양쪽 종료 코드 0, 설치 파일과 HKCU 제거 항목 정리를 확인했다. MSI 데이터베이스는 EditWeave 0.1.0, EditWeave Studio, ProductCode와 UpgradeCode, ALLUSERS=1을 확인했다.
- 최종 SHA-256은 EXE `04BB214FB7FB137350A23022583A19ACCA33E8485B86CBF6531EB7C7A8B71E03`, MSI `ADB99C6DAA7F974F0F9776834BBCBADDEA24A1F635EF7A4D83477C310E92F39A`, NSIS `214FD64BEEDE0377C6F482CDBBAF0839AAE0F389E10B55915C41FD35C0DE82A7`이다. 세 산출물 모두 Authenticode `NotSigned`이므로 코드 서명 전에는 상용 출시 완료로 판정하지 않는다.
- GitHub Desktop build workflow를 상용 게이트로 강화했다. Windows는 Base64 PFX를 임시 사용자 인증서 저장소에 가져와 허용 Subject를 확인하고 Tauri 임시 config로 SHA-256 Authenticode/timestamp 서명한 뒤 EXE·MSI·NSIS 세 파일의 `Valid`와 Signer Subject를 재검사한다. macOS는 Developer ID 인증서·공증 자격 증명을 필수화하고 `codesign --strict`, Gatekeeper `spctl`, stapled ticket을 검증한다. 양쪽 모두 `pnpm check`가 먼저 통과해야 하며 인증서가 없으면 무서명 artifact를 성공으로 업로드하지 않는다.
