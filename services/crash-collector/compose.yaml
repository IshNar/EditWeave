services:
  collector:
    build:
      context: .
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    environment:
      CUTLINE_CRASH_HOST: 0.0.0.0
      CUTLINE_CRASH_PORT: 8787
      CUTLINE_CRASH_DATA_DIR: /data
      CUTLINE_CRASH_RETENTION_DAYS: ${CUTLINE_CRASH_RETENTION_DAYS:-30}
      CUTLINE_CRASH_MAX_STORAGE_MB: ${CUTLINE_CRASH_MAX_STORAGE_MB:-512}
      CUTLINE_CRASH_MAX_BODY_KB: ${CUTLINE_CRASH_MAX_BODY_KB:-32}
      CUTLINE_CRASH_RATE_LIMIT_PER_MINUTE: ${CUTLINE_CRASH_RATE_LIMIT_PER_MINUTE:-60}
      CUTLINE_CRASH_ALLOWED_ORIGINS: ${CUTLINE_CRASH_ALLOWED_ORIGINS:-tauri://localhost,http://tauri.localhost,https://tauri.localhost}
      CUTLINE_CRASH_TRUST_PROXY: 1
    volumes:
      - crash_data:/data
    networks:
      - private
    security_opt:
      - no-new-privileges:true

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      - collector
    environment:
      CUTLINE_CRASH_DOMAIN: ${CUTLINE_CRASH_DOMAIN:?Set CUTLINE_CRASH_DOMAIN in .env}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - private
    security_opt:
      - no-new-privileges:true

networks:
  private:

volumes:
  crash_data:
  caddy_data:
  caddy_config:
