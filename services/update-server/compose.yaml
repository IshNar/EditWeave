services:
  update-server:
    build:
      context: .
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    environment:
      EDITWEAVE_UPDATE_HOST: 0.0.0.0
      EDITWEAVE_UPDATE_PORT: 8790
      EDITWEAVE_UPDATE_CHANNEL_DIR: /channel
      EDITWEAVE_UPDATE_PUBLIC_ORIGIN: https://${EDITWEAVE_UPDATE_DOMAIN:?Set EDITWEAVE_UPDATE_DOMAIN in .env}
      EDITWEAVE_UPDATE_PUBLIC_KEY: ${EDITWEAVE_UPDATE_PUBLIC_KEY:?Set EDITWEAVE_UPDATE_PUBLIC_KEY in .env}
      EDITWEAVE_UPDATE_KEY_ID: ${EDITWEAVE_UPDATE_KEY_ID:?Set EDITWEAVE_UPDATE_KEY_ID in .env}
      EDITWEAVE_UPDATE_ALLOWED_ORIGINS: ${EDITWEAVE_UPDATE_ALLOWED_ORIGINS:-tauri://localhost,http://tauri.localhost,https://tauri.localhost}
      EDITWEAVE_UPDATE_MAX_ACTIVE_DOWNLOADS: ${EDITWEAVE_UPDATE_MAX_ACTIVE_DOWNLOADS:-100}
      EDITWEAVE_UPDATE_MAX_DOWNLOADS_PER_IP: ${EDITWEAVE_UPDATE_MAX_DOWNLOADS_PER_IP:-4}
      EDITWEAVE_UPDATE_TRUST_PROXY: 1
    volumes:
      - ../../release/update-channel:/channel:ro
    networks:
      - private
    security_opt:
      - no-new-privileges:true

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      - update-server
    environment:
      EDITWEAVE_UPDATE_DOMAIN: ${EDITWEAVE_UPDATE_DOMAIN:?Set EDITWEAVE_UPDATE_DOMAIN in .env}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - private
    security_opt:
      - no-new-privileges:true

networks:
  private:

volumes:
  caddy_data:
  caddy_config:
