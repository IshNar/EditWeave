# Troubleshooting

개발 및 사용자 테스트에서 발생한 문제를 재현 가능하게 기록한다. 해결되지 않은 문제도 삭제하지 않고 상태를 갱신한다.

## 기록 형식

```markdown
## YYYY-MM-DD — 짧은 문제 이름

- 상태: 조사 중 / 우회 방법 있음 / 해결
- 환경: OS, 앱 버전, GPU, 미디어 형식
- 증상:
- 재현 절차:
- 원인:
- 해결 또는 우회 방법:
- 회귀 테스트:
```

## 2026-08-08 — 초기 개발 환경

- 상태: 해결
- 환경: Windows, Node.js 24, pnpm 11
- 증상: 빈 작업 폴더라 실행 가능한 프로젝트가 없었다.
- 재현 절차: 작업 폴더에서 파일 목록 확인.
- 원인: 신규 프로젝트였다.
- 해결 또는 우회 방법: React/TypeScript/Vite 기반 GUI 프로토타입과 문서 구조를 초기화했다.
- 회귀 테스트: 의존성 설치 후 typecheck와 production build를 수행한다.

## 2026-08-08 — TypeScript 7 초기 타입 검사 오류

- 상태: 해결
- 환경: TypeScript 7.0.2, React 19.2.8
- 증상: 초기 빌드에서 값 없는 `useRef`와 선택 해제 상태, CSS side-effect import의 타입을 엄격하게 검사했다.
- 재현 절차: `pnpm typecheck` 실행.
- 원인: `undefined` 상태를 타입에 명시하지 않았고 Vite 클라이언트 타입 선언 파일이 없었다.
- 해결 또는 우회 방법: 선택 상태와 ref에 `undefined`를 명시하고 `vite-env.d.ts`에서 Vite 클라이언트 타입을 참조했다.
- 회귀 테스트: `pnpm typecheck`와 `pnpm build`를 다시 수행한다.

## 2026-08-08 — 9:16 미리보기 제목의 과도한 줄바꿈

- 상태: 해결
- 환경: 로컬 GUI 프로토타입, 1280×720 브라우저 뷰포트
- 증상: 16:9용 데모 제목 크기가 좁은 9:16 캔버스에도 적용되어 한두 글자마다 줄이 바뀌었다.
- 재현 절차: 상단 캔버스 선택에서 `9:16 · Shorts`를 선택한다.
- 원인: 미리보기 제목 크기가 화면 너비 기준으로만 반응하고 캔버스 화면비를 구분하지 않았다.
- 해결 또는 우회 방법: 캔버스에 화면비 클래스를 부여하고 9:16·1:1 전용 타이포그래피 규칙을 추가했다.
- 회귀 테스트: 16:9, 9:16, 1:1을 각각 전환하여 제목과 안전 영역을 확인한다.

## 2026-08-08 — Tauri Windows 리소스 아이콘 누락

- 상태: 해결
- 환경: Windows, Tauri 2.11.5, Rust 1.95
- 증상: 첫 `cargo check`가 `icons/icon.ico not found` 오류로 중단됐다.
- 재현 절차: `pnpm desktop:check`를 실행한다.
- 원인: Tauri가 Windows 실행 파일 리소스를 생성할 때 필요한 아이콘 세트가 신규 프로젝트에 없었다.
- 해결 또는 우회 방법: EditWeave 벡터 아이콘 원본을 만들고 Tauri 아이콘 생성기로 플랫폼별 아이콘을 생성한다.
- 회귀 테스트: `pnpm desktop:check`와 이후 `pnpm desktop:build`를 실행한다.

## 2026-08-08 — Windows 설치 번들 아이콘 경로 누락

- 상태: 해결
- 환경: Windows, Tauri 2.11.5, MSI/NSIS 번들러
- 증상: 릴리스 실행 파일은 만들어졌지만 설치 패키징이 `Couldn't find a .ico icon` 오류로 중단됐다.
- 재현 절차: 번들 아이콘을 명시하지 않은 상태에서 `pnpm desktop:build`를 실행한다.
- 원인: 플랫폼 아이콘 파일은 생성되어 있었지만 `tauri.conf.json`의 `bundle.icon` 목록에 연결되지 않았다.
- 해결 또는 우회 방법: PNG, ICNS, ICO 파일을 번들 아이콘 목록에 명시했다.
- 회귀 테스트: `pnpm desktop:build`가 Windows 설치 패키지 경로를 출력하는지 확인한다.

## 2026-08-08 — 로컬 AI 의존성 설치 스크립트 승인 필요

- 상태: 해결
- 환경: Windows, pnpm 11.16, Transformers.js 4.2
- 증상: `onnxruntime-node`, `protobufjs`, `sharp`의 빌드 스크립트가 승인되지 않아 일반 `pnpm` 명령의 의존성 상태 검사가 실패했다.
- 재현 절차: `@huggingface/transformers` 설치 후 `pnpm typecheck`를 실행한다.
- 원인: pnpm의 공급망 보호 정책이 네이티브/후처리 설치 스크립트를 기본 차단했다.
- 해결 또는 우회 방법: 잠금 파일로 고정된 의존성을 검토한 뒤 `pnpm approve-builds --all`로 해당 3개 패키지를 승인했다.
- 회귀 테스트: `pnpm typecheck`, `pnpm test`, `pnpm build`를 연속 실행한다.

## 2026-08-08 — AI WASM 번들 크기 경고

- 상태: 우회 방법 있음
- 환경: Vite 8, Transformers.js 4.2, ONNX Runtime Web
- 증상: production build가 약 23.6 MB WASM 파일과 500 KB 이상 지연 로드 청크 경고를 출력한다.
- 재현 절차: `pnpm build`를 실행한다.
- 원인: 로컬 Whisper 실행에 필요한 ONNX WebAssembly 런타임이며 코드와 분리된 동적 청크다.
- 해결 또는 우회 방법: 편집기 초기 JavaScript에서는 동적 import로 분리했다. Beta에서는 WebGPU 우선 경로, 모델/런타임 지연 다운로드, 캐시 상태 UI를 추가한다.
- 회귀 테스트: 첫 화면 로드에서 AI 청크를 요청하지 않는지 네트워크 패널로 확인하고, 음성 인식 버튼을 누를 때만 로드되는지 점검한다.

## 2026-08-08 — WebCodecs 미지원 형식

- 상태: 우회 방법 있음
- 환경: Windows WebView/Chromium, Developer Preview 0.1.0
- 증상: 브라우저 디코더가 지원하지 않는 코덱이나 SVG 정지 이미지는 분석·출력할 수 없다.
- 재현 절차: HEVC/일부 프로 코덱 또는 SVG를 가져온다.
- 원인: 현재 공통 미디어 경로가 OS/브라우저의 WebCodecs 디코더 지원 범위를 사용한다.
- 해결 또는 우회 방법: 현재는 H.264/AAC MP4와 PNG/JPEG/WebP를 권장한다. Beta에서 선택형 FFmpeg 호환 워커와 실제 프록시 생성으로 확장한다.
- 회귀 테스트: 지원 미디어 매트릭스를 샘플 파일로 자동 실행하고 성공·명시적 오류를 모두 확인한다.

## 2026-08-08 — 첫 미디어가 1초 뒤에 삽입됨

- 상태: 해결
- 환경: Developer Preview 0.1.0, 빈 비디오/오디오 트랙
- 증상: 빈 트랙에 처음 추가한 미디어가 0초가 아니라 1초에서 시작해 의도하지 않은 검은 화면·무음이 생겼다.
- 재현 절차: 빈 프로젝트를 열고 MP4를 가져온 뒤 `선택 미디어를 타임라인에 추가`를 누른다.
- 원인: 타임라인 끝 계산 뒤 항상 1초 간격을 더했다.
- 해결 또는 우회 방법: 첫 클립은 0초, 이후 클립은 직전 마지막 프레임 뒤에 간격 없이 배치하도록 수정했다.
- 회귀 테스트: 빈 프로젝트에 `flower.mp4`를 추가하고 접근성 레이블이 `00:00:00부터`인지 확인한다.

## 2026-08-08 — 잠긴 트랙의 클립이 수정됨

- 상태: 해결
- 환경: Developer Preview 0.1.0, 비디오·오디오·자막 트랙
- 증상: 트랙을 잠가도 툴바/단축키 분할·삭제와 인스펙터 속성 변경이 가능했다.
- 재현 절차: 클립을 선택하고 트랙 잠금을 켠 뒤 분할·삭제 또는 속성 입력을 시도한다.
- 원인: 드래그만 잠금 상태를 검사하고 App 편집 명령과 인스펙터는 검사하지 않았다.
- 해결 또는 우회 방법: 편집 명령에서 잠금을 다시 검증하고 툴바와 모든 인스펙터 필드를 비활성화했다.
- 회귀 테스트: 잠금 후 분할·삭제 버튼, 이름·시간·변형 입력이 모두 비활성화되는지 확인한다.

## 2026-08-08 — 트랙 음소거가 미리보기·출력에 반영되지 않음

- 상태: 해결
- 환경: Developer Preview 0.1.0, 프로그램 모니터와 H.264/AAC 출력
- 증상: 비디오·자막·오디오 트랙을 음소거해도 프로그램 모니터 또는 최종 렌더에서 계속 사용됐다.
- 재현 절차: 타임라인에 실제 MP4와 자막을 놓고 각 트랙의 음소거 버튼을 누른다.
- 원인: 타임라인 UI 상태만 변경하고 활성 클립 선택·렌더 입력 선택에서 `muted`를 걸러내지 않았다.
- 해결 또는 우회 방법: 미리보기 영상/자막, 단일·일괄 출력 영상/자막/오디오 입력이 음소거 트랙을 제외하도록 수정했다.
- 회귀 테스트: 비디오 음소거 시 모니터 영상이 사라지고 출력 버튼이 비활성화되는지, 자막 음소거 시 오버레이가 사라지는지 확인한다.

## 2026-08-08 — 대본 텍스트 수정이 실행 취소되지 않음

- 상태: 해결
- 환경: Developer Preview 0.1.0, 대본 기반 편집
- 증상: 자막 파일 가져오기와 구간 삭제는 실행 취소됐지만 textarea에서 직접 고친 문장은 편집 기록에 남지 않았다.
- 재현 절차: 대본 문장을 수정하고 다른 패널을 누른 뒤 실행 취소한다.
- 원인: 입력 중 화면 상태만 갱신하고 편집 시작 전 스냅샷을 기록하지 않았다.
- 해결 또는 우회 방법: 포커스 시 편집 전 스냅샷을 잡고 blur 시 한 묶음의 편집 기록으로 커밋한다.
- 회귀 테스트: 문장 수정→실행 취소→다시 실행→다시 취소 순서로 원문과 수정문이 정확히 왕복하는지 확인한다.

## 2026-08-08 — SVG 미디어 분석 실패 상태가 명확하지 않음

- 상태: 해결
- 환경: Chromium/WebView, SVG 테스트 카드
- 증상: SVG는 `<img>` 미리보기에 표시되지만 `createImageBitmap` 분석이 실패해 타임라인 추가 버튼이 비활성화됐고, 카드에는 실패 원인이 보이지 않았다.
- 재현 절차: SVG 파일을 미디어로 가져온다.
- 원인: SVG 디코딩 지원 차이와 오류 상태 표시 분기가 부족했다.
- 해결 또는 우회 방법: 분석 실패 원인을 자산 카드에 표시하고 완료·실패 개수를 알림에 구분했다. 영상 제작용 정지 이미지는 PNG/JPEG/WebP를 기본 지원 형식으로 안내한다.
- 회귀 테스트: SVG 실패 표시와 PNG 정상 분석·추가를 함께 확인한다.

## 2026-08-08 — 큰 원본의 프록시 생성 제한

- 상태: 개발 해결, 사용자 검증 대기
- 환경: V1 Development Candidate, Tauri 데스크톱
- 증상: 이전 버전은 1.5GB를 초과하는 원본에서 프록시 생성을 막았다.
- 원인: 이전 변환은 결과 전체를 `BufferTarget`에 보관했다.
- 해결 또는 우회 방법: 데스크톱은 `StreamTarget`으로 앱 캐시에 직접 쓰며, 1.5GB 초과 원본에 파일 경로가 있으면 시스템 FFmpeg 보조 경로를 사용한다. 브라우저 모드는 여전히 1.5GB 제한이 있다.
- 회귀 테스트: 1.5GB 초과 파일의 변환 중 메모리, 취소, 앱 캐시 크기, 재실행 복원과 삭제를 확인한다.

## 2026-08-08 — 프로젝트 열기 직후 디스크 프록시 복원이 건너뛰어질 수 있음

- 상태: 해결
- 환경: Tauri 데스크톱, 프록시 캐시 경로가 포함된 `.editweave.json`
- 증상: 자동 저장에서 시작할 때는 복원되지만 실행 중 다른 프로젝트 파일을 열면 디스크 프록시가 남아 있어도 오프라인으로 표시될 수 있었다.
- 원인: React 상태 반영 전에 비동기 캐시 복원이 이전 `assetsRef`를 검사해 새 자산을 더 이상 유효하지 않은 것으로 판단했다.
- 해결 또는 우회 방법: 프로젝트 적용 시 복원 자산을 상태와 참조에 동시에 반영한 뒤 캐시 로드를 시작한다.
- 회귀 테스트: 캐시 메타데이터 직렬화·복원 상태를 자동 검사하고, 데스크톱 실물 캐시 재열기 검증을 릴리스 체크리스트에 유지한다.

## 2026-08-09 — 여러 미디어를 가져왔는데 일부가 없거나 같은 파일이 두 번 생김

- 상태: 경로별 부분 실패 보고·동일 원본 중복 제거·stable ID 재분석 소스 구현 완료, Windows/macOS 사용자 검증 필요
- 증상: 여러 파일을 한 번에 선택하거나 폴더에서 끌어왔는데 일부만 추가된다. 또는 같은 파일을 다시 선택했는데 목록 수가 늘지 않거나, 이전 빌드에서는 Tauri와 HTML 드롭이 겹쳐 동일 자산이 두 개 생길 수 있었다.
- 원인: 운영체제 권한, 이동·잠금 파일, 디렉터리 또는 읽을 수 없는 경로는 개별 stat/read가 실패할 수 있다. 현재는 성공 파일을 버리지 않고 분석하며 `경로 읽기 실패 N개 (첫 파일명)`을 표시한다. 지원하지 않는 형식은 `미지원 N개 (첫 파일명)`으로 분리한다. 이미 ready/analyzing인 같은 sourcePath+크기 또는 같은 브라우저 File 지문은 의도적인 중복 방지 대상으로 새 ID를 만들지 않는다.
- 해결: 읽기 실패로 표시된 첫 파일의 존재 여부와 운영체제 권한을 확인한 뒤 해당 파일만 다시 선택한다. 같은 원본을 별도 자산으로 독립 관리하려면 운영체제에서 다른 파일명으로 복사한 뒤 가져온다. 오프라인 또는 분석 실패 항목은 같은 파일을 다시 선택하면 기존 stable ID로 재연결·재분석된다.
- 판정: 읽을 수 있는 파일 2개와 권한이 없는/사라진 경로 1개를 함께 드롭했을 때 2개는 분석되고 실패 1개가 표시되어야 한다. 같은 파일을 선택과 드롭으로 연속 전달해도 프로젝트 자산은 한 개여야 하며, 분석 오류 파일을 다시 선택하면 ID를 유지한 채 ready 또는 새 오류 상태가 되어야 한다.

## 2026-08-08 — `run.bat`에서 실행 파일을 찾지 못함

- 상태: 안내 제공
- 환경: Windows, 아직 `pnpm desktop:build`를 실행하지 않은 작업 폴더
- 증상: `run.bat`을 실행해도 EditWeave 창이 열리지 않는다.
- 원인: 기본 개발 실행에 필요한 pnpm/`node_modules`도 없고 `src-tauri/target/release/editweave.exe`도 생성되지 않았다.
- 해결 또는 우회 방법: `pnpm install` 후 `run.bat`으로 최신 소스를 실행하거나, `pnpm desktop:build` 후 `run.bat release`를 사용한다.
- 회귀 테스트: 기본 실행은 `desktop:dev`, `release` 인자는 기존 EXE를 여는지 확인한다.

## 2026-08-08 — `run.bat` 명령의 첫 글자가 유실됨

- 상태: 해결
- 환경: Windows `cmd.exe`, LF 줄바꿈으로 생성된 배치 파일
- 증상: `EDITWEAVE_INSTALLER`가 `UTLINE_INSTALLER`로 해석되고 실행 파일 경로 문법 오류가 발생했다.
- 재현 절차: LF 줄바꿈 상태의 `run.bat`을 `cmd.exe /c run.bat`으로 실행한다.
- 원인: Windows 배치 파서가 LF 전용 줄바꿈과 UTF-8 한글 안내를 안정적으로 처리하지 못했다.
- 해결 또는 우회 방법: `run.bat`을 CRLF로 저장하고 시작 시 코드 페이지를 UTF-8로 설정했다.
- 회귀 테스트: 배치 파일 실행 후 새 `editweave.exe` PID와 정확한 릴리스 경로를 확인했다.

## 2026-08-09 — `run.bat` 수정 후 명령 조각과 깨진 한글이 출력됨

- 상태: 해결, 사용자 재확인 대기
- 환경: Windows `cmd.exe`, V1 Development Candidate의 LF 전용 UTF-8 배치 파일
- 증상: `TALLER`, `ules" goto release`, 깨진 한글 문자열 등이 독립 명령으로 실행됐다는 오류가 연속으로 표시됐다.
- 원인: 기능 확장 과정에서 `run.bat`을 다시 만들면서 CRLF가 LF로 되돌아갔고, UTF-8 한글 안내까지 포함돼 Windows 배치 파서가 줄 경계를 잘못 처리했다.
- 해결 또는 우회 방법: 배치 파일을 영문 ASCII 명령만 사용하도록 바꾸고, `pnpm` 배치 호출에는 `call`을 사용했으며, 파일 줄바꿈을 명시적으로 CRLF로 정규화했다.
- 회귀 테스트: 사용자가 `run.bat`과 `run.bat release`를 각각 실행해 명령 조각 오류 없이 개발 앱과 기존 EXE가 열리는지 확인한다.

## 2026-08-08 — 프록시 생성 취소 버튼이 없음

- 상태: 해결
- 환경: Developer Preview 0.1.0, 편집 프록시 생성 UI
- 증상: 변환 엔진은 취소 신호를 지원하지만 생성 중 버튼이 비활성화되어 사용자가 직접 중단할 수 없었다.
- 원인: `AbortController`를 화면 종료 정리용으로만 사용하고 미디어 패널에 취소 콜백을 전달하지 않았다.
- 해결 또는 우회 방법: 생성 중 버튼을 `취소`로 전환해 해당 자산의 컨트롤러를 중단하고, 취소는 실패가 아니라 다시 생성 가능한 기본 상태로 복구한다.
- 회귀 테스트: 프록시 생성 중 취소하면 오류 상태가 남지 않고 `생성` 버튼으로 돌아오는지 실제 화면에서 확인했다. 이어서 재생성도 성공했다.

## 2026-08-08 — VFR 진단의 실물 스마트폰 검증 필요

- 상태: 부분 해결
- 환경: Developer Preview 0.1.0, 영상 패킷 최대 240개 표본
- 증상: 합성 VFR 패턴은 감지하지만 특정 iPhone/Android 촬영본의 편집·출력 싱크 정확도는 아직 보증할 수 없다.
- 원인: 컨테이너의 평균 프레임레이트만으로 VFR을 판정할 수 없어 프레임 지속 시간 분산을 사용하며, 실제 기기별 타임스탬프 패턴 회귀 자료가 아직 부족하다.
- 해결 또는 우회 방법: VFR 경고가 보이면 고정 30fps 프록시를 생성해 편집한다. 최종 출력은 원본을 사용하므로 실물 소스의 A/V 동기화 확인이 필요하다.
- 회귀 테스트: 고정 29.97fps 패킷은 CFR, 15/30fps 혼합 패킷은 VFR로 분류한다. 후속으로 iPhone/Android 원본 픽스처를 추가한다.

## 자주 확인할 항목

## 2026-08-08 — 최신 소스와 기존 EXE의 기능이 다름

- 상태: 안내 제공
- 환경: Windows, V1 Development Candidate 소스와 0.1.0 배포 바이너리가 함께 있는 작업 폴더
- 증상: `run.bat release` 또는 기존 설치 프로그램에서 새 멀티트랙·키프레임·H.265 기능이 보이지 않는다.
- 원인: 사용자 요청에 따라 이번 패스에서는 최신 데스크톱 빌드를 만들지 않았으며 기존 EXE/MSI/NSIS는 이전 0.1.0 결과물이다.
- 해결 또는 우회 방법: 최신 소스는 `run.bat`으로 실행한다. 최신 배포 파일은 사용자 검증 후 `pnpm desktop:build`로 생성한다.
- 회귀 테스트: `run.bat`과 `run.bat release`의 화면 기능 차이를 확인하고, 새 빌드 뒤 둘이 같은 버전을 표시하는지 확인한다.

## 2026-08-08 — FFmpeg 보조 프록시를 만들지 못함

- 상태: 환경 의존
- 환경: 1.5GB 초과 또는 WebCodecs 미지원 MOV/MXF/전문 코덱 원본
- 증상: 프록시 생성 시 FFmpeg 실행 또는 변환 실패 메시지가 보인다.
- 원인: `ffmpeg`가 PATH에 없거나 설치된 빌드에 원본 디코더 또는 `libx264` 인코더가 없다.
- 해결 또는 우회 방법: FFmpeg 정식 배포본을 설치하고 새 터미널에서 `ffmpeg -version`을 확인한 뒤 EditWeave을 다시 실행한다. 미디어는 데스크톱의 `가져오기` 버튼으로 열어 원본 경로를 보존해야 한다.
- 회귀 테스트: ProRes/DNxHR 표본과 1.5GB 초과 표본에서 앱 캐시 MP4 생성·재실행 복원·최종 출력까지 확인한다.

## 2026-08-08 — H.265 출력이 시작되지 않음

- 상태: 환경 의존
- 환경: H.265/HEVC WebCodecs 인코더를 제공하지 않는 GPU·드라이버·WebView
- 증상: 내보내기에서 H.265를 선택하면 지원하지 않는 인코더 메시지가 보인다.
- 원인: H.265는 앱 코드만으로 보장할 수 없고 OS·GPU·WebView의 WebCodecs 구현과 하드웨어 드라이버에 의존한다.
- 해결 또는 우회 방법: H.264를 선택하거나 GPU 드라이버와 WebView를 업데이트한다. H.265가 필수인 배포 환경은 지원 장비 목록을 별도로 확정한다.
- 회귀 테스트: 하드웨어 우선·자동·소프트웨어 우선 각각에서 H.264/H.265 지원 결과와 생성 파일 재생을 확인한다.

## 2026-08-08 — 쇼츠 얼굴 자동 리프레임이 중앙에만 머묾

- 상태: 환경 의존
- 환경: `FaceDetector` API가 없는 WebView 또는 얼굴이 작거나 가려진 영상
- 증상: 쇼츠를 만들어도 위치 X 키프레임이 생성되지 않거나 중앙 구도만 적용된다.
- 원인: 로컬 얼굴 표본 감지는 WebView의 `FaceDetector` API가 있을 때만 실행되며 다섯 표본 프레임에서 얼굴을 찾지 못할 수도 있다.
- 해결 또는 우회 방법: 인스펙터에서 위치 키프레임을 수동 추가한다. 지원 WebView에서는 정면 얼굴이 분명한 원본으로 다시 분석한다.
- 회귀 테스트: 한 명·두 명·얼굴 없음 표본에서 추적점과 안전한 중앙 폴백을 확인한다.

## 2026-08-08 — macOS 앱이 서명 또는 공증되지 않음

- 상태: codesign·pkgutil·Gatekeeper·고정 Team ID 검증기 구현 완료, 외부 자격 증명·실제 공증 필요
- 환경: macOS Apple Silicon CI 또는 로컬 빌드
- 증상: 생성 앱을 다른 Mac에서 열 때 Gatekeeper 경고가 나타난다.
- 원인: Apple Developer ID 인증서, Apple ID 앱 암호와 Team ID가 저장소에 제공되지 않았다.
- 해결 또는 우회 방법: GitHub Actions secret에 `APPLE_CERTIFICATE`, `APPLE_CERTIFICATE_PASSWORD`, `APPLE_SIGNING_IDENTITY`, `APPLE_ID`, `APPLE_APP_PASSWORD`, `APPLE_TEAM_ID`를 설정하고 태그 빌드를 실행한다. 앱 빌드 환경의 `EDITWEAVE_UPDATE_APPLE_TEAM_ID`에도 같은 Team ID를 넣는다.
- 회귀 테스트: 새 Mac에서 DMG 설치, Gatekeeper 실행, 앱 업데이트와 미디어 권한을 확인한다.

## 2026-08-08 — 렌더 중단 안내가 나타남

- 상태: 의도된 복구 동작
- 환경: 내보내기 중 앱 강제 종료 또는 인코더 오류
- 증상: 다음 실행 시 이전 렌더가 특정 퍼센트에서 중단됐다는 알림이 보인다.
- 원인: 완성되지 않은 렌더 복구 레코드를 로컬에 유지한다.
- 해결 또는 우회 방법: 내보내기 창에서 같은 설정으로 다시 시작한다. 정상 저장 완료 시 기록은 자동 삭제된다.
- 회귀 테스트: 렌더 중 강제 종료, 재실행 안내, 재시도 성공 후 안내 소멸을 확인한다.

### 브라우저에서 로컬 영상이 열리지 않을 때

- 파일 자체가 브라우저가 디코딩할 수 있는 코덱인지 확인한다.
- 같은 `.mp4` 확장자라도 내부 코덱이 다를 수 있다.
- 브라우저 모드는 WebCodecs/브라우저 디코더 범위에 제한된다. 데스크톱은 원본 경로가 있는 경우 FFmpeg 프록시 보조 경로를 사용할 수 있다.

### 영상 길이가 10초로 표시될 때

- 메타데이터 분석에 실패했을 때 사용하는 안전 기본값이다. 미디어 카드의 디코더·코덱 오류, 원본 경로 권한과 프록시 상태를 함께 기록한다.

### 타임라인 재생과 실제 영상이 일치하지 않을 때

- 원본이 VFR이면 고정 30fps 편집 프록시를 사용하고 최종 원본 출력의 A/V 동기화를 비교한다.
- 속도 변경·역재생·중첩·멀티캠 구간이면 문제 시점과 사용한 원본/프록시 여부를 함께 기록한다.

## 2026-08-09 — 편집 화면이 `defaultCaptionStyle is not a function`으로 복구 모드에 진입함

- 상태: 수정 완료, 사용자 재확인 필요
- 환경: 최신 소스를 `run.bat` 개발 모드로 실행한 Windows 데스크톱 앱
- 증상: 시작 직후 편집 화면 대신 복구 화면이 나타나며 `defaultCaptionStyle is not a function` 오류가 표시된다.
- 원인: 기본 자막·색상·오디오 설정이 객체 상수로 선언되어 있었지만 UI와 내보내기 코드에서는 새 설정을 만드는 함수로 호출했다.
- 해결: `defaultCaptionStyle`, `defaultColorAdjustment`, `defaultAudioAdjustment`를 각각 독립된 새 객체를 반환하는 팩토리 함수로 통일했다. 자막 오류 이후 발생할 수 있던 색상·오디오의 같은 유형 오류도 함께 수정했다.
- 재확인: 개발 앱의 복구 화면에서 `앱 다시 불러오기`를 누르거나 `run.bat`을 다시 실행한 뒤 편집 화면, 클립 속성, 자막 생성을 차례로 확인한다.

## 2026-08-09 — 영상 파일 드롭과 가져오기 버튼이 모두 미디어를 등록하지 않음

- 상태: 소스 수정 완료, 사용자 재확인 필요
- 환경: 브라우저 개발 화면 또는 Tauri Windows 개발 앱
- 증상: 영상 파일을 미디어 영역에 놓거나 `가져오기`를 눌러 선택해도 목록에 추가되지 않는다.
- 원인: Tauri는 운영체제 파일 드롭을 HTML5 `drop` 이벤트 대신 네이티브 드롭 이벤트로 전달하며, 일부 Windows 파일은 `File.type`이 빈 문자열이라 MIME 전용 필터에서 제외됐다. 브라우저 가져오기 버튼도 비동기 네이티브 확인 뒤 숨은 파일 입력을 눌러 사용자 활성화가 소실될 수 있었다.
- 해결: 브라우저에서는 클릭 시 즉시 파일 입력을 열고, Tauri에서는 `onDragDropEvent`의 경로를 실제 파일로 읽는다. 미디어 종류 판별에 확장자 폴백을 추가하고 드롭 대상을 미디어 패널 전체로 넓혔으며, 선택기를 다시 열 수 있도록 입력값을 초기화했다.
- 재확인: MP4/MOV를 각각 드롭과 `가져오기`로 등록하고 `분석 중`에서 `준비됨`으로 바뀌는지 확인한다. 실패하면 화면 하단 메시지 전체를 기록한다.

## 2026-08-09 — ComfyUI 로컬 서버 연결 실패 또는 CORS 오류

- 상태: 외부 실행 옵션 필요
- 환경: EditWeave 웹뷰와 로컬 ComfyUI 서버를 함께 실행하는 Windows/macOS
- 증상: `ComfyUI 연결 실패`, `Failed to fetch` 또는 브라우저 CORS 오류가 표시된다.
- 원인: ComfyUI가 실행되지 않았거나 기본 주소가 다르거나, 로컬 API가 EditWeave 웹뷰의 교차 출처 요청을 허용하지 않는다.
- 해결: ComfyUI를 실행하고 EditWeave 대화상자 주소를 실제 서버 주소로 맞춘다. 웹뷰 연결은 ComfyUI 실행 인자에 `--enable-cors-header`를 설정한다. 워크플로는 일반 UI JSON이 아니라 `Save (API Format)`으로 저장하며 `LoadImage` 노드 또는 `{{EDITWEAVE_INPUT}}` 입력 자리표시자를 포함한다.
- 재확인: `http://127.0.0.1:8188/system_stats` 응답, 이미지 업로드, `/prompt` 큐 등록, 결과 이미지의 EditWeave 미디어 목록 재등록을 확인한다.
- 참고: ComfyUI는 EditWeave의 필수 구성 요소가 아니다. 사용하지 않으면 실행·설정할 필요가 없으며 일반 가져오기, 편집, 자막, 쇼츠와 내보내기에 영향을 주지 않는다.

## 2026-08-09 — 앱 재실행 후 렌더 작업이 `중단`으로 표시됨

- 상태: 30초 완결 MP4 체크포인트 재개 구현 완료, 장시간 실제 원본 사용자 검증 필요
- 환경: 렌더 중 앱 전체 종료, 강제 종료 또는 운영체제 재시작
- 증상: 다음 실행의 렌더 작업 창에서 이전 작업이 `중단`으로 표시된다.
- 원인: 실행 중이던 프로세스 자체는 복원할 수 없으므로 작업 상태는 `중단`으로 바뀐다. 대신 단일 시퀀스는 완전히 마무리된 30초 MP4 구간까지만 체크포인트로 인정하고, 쇼츠 묶음은 완성 파일 단위로 기록한다. 쓰는 중 종료된 현재 구간이나 최종 결합 중인 MP4는 완료로 인정하지 않는다.
- 해결: 해당 작업의 `재시도`를 누른다. 프로젝트·시퀀스·펼친 타임라인·오디오 버스·출력 설정 fingerprint가 같고 캐시 파일이 실제 존재하면 마지막 완료 구간 다음부터 이어간다. 편집이나 출력 설정이 바뀌었으면 기존 체크포인트를 폐기하고 처음부터 시작한다.
- 출력 위치가 이미 선택된 작업은 같은 경로를 재사용한다. 최종 결합 중 종료되어 불완전 MP4가 생겨도 재시도 시 파일을 잘라 새로 결합하며, 완결 구간은 다시 인코딩하지 않는다. 결합은 로컬 encoded packet 재다중화이므로 FFmpeg 설치를 요구하지 않는다.
- 범위: 앱 캐시 파일을 유지할 수 있는 Windows/macOS 데스크톱 기능이다. 브라우저 실행은 운영체제가 영속 파일 경로를 제공하지 않으므로 세션 중 일시정지·재개만 지원한다.
- 재확인: 65초 이상 단일 시퀀스에서 두 번째 구간 도중 앱을 종료한 뒤 `재시도`했을 때 `완료 체크포인트 1/3`에서 시작하는지 확인한다. 최종 파일의 30초·60초 경계에서 영상 프레임, A/V 동기, 오디오 클릭이 없어야 하며 전체 러닝타임이 원본 시퀀스와 일치해야 한다.

## 2026-08-09 — 렌더 큐가 다음 작업으로 넘어가지 않거나 파일명이 바뀜

- 상태: 영속 대기열·자동 순차 실행 구현 완료, 사용자 장비 검증 필요
- 환경: 현재 프로젝트의 단일 시퀀스와 쇼츠 일괄 작업을 둘 이상 큐에 추가한 Windows/macOS 데스크톱 앱
- 증상: `현재 프로젝트 큐 실행` 버튼이 비활성화되거나, 출력 파일에 시퀀스명·번호 또는 `-2` 접미사가 붙는다.
- 원인: 다른 프로젝트의 대기 작업은 현재 프로젝트에서 실행하지 않는다. 같은 큐 또는 선택 폴더에 같은 이름이 있으면 기존 MP4 보호를 위해 안전한 고유 이름을 만든다.
- 해결: 작업 카드에 다른 프로젝트명이 표시되면 해당 프로젝트를 연 뒤 큐를 시작한다. 큐 시작 시 폴더를 한 번 선택하면 현재 프로젝트의 대기 작업만 순서대로 처리된다. `순차 실행 중지`는 현재 렌더를 끝낸 뒤 멈추고, 활성 작업의 `취소`는 현재 렌더와 연속 실행을 함께 중지한다.
- 재시작: 아직 시작하지 않은 `대기` 작업은 앱 종료 후에도 유지된다. 진행 중이던 작업은 안전상 `중단`으로 표시되며 개별 `재시도`를 누르면 단일 시퀀스는 마지막 완결 30초 체크포인트부터, 쇼츠 묶음은 실제 파일이 남은 완료 시퀀스 다음부터 재개한다.

## 2026-08-09 — 중첩 시퀀스 내용이 보이지 않음

- 상태: 소스 구현 완료, 사용자 재확인 필요
- 환경: 재생 헤드의 여러 트랙 클립을 `중첩`으로 묶은 프로젝트
- 증상: 보라색 `NEST` 클립은 보이지만 내부 영상·오디오·자막이 미리보기 또는 출력에 나타나지 않는다.
- 확인: 상단 시퀀스 선택기에서 `Nested ·` 항목을 열어 내부 클립과 원본 연결 상태를 확인한다. 부모 중첩 클립이나 내부 트랙이 음소거·숨김인지도 확인한다.
- 해결: 오프라인 원본은 미디어 패널에서 다시 연결한다. 손상된 외부 프로젝트가 서로를 참조하는 순환 중첩을 포함하면 순환 참조는 안전하게 건너뛴다.
- 재확인: V/A/T 클립을 함께 중첩하고 부모 시퀀스 미리보기, 내부 시퀀스 편집, 프로젝트 재열기, MP4 출력 순서로 확인한다.

## 2026-08-09 — 256MB 초과 영상 가져오기에서 메모리 사용이 급증하거나 프록시가 실패함

- 상태: 소스 개선 완료, 실물 대용량 파일 검증 대기
- 환경: Tauri 데스크톱, 256MB 초과 MP4/MOV/MKV 등
- 증상: 가져오기 직후 앱 메모리가 원본 크기만큼 증가하거나 자동 프록시가 오류 상태가 된다.
- 원인: 이전 경로는 네이티브 파일을 전부 `Uint8Array`로 읽었다. 현재는 영상만 파일 범위 읽기를 사용하므로 실패 시 경로 권한, 컨테이너 인덱스 또는 FFmpeg/WebCodecs 지원 범위를 확인해야 한다.
- 해결: 시스템 `가져오기`로 원본을 다시 선택해 경로 권한을 갱신한다. 전문 코덱이면 FFmpeg를 PATH에 설치하고 프록시를 다시 만든다. 대용량 오디오는 범위 읽기 대상이 아니며 실제 파일로 읽는다.
- 편집 안전장치: 대용량 영상은 자동 프록시가 준비되기 전 타임라인 추가 버튼이 비활성화된다. 준비 후 영상과 내장 오디오 미리보기는 프록시를 함께 사용한다.
- 메모리 경로: 디스크 프록시는 앱 캐시 전용 asset protocol URL로 재생하므로 복원 시 프록시 전체 크기만큼 JavaScript 메모리가 늘어서는 안 된다. 로컬 전사를 시작하면 디코딩을 위해 그 시점에 프록시 바이트를 읽는다.
- 재확인: 작업 관리자 메모리, 분석 완료, 자동 프록시 진행률, 원본/프록시 전환과 최종 원본 출력을 확인한다.

## 2026-08-09 — 앱 재실행 뒤 이전 원본 경로가 오프라인으로 표시됨

- 상태: persisted scope 등록 완료, 사용자 재확인 필요
- 환경: Tauri 데스크톱, 저장 후 재실행한 `.editweave.json`
- 증상: 디스크에 파일이 그대로 있어도 프로젝트가 원본을 오프라인으로 표시한다.
- 원인: 운영체제 파일 선택으로 얻은 동적 접근 범위가 재실행 뒤 복원되지 않았거나 파일이 이동·이름 변경됐다.
- 해결: 미디어 재연결로 같은 파일을 다시 선택한다. 최신 데스크톱 소스에는 Tauri persisted scope가 등록되어 새 선택 범위를 보존한다.
- 재확인: 원본을 가져오고 프로젝트 저장→앱 완전 종료→재실행→자동 재연결 순서로 확인한다.

## 2026-08-09 — 파형 동기화가 클립을 이동하지 않음

- 상태: 의도된 안전 동작
- 환경: 서로 다른 카메라·레코더의 오디오가 포함된 두 클립
- 증상: `파형 동기화`를 눌러도 이동한 클립이 없다는 안내가 나온다.
- 원인: 파형 상관 신뢰도가 0.25보다 낮거나, 두 자산의 분석 파형이 없거나, 후보가 기준 클립 시작점에서 60초보다 멀리 있다.
- 해결: 두 자산 분석 완료와 오디오 디코딩 상태를 확인하고 대략 같은 위치에 놓은 뒤 다시 실행한다. 음악 반복 구간처럼 오동기화 가능성이 큰 소스는 수동 슬립으로 맞춘다.
- 재확인: 공통 클랩 소리가 있는 두 원본에서 이동량과 입 모양/소리 일치를 확인한다.

## 2026-08-09 — 멀티캠 각도 버튼을 눌러도 전환되지 않음

- 상태: 입력 조건 안내
- 환경: 멀티캠 중첩 클립과 각도 1~4 버튼
- 증상: 각도 전환 대신 각도 수가 부족하다는 메시지가 나오거나 일반 클립이 바뀌지 않는다.
- 원인: 선택 클립이 멀티캠 시퀀스가 아니거나 생성된 비디오 각도 수보다 큰 번호를 눌렀다.
- 해결: 재생 헤드에 겹친 두 개 이상의 비디오 클립으로 `MC`를 만든 뒤 멀티캠 클립을 선택한다. 재생 헤드가 클립 내부이면 해당 지점에 컷을 만들고 오른쪽 구간의 각도를 변경한다.
- 재확인: 각도 1/2를 번갈아 누른 컷 경계와 미리보기·MP4 결과를 비교한다.

## 2026-08-09 — 크로마키 또는 색상 스코프 미리보기가 느림

- 상태: 성능 고도화 대상
- 환경: 고해상도 다중 레이어, 크로마키 또는 Source Scopes 활성화
- 증상: 재생 중 프레임이 끊기거나 스코프 갱신이 늦다.
- 원인: 크로마키는 축소한 프레임의 픽셀을 CPU 캔버스에서 처리하고 스코프도 선택 프레임 픽셀을 읽는다.
- 해결: 편집 프록시를 사용하고 필요하지 않을 때 인스펙터의 스코프 영역을 벗어난다. 다중 4K 크로마 레이어는 후속 GPU 셰이더 경로가 필요하다.
- 재확인: 원본/프록시에서 같은 구간의 재생 프레임과 최종 출력의 키 경계를 비교한다.

## 2026-08-09 — 효과 키프레임을 조절했는데 기본값이 바뀐 것처럼 보임

- 상태: 의도된 자동 키프레임 동작
- 환경: 시각 효과 키프레임이 하나 이상 있는 영상 클립
- 증상: 재생 헤드를 다른 시점에 놓고 크롭·마스크·크로마 값을 바꾸면 키프레임이 새로 생긴다.
- 원인: 효과 애니메이션이 시작된 뒤에는 현재 재생 헤드 값 변경을 해당 시점의 키프레임으로 기록하도록 설계했다.
- 해결: 전체 클립의 고정 효과로 돌아가려면 효과 키프레임을 모두 삭제한 뒤 값을 조정한다.
- 재확인: 두 시점에 효과 키프레임을 만들고 중간 프레임 보간이 미리보기와 출력에서 같은지 확인한다.

## 2026-08-09 — 마스크 페더·반전 결과가 느리거나 예상과 다름

- 상태: 소스 구현 완료, GPU 고도화 필요 가능
- 환경: 페더 또는 반전을 사용한 고해상도 영상·이미지 레이어
- 증상: 재생 프레임이 느려지거나 반전 시 영상 전체가 사라진다.
- 원인: 소프트 마스크는 알파 캔버스를 프레임마다 합성한다. 마스크가 `없음`이고 크롭도 0인 상태에서 반전하면 전체 프레임을 반전하므로 투명해지는 것이 정상이다.
- 해결: 타원·둥근 사각형·다각형 또는 크롭 범위를 먼저 지정한 뒤 반전한다. 편집 중에는 프록시를 사용하고 페더 비율을 필요한 만큼만 올린다.
- 재확인: 같은 프레임의 하드 마스크, 2~5% 페더, 반전 결과를 프로그램 모니터와 MP4에서 비교한다.

## 2026-08-09 — 얼굴 모션 추적 버튼이 추적점을 만들지 못함

- 상태: 환경·소스 의존
- 환경: 선택 클립 구간의 로컬 FaceDetector와 영상 프레임 디코더
- 증상: 추적 API 미지원, 디코더 없음 또는 얼굴 추적점 부족 메시지가 표시된다.
- 원인: WebView가 `FaceDetector`를 제공하지 않거나 얼굴이 너무 작고 가려졌거나, 원본 코덱을 WebCodecs가 디코딩하지 못한다.
- 해결: 편집 프록시를 만든 뒤 다시 시도하고, 얼굴이 분명한 구간으로 클립을 트림한다. API 미지원 장비에서는 변형 키프레임을 수동 사용한다.
- 진행 중 같은 버튼을 누르면 추적을 취소하며 기존 키프레임은 변경하지 않는다.
- 재확인: 정면 단일 인물 클립에서 분석 진행률, 생성 키프레임 수, 중앙 추종 움직임과 실행 취소를 확인한다.

## 2026-08-09 — Delivery Guard의 LUFS와 True Peak가 외부 미터와 다름

- 상태: 빠른 예상 기능, 정밀 실측 후속 예정
- 환경: 가져오기 분석 파형과 EditWeave 오디오 믹서
- 증상: 표시된 LUFS/True Peak가 최종 파일을 전문 미터로 측정한 값과 다르다.
- 원인: 현재 값은 빠른 납품 사전 검사를 위해 축약 파형과 클립·트랙 게인을 사용한 예상값이며 EBU R128 전체 스캔이 아니다.
- 해결: `-14 LUFS 맞춤`은 초벌 기준으로 사용하고 최종 납품 파일은 별도 BS.1770/EBU R128 미터로 확인한다.

## 2026-08-09 — 역할 버스 센드나 리미터가 미리보기와 출력에서 다르게 들림

- 상태: 버스 라우팅 구현 완료, 사용자 청취 검증 필요
- 환경: 같은 시간에 여러 클립이 겹치거나 클립 보조 센드와 역할 버스 리미터를 함께 사용하는 프로젝트
- 증상: 편집 중 모니터링한 피크 억제 정도가 최종 MP4와 미세하게 다를 수 있다.
- 원인: 최종 출력은 역할별 오디오를 먼저 합산한 다음 버스 리미터를 적용한다. 저지연 미리보기는 각 미디어 요소의 Web Audio 그래프에서 버스 게인·활성 상태·센드 레벨과 리미터 기준을 근사한다.
- 해결: 버스 솔로로 역할별 밸런스를 확인하고 최종 납품 전 짧은 범위를 출력해 버스 합산 결과를 청취한다. 실제 BS.1770/EBU R128 출력 파일 스캔과 공유 버스 그래프는 후속 전문 오디오 항목이다.
- 중첩 동작: 중첩 시퀀스는 내부 역할 버스의 활성 상태·게인·리미터와 자식/부모 게인·팬 자동화를 먼저 합성한 뒤 현재 시퀀스의 역할 버스로 들어간다. 기본값이 아닌 내부·외부 버스 게인은 두 단계로 누적되는 것이 정상이다.

## 2026-08-09 — 중첩 시퀀스가 검게 나오거나 출력 준비가 차단됨

- 상태: 경계 전환·참조 그래프 진단 구현 완료, 사용자 프로젝트 재확인 필요
- 환경: 중첩 시퀀스를 삭제·교환했거나 외부에서 `.editweave.json`의 시퀀스 참조를 수정한 프로젝트
- 증상: 중첩 클립 내부가 비어 보이거나 Delivery Guard가 누락·순환 참조·중첩 깊이를 차단 사유로 표시한다.
- 원인: 대상 시퀀스가 없거나 A → B → A처럼 자신에게 돌아오는 참조, 또는 16단계를 넘는 비정상 중첩은 안전하게 펼칠 수 없다.
- 해결: Delivery Guard에 표시된 시퀀스 경로를 따라 누락된 시퀀스를 복구하거나 순환을 만드는 중첩 클립을 제거한다. 정상 경계의 자식·부모 전환은 펼친 합성 클립에 환산되어 미리보기와 출력에 유지된다.

## 2026-08-09 — 사용자 모션 템플릿을 가져올 수 없거나 적용 시간이 다름

- 상태: 로컬 저장·JSON 공유 구현 완료, 사용자 재확인 필요
- 환경: 변형 또는 시각 효과 키프레임이 있는 영상/이미지 클립
- 증상: JSON 가져오기가 거부되거나 원본과 길이가 다른 클립에서 키프레임 시점이 달라 보인다.
- 원인: EditWeave 모션 템플릿 구조가 아닌 JSON은 안전하게 거부한다. 정상 템플릿은 시작·끝 동작을 유지하도록 저장 당시 클립 길이 대비 대상 클립 길이의 비율로 시간을 재배치한다.
- 해결: 인스펙터의 `공유`로 만든 `.editweave-motion.json`을 사용한다. 절대 시간이 필요한 동작은 적용 후 키프레임 시간을 직접 조정한다. 브라우저 데이터 삭제 시 로컬 템플릿도 지워질 수 있으므로 중요한 템플릿은 JSON으로 보관한다.

## 2026-08-09 — 클랩 또는 TC 동기화가 일부 클립을 움직이지 않음

- 상태: 컨테이너/수동 TC·클랩·파형 동기화 구현 완료, 포맷별 사용자 검증 필요
- 환경: 여러 카메라/레코더 클립, 분석 파형, FFprobe 컨테이너 TC 또는 사용자가 입력한 `HH:MM:SS:FF`/`HH:MM:SS;FF` 시작 타임코드
- 증상: 클랩 동기화가 피크를 찾지 못하거나 TC 동기화가 일부 클립을 건너뛴다.
- 원인: 클랩은 중앙값 대비 충분히 뚜렷한 단일 상승 피크가 필요하다. TC 방식은 기준과 대상 자산 모두 시작 타임코드가 있어야 하며 잠긴 트랙은 이동하지 않는다.
- 해결: 손뼉 소리가 약하거나 음악 피크가 반복되면 전체 `파형 동기화`를 사용한다. TC 방식은 미디어 패널에서 컨테이너 또는 수동 소스 시작 TC를 확인하고, 이동할 트랙의 잠금을 해제한다. FFmpeg 프록시는 원본 TC를 다시 판독해 일치 여부를 표시한다.
- 재확인: 대화 중심, 음악 중심, 클리핑 소스에서 예상값과 최종 실측값의 오차를 기록한다.

## 2026-08-09 — 원본 변경 반영 시 쇼츠에서 직접 수정한 컷이 사라짐

- 상태: 확인 후 교체하는 의도된 동작
- 환경: Creator Source Graph로 만든 파생 쇼츠
- 증상: `원본 변경 반영` 승인 후 파생 시퀀스에서 직접 수정한 클립 편집이 원본 기준 결과로 바뀐다.
- 원인: 현재 동기화는 세부 병합이 아니라 저장된 원본 범위를 다시 파생하는 방식이다.
- 해결: 확인 대화상자에서 교체 범위를 확인하고 필요한 경우 파생 시퀀스의 별도 프로젝트 버전을 저장한 뒤 반영한다. 세부 병합은 후속 개발 대상이다.
- 재확인: 원본 수정→업데이트 표시→취소 시 보존→승인 시 재생성 순서를 확인한다.

## 2026-08-09 — 4K 출력이 시작되지 않거나 장시간 매우 느림

- 상태: 장비 의존, 사용자 검증 필요
- 환경: 2160급 H.264/H.265 WebCodecs 출력
- 증상: 인코더 미지원 오류, GPU 메모리 부족 또는 낮은 렌더 속도가 발생한다.
- 원인: 4K 캔버스 합성과 인코딩은 GPU·WebView의 최대 해상도, 선택 코덱과 하드웨어 드라이버에 영향을 받는다.
- 해결: H.264와 하드웨어 우선을 먼저 사용하고 실패하면 1080p 또는 소프트웨어 우선을 비교한다. H.265는 지원 장비에서만 선택한다.
- 재확인: 30초 표본 뒤 10분 이상 프로젝트 순서로 파일 재생, 프레임 수, 오디오 싱크와 메모리를 확인한다.

## 2026-08-09 — 자동 저장 이력이 더 이상 늘지 않음

- 상태: 오류 복구 처리 완료, 용량 정리 필요 가능
- 환경: 브라우저/WebView 로컬 저장소에 큰 프로젝트 이력이 누적된 경우
- 증상: 편집은 계속되지만 저장 상태에 자동 저장 실패가 표시된다.
- 원인: 로컬 저장소 할당량 초과 또는 직렬화할 수 없는 데이터가 포함됐다.
- 해결: 프로젝트 파일을 직접 저장한 뒤 오래된 자동 저장 이력과 불필요한 프로젝트 자산·프록시를 정리한다. 자동 저장 실패는 편집 화면을 중단시키지 않는다.
- 재확인: 30초 이상 간격으로 수정하고 자동 저장 기록 창에서 새 체크포인트가 생기는지 확인한다.

## 2026-08-09 — 속도 램프 구간의 재생·오디오·교환 결과가 예상과 다름

- 상태: 소스 구현 완료, 사용자 장비 검증 필요
- 환경: 속도 지점이 2개 이상인 영상·오디오 클립, 역재생 또는 FCPXML/EDL 교환
- 증상: 고배속 구간의 미리보기가 끊기거나 오디오 음정이 변하고, EDL을 다른 편집기에서 열면 속도 곡선이 자동 복원되지 않는다.
- 원인: 미리보기 디코딩 성능은 코덱과 WebView에 좌우된다. 현재 `속도 변경 시 음정 유지`가 켜지면 40ms grain·10ms hop과 ±6ms 상관 정렬을 사용하고, 꺼지면 영상과 같은 sample-time 재표본화 때문에 음정도 변한다. CMX 3600 EDL은 가변 속도 곡선이나 음정 유지 설정을 표현하지 못한다.
- 해결: 음정을 유지하려면 인스펙터 토글을 켜고 편집 프록시를 사용한다. 지나치게 촘촘한 속도 지점과 25% 미만·400% 초과는 grain 질감이 두드러질 수 있다. 가변 속도 교환은 FCPXML을 사용하며 EDL은 `EDITWEAVE SPEED RAMP` 주석을 보고 대상 편집기에서 곡선을 재구성한다.
- 재확인: 25%→100%→400% 클립을 정방향·역방향으로 토글 on/off 재생하고, 대사 기본음과 음악 음정, transient, 분할·트림·2/10초 렌더 청크 경계와 MP4 A/V 동기를 비교한다. FCPXML과 EDL은 각각 시간 맵과 주석 보존 여부를 확인한다.

## 2026-08-09 — 장면 전환 감지가 컷을 놓치거나 플래시를 컷으로 판단함

- 상태: 로컬 하드 컷 감지 구현 완료, 소스별 사용자 검증 필요
- 환경: 빠른 플래시, 디졸브, 매우 긴 클립, 저조도·노이즈 영상
- 증상: 완만한 디졸브가 목록에 없거나 카메라 플래시·큰 움직임이 장면 전환 후보로 표시된다.
- 원인: 현재 감지는 축소 프레임의 휘도·히스토그램 차이와 적응형 임계값을 사용하는 하드 컷 중심 분석이다. 긴 클립은 최대 표본 수에 맞춰 간격이 넓어지고 완만한 전환은 점수가 분산된다.
- 해결: 잘못 감지된 후보는 검토 창에서 선택 해제한다. 긴 원본은 먼저 필요한 구간으로 트림하거나 프록시를 사용해 구간별로 분석한다. 감지 결과를 먼저 마커로 적용하면 원본 클립을 바꾸지 않고 확인할 수 있다.
- 재확인: 토킹헤드 하드 컷, B-roll 몽타주, 플래시, 디졸브 표본에서 후보 수·타임코드 오차·취소·마커·분할·실행 취소를 각각 확인한다.

## 2026-08-09 — 다각형 일반 물체 추적 마스크가 다른 물체로 이동함

- 상태: 이동 중심 템플릿 추적 구현 완료, 복잡한 변형 고도화 필요
- 환경: 빠른 이동, 모션 블러, 가림, 회전·크기·원근 변화가 큰 물체
- 증상: 마스크가 물체를 놓치거나 비슷한 밝기의 주변 물체로 이동한다.
- 원인: 현재 일반 물체 추적은 첫 다각형 경계의 축소 휘도 템플릿을 이전 위치 주변에서 찾고 천천히 갱신한다. 회전·스케일·원근 모델은 아직 사용하지 않는다.
- 해결: 물체를 여백 적게 감싸는 다각형을 만들고, 움직임이 급격히 바뀌거나 가려지는 지점에서 클립을 나눠 구간별로 추적한다. 편집 프록시가 원본과 같은 화면비인지 확인한다. 결과가 벗어난 키프레임은 목록에서 삭제하고 해당 지점부터 다시 지정한다.
- 재확인: 대비가 뚜렷한 평행 이동 물체에서 먼저 확인한 뒤, 느린 크기 변화·부분 가림·역재생 순서로 평균 신뢰도와 마스크 중심 오차를 기록한다.

## 2026-08-09 — 오디오 게인·팬 키프레임 사이에서 음량이나 위치가 급변함

- 상태: 클립 자동화 소스 구현 완료, 사용자 청취 검증 필요
- 환경: 서로 다른 게인·팬 값과 보간 방식이 인접한 오디오/영상 클립
- 증상: 짧은 구간에서 음량 또는 스테레오 위치가 예상보다 빠르게 변하거나 분할 경계에서 다르게 들린다.
- 원인: 키프레임 간격이 매우 짧거나 다음 키프레임의 가속/감속 보간이 큰 값 차이에 적용된 경우다. 팬은 -100~100, 게인은 -60~24dB 범위를 보간한다.
- 해결: 시작·끝 값을 유지할 지점에 같은 값을 한 번 더 기록하고 짧은 구간은 선형을 사용한다. 분할 뒤에는 왼쪽 끝과 오른쪽 시작 키프레임 값이 같은지 확인한다. 트랙 음량·팬은 클립 자동화에 추가로 합산된다.
- 재확인: 0→-12→0dB와 -100→0→100 팬 자동화를 미리보기와 MP4에서 비교하고, 중간 지점 분할·실행 취소·쇼츠 파생 뒤 경계 값을 확인한다.

## 2026-08-09 — 트랙 믹서 페이더가 `AUTO`로 표시되어 움직이지 않음

- 상태: 트랙 절대 시간 자동화 구현 완료, 사용자 청취 검증 필요
- 환경: 트랙 속성에서 음량·팬 키프레임을 하나 이상 기록한 오디오/비디오 트랙
- 증상: 오디오 믹서의 해당 트랙 페이더가 비활성화되고 수치 대신 `AUTO`가 표시된다.
- 원인: 시간 위치 없이 믹서 페이더를 움직이면 어떤 키프레임을 수정할지 모호하므로, 자동화가 있는 트랙은 재생 헤드가 보이는 트랙 인스펙터에서만 값을 쓰도록 제한했다.
- 해결: 타임라인에서 트랙 헤더를 선택하고 인스펙터의 `믹서 · 자동화`에서 재생 헤드를 원하는 시점에 놓아 음량·팬을 수정한다. 자동화를 없애려면 키프레임을 모두 삭제한다. `-14 LUFS 맞춤`은 AUTO 트랙의 모든 음량 키프레임을 같은 비율로 조정한다.
- 시간 규칙: 트랙 자동화는 클립이 아니라 타임라인 절대 시간에 고정된다. 클립 이동 후 자동화도 따라가게 하려면 해당 키프레임 시간을 직접 옮겨야 하며, 자동화 팔로우 옵션은 후속 고도화 항목이다.

## 2026-08-09 — RGB 곡선을 켜면 4K 미리보기·출력이 느려짐

- 상태: 정확도 우선 픽셀 LUT 구현 완료, GPU 셰이더 고도화 필요 가능
- 환경: Master/R/G/B 곡선이 기본 직선이 아닌 고해상도 영상, 여러 레이어 또는 조정 레이어
- 증상: 곡선을 사용하지 않을 때보다 미리보기 프레임과 4K 출력 속도가 낮아진다.
- 원인: 현재 곡선은 실제 픽셀 일치를 위해 프레임의 RGBA 데이터를 읽고 256단계 LUT를 적용한 뒤 다시 기록한다. 4K는 프레임당 약 830만 픽셀을 처리한다.
- 해결: 편집 중에는 프록시를 사용하고 곡선이 필요 없는 채널은 초기화한다. 여러 클립에 같은 곡선을 반복하기보다 가능한 경우 하나의 조정 레이어에 적용한다. 기본 직선 네 채널은 자동으로 빠른 경로를 사용한다.
- 재확인: 동일 30초 구간에서 곡선 없음, Master만, R/G/B 모두, 조정 레이어 순으로 미리보기와 MP4 색 일치·처리 시간을 비교한다.

## 2026-08-09 — HSL 선택 색 보정이 피부나 배경까지 함께 바꿈

- 상태: HSL Qualifier 픽셀 처리와 선택 매트 미리보기 구현 완료
- 환경: 선택 색과 비슷한 색상이 피부·배경·조명에 함께 존재하는 영상
- 증상: 특정 옷이나 제품 색을 바꾸려 했지만 비슷한 피부색·배경색도 함께 변한다.
- 원인: Qualifier는 색상뿐 아니라 채도·명도 범위를 함께 만족하는 픽셀을 선택한다. 색상 범위와 부드러움이 넓거나 채도/명도 밴드가 전체 범위이면 비슷한 픽셀이 포함된다.
- 해결: `선택 매트 보기`를 켜 흰색으로 표시되는 포함 영역을 확인한다. 색상 범위를 줄인 뒤 채도 최소와 명도 최소·최대를 좁히고, 경계가 거칠 때만 부드러움을 올린다. 공간적으로 같은 색을 분리해야 하면 다각형 마스크와 함께 사용한다. 선택 매트는 최종 출력에 포함되지 않는다.
- 재확인: 원색 제품, 피부 근처의 주황색, 저채도 배경에서 선택 노출·채도·색 이동을 각각 적용해 미리보기와 MP4의 포함 영역을 비교한다.

## 2026-08-09 — 저장한 출력 프리셋이 목록에서 사라짐

- 상태: 브라우저/WebView 로컬 저장 구현 완료
- 환경: 앱 저장소 초기화, WebView 데이터 삭제 또는 다른 Windows/macOS 장비
- 증상: 사용자가 저장한 출력 프리셋이 다시 열었을 때 없거나 다른 장비에 나타나지 않는다.
- 원인: 사용자 출력 프리셋은 현재 장비의 EditWeave 로컬 저장소에만 보존되며 프로젝트 파일이나 클라우드로 동기화되지 않는다.
- 해결: 앱 데이터 정리 전에 프리셋 값을 기록하고 다른 장비에서는 같은 설정을 다시 저장한다. 프로젝트 포함·내보내기 기능은 후속 범위다. 기본 YouTube/Shorts/4K 프리셋은 항상 다시 제공된다.
- 재확인: 사용자 프리셋 저장→앱 재열기→적용→삭제와 기본 프리셋 복귀를 순서대로 확인한다.

## 2026-08-09 — 보이스오버 마이크가 연결되지 않거나 다시 열면 녹음 원본이 오프라인임

- 상태: 로컬 녹음·데스크톱 영속 저장 구현 완료, 사용자 장비 검증 필요
- 환경: Windows/macOS 마이크 권한, WebView `MediaRecorder`, 브라우저 개발 서버 또는 데스크톱 앱
- 증상: 장치 목록에 마이크가 없고 입력 레벨이 움직이지 않거나, 브라우저에서 녹음한 프로젝트를 새로 열면 녹음 자산이 오프라인으로 표시된다.
- 원인: 운영체제에서 EditWeave/WebView의 마이크 권한이 차단됐거나 현재 WebView가 지원되는 오디오 녹음 인코더를 제공하지 않을 수 있다. 브라우저 개발 서버는 파일 시스템 영속 경로를 만들 수 없어 녹음이 현재 세션에만 유지된다.
- 해결: 운영체제 개인정보 보호 설정에서 앱의 마이크 접근을 허용한 뒤 `마이크 연결`을 다시 누른다. 영속 프로젝트 작업은 `run.bat` 또는 설치된 데스크톱 앱에서 진행한다. 데스크톱 녹음은 앱 데이터의 프로젝트별 `recordings` 폴더에 저장되고 프로젝트 재열기 때 자동 연결된다.
- 재확인: 장치 선택→레벨 확인→3초 카운트다운→일시정지/재개→완료·배치→프로젝트 저장→앱 재열기 순서로 대화 역할과 원본 연결을 확인한다.
- 다중 테이크: 각 `테이크 완료` 뒤 미리듣기 플레이어가 생긴다. 다음 녹음 시작 시 기존 미리듣기는 자동 정지되며, `선택 테이크 배치`를 누르기 전 삭제한 테이크와 채택하지 않은 테이크는 프로젝트 파일로 보존하지 않는다.

## 2026-08-09 — 카메라 파일의 시작 타임코드·릴이 자동으로 표시되지 않음

- 상태: FFprobe 컨테이너 메타데이터 판독 구현 완료, 포맷별 사용자 검증 필요
- 환경: 데스크톱 앱, MOV/MXF/MP4 등 타임코드 태그가 있는 원본, 시스템 PATH의 FFprobe
- 증상: 미디어를 가져온 뒤 `소스 시작 TC`에 `컨테이너` 표시가 없거나 릴 메타데이터가 비어 있다.
- 원인: FFprobe가 설치되지 않았거나 컨테이너가 표준 `timecode`, `reel_name`, `reel` 태그를 기록하지 않은 경우다. 브라우저 개발 서버는 로컬 실행 파일을 호출하지 않는다.
- 해결: FFmpeg/FFprobe를 PATH에 설치한 데스크톱 앱에서 원본을 다시 가져온다. 메타데이터가 없는 파일은 `HH:MM:SS:FF`를 직접 입력하며 29.97/59.94 드롭프레임은 `HH:MM:SS;FF`로 입력한다. 수동 값은 이후 재분석에서도 우선 보존된다.
- 재확인: NDF와 DF 카메라 원본을 각각 가져와 컨테이너 표시, 세미콜론 보존, 릴 이름, TC 동기화 결과를 확인한다.

## 2026-08-09 — 프록시를 켜면 카메라 타임코드 기준 컷이 어긋날 수 있음

- 상태: FFmpeg 프록시 메타데이터 전달·반 프레임 일치 검사 구현 완료, 사용자 원본 검증 필요
- 환경: 컨테이너 시작 TC가 있는 원본과 FFmpeg 기반 디스크 프록시
- 증상: 프록시 카드 또는 미디어 상태에 `TC 불일치`가 표시된다.
- 원인: 원본의 타임코드 태그가 프록시 컨테이너에 복사되지 않았거나 프레임레이트 변환 과정에서 시작 TC가 반 프레임보다 크게 달라졌다.
- 해결: 해당 자산은 `원본 보기`로 전환한 뒤 프록시를 삭제·재생성한다. 경고가 유지되면 수동 소스 TC를 원본 기준으로 확인하고 그 프록시를 TC 동기화 편집에 사용하지 않는다. 최종 출력은 항상 원본을 사용한다.
- 판정: FFprobe로 원본과 완성 프록시의 TC를 모두 읽은 경우에만 `TC 일치 확인` 또는 `TC 불일치`를 표시한다. 브라우저·Mediabunny 세션 프록시는 프로젝트의 논리 TC를 공유하지만 컨테이너 태그 검증은 하지 않는다.

## 2026-08-09 — 변형 값 그래프의 점이 위·아래 경계에 붙음

- 상태: 변형 값 그래프 1차 구현 완료, 자유 베지어 핸들은 후속
- 환경: 위치 ±1000, 크기 1~400%, 회전 ±360°, 불투명도 0~100% 범위를 넘는 키프레임
- 증상: 숫자 입력값은 유지되지만 그래프 점과 곡선이 위 또는 아래 경계에 붙어 보인다.
- 원인: 속성별 그래프는 실무에서 자주 쓰는 고정 표시 범위로 정규화하며 범위 밖 값은 화면 경계에 클램프한다. 실제 키프레임 값과 미리보기 계산은 변경하지 않는다.
- 해결: 경계 밖의 정밀 값은 숫자 필드로 입력한다. 그래프에서는 점을 끌어 시간과 표시 범위 안 값을 빠르게 조절하고, 보간 방식은 아래 키프레임 목록에서 선택한다.

## 2026-08-09 — MP4 출력은 완료됐지만 러드니스 실측이 실패함

- 상태: 완성 파일 EBU R128 실측 구현 완료, 사용자 출력 파일 검증 필요
- 환경: 데스크톱 단일/쇼츠 일괄 MP4 출력, 오디오 포함, 시스템 PATH의 FFmpeg
- 증상: 렌더 작업은 `완료`지만 `러드니스 실측 일부 실패`가 표시되거나 LUFS·LRA·True Peak 배지가 없다.
- 원인: FFmpeg가 없거나 완성 파일에 판독 가능한 오디오 스트림이 없거나, 출력 직후 파일을 다른 프로그램이 잠근 경우다. 영상 저장 성공과 후속 품질 측정은 의도적으로 별도 단계다.
- 해결: FFmpeg를 PATH에 설치하고 오디오 포함 출력을 선택한 뒤 다시 렌더한다. 무음 납품이면 실측 배지가 없는 것이 정상이며, 이미 저장된 MP4는 측정 실패 때문에 삭제되지 않는다.
- 판정: 미리보기 Delivery Guard와 믹서의 값은 분석 파형을 사용한 빠른 예상치다. 렌더 작업의 `실측` 배지만 완성 MP4를 EBU R128/ITU-R BS.1770 방식으로 다시 읽은 결과다.
- 재확인: -14 LUFS 기준 음원, 무음 파일, 클리핑 파일, 쇼츠 2개 이상을 출력해 통합 LUFS·LRA·True Peak와 실패 격리를 확인한다.

## 2026-08-09 — Aux 프리 센드가 클립 페이더를 내려도 계속 들림

- 상태: 최대 4개 프리/포스트 Aux 라우팅 구현 완료, 사용자 청취 검증 필요
- 환경: 클립 또는 트랙 게인이 내려간 오디오와 활성 역할 버스 Aux 센드
- 증상: 클립 게인이나 트랙 음량을 내려도 `프리` 센드로 보낸 신호가 대상 버스에서 계속 들린다.
- 원인: 프리 페이더는 의도적으로 클립·트랙 페이더 전의 EQ·Dynamics 처리 신호를 분기한다. `포스트` 센드는 메인 페이더·팬 결과를 따른다.
- 해결: 메인 페이더를 따라야 하는 리버브·딜레이 계열은 `포스트`를 선택한다. 독립 헤드폰 믹스·병렬 처리처럼 페이더와 무관해야 하는 경우만 `프리`를 사용한다. 같은 역할 버스로 보내는 센드는 이중 합산을 막기 위해 무시된다.
- 미리보기: 저지연 Web Audio에서도 메인과 각 프리/포스트 센드를 별도 버스 체인으로 분기한다. 프리 센드는 클립·트랙 페이더와 팬 앞에서 갈라지며, 최종 MP4는 모든 클립 신호를 역할 버퍼에 합산한 뒤 버스 체인을 처리한다.

## 2026-08-09 — 버스 삽입 효과 순서를 바꾼 뒤 음색·피크가 달라짐

- 상태: 역할 버스 6슬롯 순차 삽입 체인 구현 완료, 사용자 청취·출력 비교 필요
- 환경: 오디오 믹서의 대화·음악·효과음·환경음 버스, 하이패스/EQ/컴프레서/리미터를 둘 이상 사용
- 증상: 같은 효과와 값인데도 위·아래 순서를 바꾸면 음색이나 압축량, 피크가 달라진다.
- 원인: 삽입 체인은 위에서 아래로 직렬 처리한다. 예를 들어 EQ 부스트 뒤 컴프레서는 부스트된 대역에 더 강하게 반응하며, 리미터 뒤 메이크업 게인은 다시 피크를 올릴 수 있다.
- 해결: 정리용 하이패스·EQ → 다이내믹 제어용 컴프레서 → 마지막 리미터 순서를 기본으로 사용한다. 각 슬롯 번호 버튼으로 우회 비교하고 화살표로 순서를 바꾼다. 별도의 `출력 안전 리미터`는 삽입 체인과 버스 게인 뒤에 항상 적용된다.
- 판정: 믹서 LUFS/True Peak는 빠른 파형 예상치이므로 비선형 버스 효과의 최종 판정에는 렌더 작업에 기록되는 완성 MP4 EBU R128 실측값을 사용한다.

## 2026-08-09 — ADR 테이크를 바꿨는데 여러 녹음이 동시에 들리거나 원본이 사라짐

- 상태: 영속 대본 큐·테이크 레인·채택 전환·공유 원본 보존·Delivery Guard 무결성 검사 구현 완료, 사용자 세션 검증 필요
- 환경: 같은 ADR 큐에서 두 개 이상 녹음한 데스크톱 프로젝트
- 증상: 타임라인에 여러 ADR 클립이 같은 시각에 겹쳐 보이거나, 큐 삭제 뒤 녹음 미디어가 함께 사라진다.
- 원인: 각 녹음은 비교와 재채택을 위해 별도 오디오 레인에 의도적으로 보존된다. 초록색 `채택` 레인만 출력되고 흐린 `대기` 레인은 `audioDisabled` 상태다. 큐 삭제는 해당 큐 전용 타임라인 클립과 녹음 원본을 함께 삭제하는 복구 불가 작업이지만, 다른 클립이나 ADR 큐에서 같은 asset ID를 재사용 중인 원본은 삭제하지 않는다.
- 해결: ADR 창의 대본 큐에서 원하는 T 번호를 눌러 채택한다. 세션 참조를 보호하기 위해 ADR 테이크의 개별 삭제·분할·이동·트림은 일반 타임라인에서 제한되며, ADR 원본 정리는 큐 목록의 삭제 버튼을 사용한다. 확인창의 `전용 원본 N개 삭제 / 공유 원본 N개 보존` 수를 확인한다. 일반 미디어 패널에서 ADR 자산을 제거하려면 해당 자산을 쓰는 모든 클립과 ADR 큐를 먼저 정리해야 한다.
- 납품 차단: Delivery Guard의 `ADR 녹음 원본 사용 불가`, `ADR 테이크 레인 누락`, `ADR 컴프 참조 손상`은 현재 출력 시퀀스 또는 실제 nested 의존 시퀀스의 큐가 손상됐다는 뜻이다. 원본을 다시 연결하거나 해당 큐의 트랙·테이크를 복원한다. 단순 보관용 병합 감사 분기의 ADR은 현재 출력 검사에서 제외된다.
- 재확인: 3개 테이크 저장→각 T 번호 채택 전환→프로젝트 저장·재열기→선택 테이크만 미리보기/출력→확인 후 큐 삭제 순서로 검사한다.

## 2026-08-09 — ADR 부분 채택 뒤 테이크가 중간에 바뀌거나 무음이 생김

- 상태: 구간별 비파괴 ADR 컴핑 구현 완료, 사용자 청취 검증 필요
- 환경: 같은 큐의 여러 ADR 테이크 중 짧은 구간만 인스펙터에서 채택
- 증상: 청록색 채택 오버레이 경계에서 테이크가 바뀌며, 선택한 테이크 녹음 길이보다 뒤 구간에서는 무음이 날 수 있다.
- 원인: 컴프 인·아웃은 프로젝트 절대 시간이며 선택 테이크 원본이 그 구간까지 녹음되지 않았다면 재생할 샘플이 없다. EditWeave은 경계 전후 12ms 등가 크로스페이드를 적용하지만, 두 테이크의 배경음·위상 차이가 크면 전환감이 남을 수 있다.
- 해결: 타임라인 재생 헤드를 큐 안에 놓고 `재생 헤드 → 인/아웃`으로 유효 범위를 좁힌 뒤 채택한다. 전환감이 있으면 컴프 경계를 무음·호흡 구간으로 옮긴다.
- 판정: 마이크 창의 T 번호 채택은 큐 전체 컴프를 해당 테이크 하나로 재설정한다. 부분 채택은 ADR 테이크 클립을 선택한 뒤 인스펙터에서 수행하며, 구간 칩과 타임라인 청록 오버레이가 저장 후에도 동일해야 한다.

## 2026-08-09 — 다중 마스크를 추가했더니 단일 마스크 설정이 사라짐

- 상태: 기존 단일 마스크 호환과 8레이어 다중 마스크 합성 구현 완료, 사용자 화면·출력 비교 필요
- 환경: 기존 타원·사각형·다각형 단일 마스크가 있는 클립에서 다중 마스크 편집 시작
- 증상: `빠른 단일 마스크`가 `없음`으로 바뀌고 새 마스크 레이어 목록에 `기본 마스크`가 나타난다.
- 원인: 첫 다중 마스크 편집 시 기존 단일 마스크를 동일한 모양·점·페더·반전의 첫 레이어로 비파괴 승격한다. 이후에는 서로 충돌하는 두 데이터 구조 대신 다중 마스크 랙이 기준이 된다.
- 해결: 첫 레이어가 기존 모양을 유지하는지 확인하고 다음 레이어의 결합을 `더하기`, `빼기`, `교차` 중 선택한다. 단일 방식으로 되돌리려면 다중 레이어를 모두 삭제한 뒤 `빠른 단일 마스크`를 다시 선택한다.
- 판정: 페더·불투명도·반전이 있는 마스크와 다중 결합은 Canvas 경로를 사용한다. 프로그램 미리보기와 최종 파일의 가장자리·구멍·교차 영역이 같아야 한다.

## 2026-08-09 — 베지어 마스크 곡선이 키프레임 사이에서 갑자기 바뀜

- 상태: 베지어 앵커·핸들 보간 구현 완료, 사용자 애니메이션 검증 필요
- 원인: 부드러운 보간은 두 키프레임의 마스크 ID, 레이어 수, 앵커 수가 같을 때 가능하다. 중간 키프레임에서 점을 추가·삭제하거나 레이어 순서를 바꾸면 구조가 달라져 가까운 키프레임 모양으로 단계 전환한다.
- 해결: 움직임을 기록할 구간에서는 마스크 레이어와 앵커 수를 먼저 확정한 다음 앵커·핸들 위치만 변경한다. 구조 변경이 필요하면 전환 직전에 별도 키프레임을 두어 의도한 컷처럼 만든다.

## 2026-08-09 — 4점 원근 추적 중 마스크가 배경으로 미끄러짐

- 상태: 네 코너 적응형 패치 추적과 쌍선형 마스크 변형 구현 완료, 사용자 원본별 검증 필요
- 환경: 회전·확대·기울기가 있는 물체, 강한 모션 블러·가림·반복 무늬 배경
- 원인: 각 코너는 이전 위치 주변의 밝기 템플릿을 독립 검색하고 6%씩 외형을 갱신한다. 장시간 가림, 큰 프레임 간 이동, 특징이 없는 단색 영역에서는 잘못된 유사 패턴으로 이동할 수 있다.
- 해결: 프록시를 사용하더라도 물체가 충분히 크게 보이는 구간으로 클립을 나누고, 대비가 뚜렷한 테두리를 포함하도록 첫 마스크를 잡는다. 추적 뒤 신뢰도가 낮거나 미끄러진 지점의 시각 효과 키프레임을 삭제·수정한다.
- 한계: 현재 4점 방식은 제한적 원근 변형까지 대응하지만 가림 뒤 객체 재식별은 하지 않는다. 이 항목은 후속 장시간 추적 단계로 남아 있다.

## 2026-08-09 — HDR 원본이 프로그램 모니터나 출력에서 SDR처럼 보임

- 상태: PQ/HLG/BT.2020 메타데이터 판독과 HDR 편집 프록시의 Rec.709 톤매핑 구현 완료, 네이티브 HDR 출력은 후속 단계
- 환경: `smpte2084` PQ 또는 `arib-std-b67` HLG 전송 특성을 가진 영상
- 증상: 미디어 상세 정보에 HDR이 표시되지만 프로그램 모니터와 현재 MP4 출력은 SDR 밝기 범위로 보인다.
- 원인: 현재 합성 캔버스와 H.264/H.265 납품 경로는 Rec.709 SDR 기준이다. 대용량 HDR 프록시는 모니터링 일관성을 위해 FFmpeg zscale·Hable 톤매핑 후 BT.709 태그를 기록한다.
- 해결: 현재 버전에서는 Delivery Guard의 `HDR 원본 · SDR 납품 확인` 경고를 확인하고 하이라이트·피부톤을 SDR 기준으로 검사한다. PQ/HLG 그대로의 마스터가 필요하면 네이티브 HDR 출력 단계가 완료되기 전까지 원본을 외부 HDR 마스터링 도구로 전달한다.
- 한계: HDR 마스터링 디스플레이, MaxCLL/MaxFALL 보존과 10-bit HDR 인코딩은 아직 완료로 표시하지 않는다.

## 2026-08-09 — 색상 노드의 연결이 삭제·재정렬 뒤 바뀌거나 Tone Map 결과가 HDR 파일이 아님

- 상태: 최대 16개 DAG형 색상 노드와 공통 미리보기·출력 처리 구현 완료, 사용자 원본별 화면 비교 필요
- 환경: Primary·Curves·Qualifier·Look/LUT·HDR Tone Map 노드를 둘 이상 연결한 클립·조정 레이어·중첩 시퀀스
- 증상: 노드를 삭제하거나 위·아래로 옮긴 뒤 일부 입력이 이전과 달라지며, Tone Map 노드를 사용해도 완성 MP4가 PQ/HLG 10-bit HDR로 식별되지 않는다.
- 원인: 순환 그래프를 막기 위해 각 노드는 SOURCE와 목록상 자신보다 앞선 노드만 입력으로 선택할 수 있다. 삭제·재정렬로 입력이 뒤쪽 또는 존재하지 않는 노드를 가리키면 가장 가까운 유효한 이전 출력으로 자동 연결한다. Tone Map 노드는 현재 8-bit SDR Canvas 픽셀에 Hable·Reinhard·Mobius 곡선을 적용하는 SDR 표시·납품 처리이며 HDR 컨테이너 메타데이터나 10-bit 신호를 생성하지 않는다.
- 해결: 재정렬 뒤 각 노드의 `입력`과 `PROGRAM OUTPUT`을 확인하고, 병렬 분기가 필요하면 먼저 공통 노드를 둔 뒤 그 아래 노드에서 복수 입력을 선택한다. HDR 마스터가 필요하면 현재 Delivery Guard의 SDR 납품 경고를 따르고 네이티브 HDR 출력 개발이 완료되기 전까지 외부 마스터링 경로를 사용한다.
- 판정: 우회한 노드는 입력을 그대로 통과시키고 Mix 0%도 원본 입력을 반환한다. 클립·조정 레이어·중첩 시퀀스의 프로그램 화면과 SDR 출력 파일에서 노드 순서·합성·출력 선택 결과가 같아야 한다.

## 2026-08-09 — Program Scopes가 재생 중 느리게 갱신되거나 소스 스코프와 값이 다름

- 상태: 최종 합성 Canvas 기반 프로그램 스코프 구현 완료, 사용자 GPU·WebView 성능 검증 필요
- 환경: 복수 영상 트랙, 조정 레이어, 자막, 픽셀 기반 곡선·Qualifier·색상 노드가 활성화된 프로그램 재생
- 증상: Program Scopes의 파형·RGB·히스토그램이 영상 프레임레이트보다 느리게 바뀌거나 기존 Source Scopes와 분포가 다르다.
- 원인: 스코프는 UI 성능을 위해 최대 960px 프로그램 합성 프레임을 재생 중 250ms 간격 이하로 표본화한다. 또한 Source Scopes는 한 원본 프레임만 읽지만 Program Scopes는 위쪽 트랙, 합성 모드, 마스크, 조정 레이어와 자막까지 적용된 최종 픽셀을 읽으므로 값이 달라지는 것이 정상이다.
- 해결: 순간 프레임을 정밀 확인할 때 재생을 멈추고 재생 헤드를 원하는 위치에 둔다. 고비용 노드가 많아 프로그램 재생 자체가 느리면 편집 프록시를 사용하거나 일부 노드를 우회한 뒤 최종 정지 프레임에서 다시 켠다.
- 판정: 정지 상태에서 색상 노드·조정 레이어·자막을 바꾸면 같은 재생 헤드에서도 프레임 지문이 달라져 스코프가 다시 계산되어야 한다. 소스 모니터를 명시적으로 연 경우에만 Source Scopes로 되돌아간다.

## 2026-08-09 — HDR 파일인데 MaxCLL·MaxFALL 또는 Mastering Display가 비어 있음

- 상태: FFprobe stream side data 판독 구현 완료, 카메라·컨테이너별 사용자 검증 필요
- 환경: PQ/HLG/BT.2020 원본, 시스템 PATH의 FFprobe, 데스크톱 앱
- 증상: 색공간에는 PQ 또는 HLG가 표시되지만 `HDR 정적 메타데이터`가 `없음`이거나 일부 값만 보인다.
- 원인: 전송 특성과 색원색 태그는 있어도 원본 스트림에 SMPTE ST 2086 Mastering display 또는 Content light level metadata가 기록되지 않은 파일이 있다. FFprobe가 없거나 오래된 빌드가 stream side data를 노출하지 않는 경우도 같다.
- 해결: 최신 FFmpeg/FFprobe를 PATH에 설치한 데스크톱 앱에서 원본을 다시 가져온다. 값이 계속 없으면 원본 제작 단계에서 메타데이터가 실제로 기록됐는지 확인하며 EditWeave은 없는 MaxCLL·MaxFALL을 추정값으로 채우지 않는다.
- 판정: Delivery Guard는 존재하는 값만 HDR 경고에 표시한다. 메타데이터가 없다고 원본이 SDR이라는 뜻은 아니며 PQ/HLG 판정은 color transfer를 우선 사용한다.

## 2026-08-09 — ComfyUI 워크플로 실행 버튼이 비활성화되거나 동의 오류가 표시됨

- 상태: 외부 프로세스 데이터 동의 게이트 구현 완료, 사용자 ComfyUI 환경 검증 필요
- 환경: 선택 이미지, API 형식 워크플로 JSON, 사용자가 설정한 ComfyUI 주소
- 증상: 이미지와 워크플로가 있어도 실행 버튼이 비활성화되거나 `AI 데이터 설정에서 ... 동의해야` 오류가 표시된다.
- 원인: ComfyUI는 EditWeave의 핵심 로컬 편집 엔진이 아니라 별도 프로세스다. 선택 이미지와 워크플로가 해당 주소로 전송되고 워크플로 노드가 추가 외부 API를 사용할 수도 있으므로 명시 동의를 기본값으로 요구한다.
- 해결: 상단 방패 아이콘의 `AI 데이터 설정`에서 전송 범위를 읽고 허용하거나 ComfyUI 창의 동일 동의 체크박스를 켠다. 동의하지 않으면 일반 편집·로컬 전사·장면 감지·배경 제거·추적은 그대로 사용할 수 있다.
- 철회: 동의 체크를 끄면 이후 ComfyUI 신규 실행이 즉시 차단된다. 이미 ComfyUI 측에 저장된 입력·출력이나 프로젝트로 가져온 결과 이미지는 자동 삭제되지 않으므로 각 위치에서 별도로 정리한다.

## 2026-08-09 — 로컬 전사에서 같은 사람이 여러 화자로 나뉘거나 다른 사람이 합쳐짐

- 상태: acoustic-v1 음성 구간 임베딩·최대 4인 군집 구현 완료, 실제 대화 원본별 사용자 검증 필요
- 환경: 배경음악·잔향·전화 음질·겹쳐 말하기·매우 짧은 한두 단어가 포함된 음성
- 증상: 대본의 `화자 1`~`화자 4`가 실제 인물 경계와 다르거나 신뢰도가 낮다.
- 원인: 현재 방식은 외부 전송 없는 로컬 음색·피치 임베딩이며 사람 신원을 학습한 신경망 재식별 모델은 아니다. 0.65초보다 짧은 구간, 음악과 효과음이 큰 구간, 두 사람이 동시에 말하는 구간은 안정된 음색 표본이 부족하다.
- 해결: 각 구간의 신뢰도 배지를 참고해 화자 이름 입력란을 직접 수정한다. 배경음악이 없는 대화 원본으로 다시 전사하거나 대본 구간을 자연스러운 문장 길이로 유지하면 군집이 더 안정적이다.
- 판정: `acoustic-v1`은 기존 단일 RMS/zero-crossing 임계값보다 실제 구간 전체의 음향 유사도를 사용하지만 생체 신원 판별 기능은 아니다. 장시간 가림 뒤와 같은 화자 재식별 수준은 후속 로컬 신경망 모델 범위로 남긴다.

## 2026-08-09 — AI 초벌 후보가 이전보다 적거나 점수가 달라짐

- 상태: 프로젝트별 creator-feedback-v1 적용/유지 학습 구현 완료, 사용자 편집 패턴 검증 필요
- 환경: 같은 프로젝트에서 침묵·군더더기·반복 후보를 여러 번 `제거 적용` 또는 `유지`한 뒤 재분석
- 증상: 동일한 원본인데도 특정 유형의 후보가 덜 보이거나 카드 점수·이유가 달라진다.
- 원인: 피드백 3건부터 유형별 수용률에 따라 후보 점수와 표시 임계값을 조절한다. 자주 유지한 유형은 더 확실한 후보만 보이고 자주 제거한 유형은 더 넓게 제안한다.
- 해결: AI 패널 상단에서 반영된 피드백 건수를 확인한다. 의도하지 않은 선택이 누적됐거나 다른 채널 편집 규칙으로 전환할 때 `학습 초기화`를 눌러 프로젝트 프로필을 기본값으로 되돌린다.
- 범위: 유형 수용률, 가져온 유지율 표본, 하이라이트 의미 중심은 현재 프로젝트 프로필에 저장된다. 여러 프로젝트·채널 사이의 팀 프로필 공유는 별도 기능 범위다.
- 하이라이트: 하이라이트 카드는 삭제 후보가 아니라 챕터·쇼츠 검토 후보다. 적용하면 시작점에 노란 챕터 마커가 생기며 원본 컷은 바뀌지 않는다. 질문·수치·핵심어 특징뿐 아니라 로컬 의미 임베딩, 가져온 유지율, 이전 하이라이트 적용/유지 의미 중심을 함께 사용한다.

## 2026-08-09 — 검토 패키지를 병합했는데 코멘트가 추가되지 않거나 다른 시간에 보임

- 상태: editweave-review-v1 오프라인 코멘트 병합 구현 완료, 편집자 간 왕복 검증 필요
- 환경: `.editweave-review.json`을 다른 편집자 또는 다른 프로젝트에서 가져오기
- 증상: 병합 결과가 0개 추가로 표시되거나, 다른 프로젝트 패키지의 코멘트가 현재 영상과 맞지 않는 위치에 보인다.
- 원인: 동일 ID 코멘트는 새 항목이 아니라 갱신으로 처리하며 ID가 달라도 시간(0.001초 단위)·작성자·내용·상태가 모두 같으면 중복으로 건너뛴다. 다른 프로젝트나 편집 길이가 달라진 시퀀스에는 같은 절대 시간이 같은 장면을 뜻하지 않을 수 있다.
- 해결: 다른 프로젝트 경고가 나오면 영상 버전과 러닝타임을 먼저 확인한다. 같은 프로젝트의 왕복 검토에는 CSV가 아니라 검토 패키지를 사용해 ID와 해결 상태를 보존한다. 잘못 병합한 경우 실행 취소로 직전 마커 병합을 되돌린다.
- 범위: 이 패키지는 코멘트만 병합한다. 클립·효과·미디어의 전체 프로젝트 버전 병합과 온라인 동시 편집은 서버·충돌 정책이 필요한 후속 범위다.

## 2026-08-09 — 업데이트 확인에서 HTTPS·SHA-256·서명 오류가 표시됨

- 상태: 매니페스트 생성·서명, 공개 키 catalog/실물 SHA 검증 업데이트 서버 구현 완료, 운영 도메인·실제 키·코드 서명 연결 필요
- 환경: `VITE_EDITWEAVE_UPDATE_MANIFEST`, `VITE_EDITWEAVE_UPDATE_PUBLIC_KEY`, `VITE_EDITWEAVE_UPDATE_KEY_ID`가 설정된 배포 빌드
- 증상: 업데이트 확인 시 `HTTPS여야 합니다`, `SHA-256 형식이 올바르지 않습니다`, `서명이 없습니다/올바르지 않습니다`, `공개 키가 설정되지 않았습니다` 오류가 표시된다.
- 원인: 운영 도메인의 HTTP, 64KB 초과 JSON, 리다이렉트, 자격 증명 포함 URL과 localhost 다운로드는 차단한다. `sha256`은 정확히 64자리 16진수여야 하며 `editweave-update-v1`의 고정 필드 payload를 빌드에 포함된 32-byte Ed25519 공개 키와 일치하는 개인 키로 서명해야 한다.
- 해결: `release/update-manifest.example.json`을 복사해 실제 HTTPS 설치 파일 URL과 SHA-256으로 교체한 뒤 `node release/sign-update-manifest.mjs <manifest.json> <개인키.pem>`으로 서명한다. 출력된 keyId·공개 키를 Vite 환경에 넣고 같은 공개 키를 Cargo의 `EDITWEAVE_UPDATE_PUBLIC_KEY`에도 넣어 앱을 빌드한 뒤 같은 JSON을 게시한다. 로컬 개발은 localhost 또는 127.0.0.1에서만 무서명 매니페스트 확인을 허용하지만 앱 내부 직접 다운로드는 서명이 있어야 한다.
- 운영 서버: `services/update-server/README.md` 순서대로 플랫폼 설치 파일을 `release/update-channel/artifacts`에 두고 `release/create-update-manifest.mjs`로 `/manifests/<channel>/<platform>.json`을 만든 뒤 오프라인 개인 키로 서명한다. 서버는 공개 키·keyId·public origin을 설정해야 하며 시작 시 모든 서명과 실제 파일 SHA를 확인한다. catalog 하나가 틀리면 시작 또는 reload를 거부하고, 서비스 중 파일이 바뀌면 503 `catalog_reload_required`를 반환한다.
- 다운로드 판정: 데스크톱은 사용자가 경로를 선택한 뒤 최대 5회까지 HTTPS 리다이렉트를 허용하고 2GB를 넘으면 중단한다. 수신 중에는 PID별 `.part`만 쓰며 SHA-256 불일치·빈 파일·쓰기 실패 시 임시 파일을 삭제한다. 일치한 파일만 최종 경로로 옮기고 30분·1회용 토큰으로 등록하며, 실행 직전에 해시를 다시 계산한다. EXE/MSI는 Windows, DMG/PKG는 macOS에서만 허용한다.
- OS 서명 판정: Windows는 Authenticode 상태가 `Valid`이고 인증서 Subject가 `EDITWEAVE_UPDATE_SIGNER_SUBJECT`를 포함해야 한다. macOS DMG는 `codesign --verify`와 Gatekeeper open assessment, PKG는 `pkgutil --check-signature`와 Gatekeeper install assessment를 통과하고 출력에 `EDITWEAVE_UPDATE_APPLE_TEAM_ID`가 있어야 한다. 다운로드 직후와 실행 직전에 모두 검사하며 실패한 다운로드 파일은 삭제하고 실행 토큰을 발급하지 않는다.
- 적용 결과: 설치 실행 전에 목표/이전 버전, canonical payload, 서명과 파일 경로를 로컬에 기록한다. 다른 앱 세션에서 목표 버전 이상이면 성공으로 제거하고, 실행 10분 뒤에도 이전 버전이면 `적용되지 않았습니다`를 표시한다. 검증 기록은 7일 뒤 만료된다. 같은 업데이트를 다시 확인하면 기존 파일의 Ed25519 payload·플랫폼·SHA-256·OS 서명을 Rust에서 처음부터 재검증해 새 30분·1회 토큰을 발급하며, 파일이 없거나 달라졌으면 기록을 지우고 새 다운로드를 제안한다.
- 한계: 현재는 검증된 설치 파일을 사용자 확인 후 여는 단계다. 실행 중 앱의 무인 교체·실패 롤백은 Tauri updater `.sig`, Windows/macOS 코드 서명·공증과 운영 서버가 준비된 후에만 활성화한다.

## 2026-08-09 — 팀 오디오 템플릿을 가져오지 못하거나 ADR 기본값이 예상과 다름

- 상태: 역할 버스·삽입 체인·ADR 기본값의 로컬 템플릿과 JSON 공유 구현 완료, 사용자 장치명별 검증 필요
- 환경: 오디오 믹서의 `.editweave-audio.json` 저장·가져오기, 다른 컴퓨터의 마이크 장치
- 증상: 가져오기 뒤 오류 문구가 보이거나, 템플릿을 적용해도 다른 컴퓨터에서 기대한 마이크가 자동 선택되지 않는다.
- 원인: EditWeave 오디오 템플릿 버전·ID·이름·버스 데이터가 없는 일반 JSON은 거부한다. 마이크는 운영체제가 노출한 장치 이름이 템플릿의 선호 이름과 정확히 같을 때만 우선 선택하며, 다른 장치명·권한 차이가 있으면 기본 입력으로 대체한다.
- 해결: 오디오 믹서의 `공유` 버튼으로 만든 파일을 사용한다. 다른 컴퓨터에서는 ADR 기본값의 선호 마이크 이름을 해당 운영체제에 표시된 이름으로 바꿔 다시 저장하거나 ADR 창에서 직접 장치를 선택한다.
- 복구: 템플릿 적용은 현재 시퀀스의 역할 버스를 편집 기록 한 건으로 바꾸므로 즉시 Undo할 수 있다. 템플릿 라이브러리 삭제는 프로젝트 버스 설정이나 이미 녹음한 ADR 원본을 삭제하지 않는다.

## 2026-08-09 — 자유 속도 곡선이나 속도 템플릿 적용 뒤 지점 시간이 달라짐

- 상태: cubic Bézier 자유 속도 곡선과 `editweave-speed-template-v1` 공유 구현 완료, 사용자 원본별 재생 감각 검증 필요
- 환경: 길이가 다른 클립 사이의 `.editweave-speed.json` 적용, 5%~1600% 속도 램프
- 증상: 템플릿 원본과 대상 클립에서 키프레임의 절대 초 위치가 다르거나 자유 곡선의 체감 가속이 예상과 다르다.
- 원인: 속도 템플릿은 컷 길이가 달라도 같은 연출 비율을 유지하기 위해 각 지점 시간을 `대상 길이/원본 길이` 비율로 배치한다. 자유 곡선 제어점은 화면 좌표가 아니라 0~1로 정규화된 구간 시간·속도 진행률이며 구간 시작과 끝의 실제 속도 차이에 따라 체감이 달라진다.
- 해결: 대상 클립에서 큰 속도 그래프의 지점을 먼저 원하는 시간으로 옮기고, 해당 지점의 `자유 곡선` 편집기에서 두 제어점을 조절한다. 절대 초 위치가 반드시 같아야 하면 템플릿 적용 후 시간 값을 직접 맞춘다.
- 판정: 속도 그래프 선, 프로그램 재생, 오디오 위치와 최종 출력의 장면 시점이 같아야 한다. 트림·분할은 원래 곡선을 선형 표본으로 다시 만들어 경계의 소스 프레임 연속성을 우선 보존한다.

## 2026-08-09 — 속도 램프가 있는 중첩 시퀀스의 자식 프레임 또는 레이어 순서가 예상과 다름

- 상태: 부모/자식 적분형 시간 재매핑과 사용자 합성 우선순위 구현 완료, 복합 원본 사용자 검증 필요
- 환경: 부모와 자식 양쪽에 속도 램프·역재생이 있고 여러 영상 트랙·ADR 컴프가 겹치는 중첩 시퀀스
- 증상: 중첩 경계에서 한 프레임이 달라 보이거나, 같은 시간에 겹친 중첩 영상이 다른 트랙 위/아래에 나타난다.
- 원인: 중첩된 두 속도 곡선은 출력 구간당 최대 120개 선형 표본으로 합성한다. 매우 짧은 구간에서 극단적인 5%↔1600% 변화는 표본 사이에서 한 프레임 수준 차이가 날 수 있다. 합성 순서는 중첩 클립의 숫자형 우선순위와 기본 트랙 순서를 함께 사용한다.
- 해결: 극단적 램프 구간을 조금 늘리거나 중간 속도 지점을 추가한다. 레이어 순서는 중첩 클립 인스펙터의 `합성 우선순위`를 조정하며 100은 기본 트랙 약 한 단계에 해당하고 높은 값이 위에 보인다.
- 판정: 프로그램 미리보기와 최종 출력은 동일한 펼친 트랙과 소스 시간 표본을 사용해야 한다. 자식 변형·시각·오디오 자동화와 ADR 컴프 경계도 같은 장면 시점에서 바뀌어야 한다.

## 2026-08-09 — FFprobe가 없을 때 소스 TC·릴이 일부 파일에서만 표시됨

- 상태: 일반 컨테이너 원시 태그와 QuickTime `tmcd` 이진 트랙의 로컬 대체 판독 구현 완료, 카메라별 사용자 검증 필요
- 환경: FFprobe가 PATH에 없는 Windows/macOS, MP4/MOV/MKV/오디오 원본
- 증상: `timecode`, `reel_name`, `tape` 태그가 있는 파일은 TC·릴이 나타나지만 일부 카메라 MOV에서는 계속 비어 있다.
- 원인: 대체 판독기는 일반 원시 태그와 자체 파일 안의 표준 `tmcd` 첫 sample을 지원한다. 외부 media data reference, 손상된 sample table, 음수 TC, 256MB를 넘는 비정상적으로 큰 `moov`, 비표준 제조사 atom은 안전 판독 대상에서 제외한다.
- 해결: 해당 원본은 FFprobe를 설치해 다시 분석하거나 미디어 패널에서 소스 시작 TC를 수동 입력한다. 수동 TC는 이후 재연결·재분석에서도 자동 값보다 우선한다.
- 판정: FFprobe의 TC가 있으면 우선하고, 없을 때 `tmcd`, 원시 태그 순서로 보충해야 한다. 임의의 숫자나 촬영 날짜를 TC로 추정해서는 안 된다. 29.97/59.94 `tmcd`의 drop-frame flag가 켜졌다면 세미콜론 TC가 표시되어야 한다.

## 2026-08-09 — 물체가 가려진 뒤 마스크가 다른 물체를 따라가거나 교정 전 구간이 사라짐

- 상태: 장기 identity template 재획득과 재생 헤드 수동 교정 재시작 구현 완료, 실제 가림·모션 블러 원본 검증 필요
- 환경: 자유형 마스크 4점 추적 중 대상이 화면에서 가려지거나 크기가 약 0.7~1.3배 변하는 영상
- 증상: 가림 뒤 비슷한 무늬를 잘못 잡거나, 교정 후 다시 추적했을 때 앞부분 키프레임이 바뀐 것처럼 보인다.
- 원인: 재획득은 얼굴 신원 모델이 아니라 최초 마스크 영역의 로컬 밝기 identity template을 사용하므로 완전히 같은 무늬가 여러 개이거나 강한 회전·조명 변화에는 후보가 모호할 수 있다. 다시 추적은 재생 헤드 이후 키프레임을 의도적으로 교체한다.
- 해결: 잘못 붙기 직전 재생 헤드에서 마스크 점을 대상에 다시 맞추고 `현재 마스크 교정점부터 다시 추적`을 실행한다. 교정 시점 이전 키프레임은 유지되며 이후 구간만 새 기준으로 바뀐다. 1.3배를 넘는 급격한 크기 변화는 중간 교정점을 추가한다.
- 판정: 낮은 신뢰도 프레임이 단기 템플릿을 갱신하지 않아야 하고, 재획득 횟수가 완료 알림에 표시되어야 한다. 교정 시점 이전 키프레임 ID와 시간이 그대로 남아 있어야 한다.

## 2026-08-09 — 영상 AI 배경 제거 가장자리가 떨리거나 실행이 차단됨

- 상태: 최대 60프레임 MODNet 전경 분석·24점 다각형·시간 안정화 구현 완료, 실제 인물·제품 영상 사용자 검증 필요
- 환경: 선택 영상 클립, 로컬 WebGPU 또는 WASM, 복잡한 머리카락·반투명 물체·기존 다중 마스크
- 증상: 세밀한 머리카락이 단순화되거나 프레임 사이 외곽선이 움직이며, `기존 마스크를 삭제`하라는 안내로 실행되지 않을 수 있다.
- 원인: 현재 영상 경로는 알파 영상을 별도 코덱으로 만드는 대신 기존 편집·출력 엔진과 일치하도록 MODNet 알파를 24점 벡터 실루엣으로 축약한다. 빈 목표 행은 가장 가까운 유효 알파 행으로 채워 모든 시간 표본의 점 구조를 고정하지만 머리카락 같은 세부 알파는 단순화된다. 기존 마스크가 8개면 합성 한도를 넘으므로 사용자 마스크를 임의 삭제하지 않는다.
- 해결: 인스펙터에서 불필요한 기존 마스크를 하나 삭제하고 다시 실행한다. 세밀한 가장자리는 생성된 `AI 영상 전경` 마스크의 페더와 점을 원하는 재생 헤드에서 직접 교정한다. 매우 빠른 움직임에는 클립을 짧게 나눠 각각 분석한다.
- 판정: 기존 시각 효과 키프레임 시간과 다른 마스크가 유지되고 AI 마스크만 동일 ID로 변화해야 한다. 프로그램 미리보기와 최종 출력의 전경 범위가 같아야 하며 취소하면 기존 효과가 바뀌지 않아야 한다.

## 2026-08-09 — YouTube 유지율 CSV를 가져오지 못하거나 의미 분석이 기본 후보로 대체됨

- 상태: CSV 형식 판독·로컬 E5 의미 분석·기본 후보 복구 구현 완료, 실제 채널 CSV별 사용자 검증 필요
- 환경: AI 초벌 패널, YouTube Analytics에서 내보낸 CSV, 첫 의미 분석의 모델 다운로드, WebGPU 또는 WASM
- 증상: CSV 가져오기에서 시간/유지율 열을 찾지 못했다는 안내가 나오거나, 분석 완료 알림에 의미 분석 실패와 기본 후보 개수만 표시된다.
- 원인: CSV에 `time`, `timestamp`, `elapsedVideoTimeRatio`, `video position` 중 하나와 `retention`, `audienceWatchRatio`, `audience retention` 계열 열이 필요하다. 첫 의미 분석에서는 다국어 E5 모델을 내려받으므로 네트워크·저장 공간·WebGPU/WASM 준비 문제로 의미 단계만 실패할 수 있다.
- 해결: 유지율 그래프 이미지가 아니라 원시 CSV를 사용하고 시간과 유지율 열 이름·데이터 3행 이상을 확인한다. 위치 비율은 현재 타임라인의 마지막 실제 클립/대본 시각에 맞춰지므로 CSV가 생성된 영상 버전과 현재 편집본 길이도 확인한다. 모델 실패 시 기본 침묵·군더더기·반복·설명형 하이라이트 후보는 그대로 사용할 수 있으며 네트워크 복구 후 다시 분석하면 모델이 기기에 캐시된다.
- 개인정보: CSV·대본 추론은 EditWeave의 로컬 모델에서 수행한다. 임베딩과 적용/유지 중심은 프로젝트에 저장되지만 ComfyUI나 외부 추론 서버로 보내지 않는다.
- 판정: 유지율 파일명·표본 수가 AI 패널에 표시되고, 후보 이유에 해당 구간 유지율과 상위 유지 구간 의미 패턴이 나타나야 한다. 하이라이트 적용/유지 후 다시 분석하면 반영된 의미 학습 건수도 이유에 표시되어야 한다.

## 2026-08-09 — HDR10/HLG 출력이 시작되지 않거나 원본 HDR보다 평평하게 보임

- 상태: WebGPU 10-bit I420P10·HEVC Main10과 현재 raw HDR 색상·시각 효과의 RGBA16F 합성 소스 구현 완료, 사용자 장비 검증 필요
- 환경: 내보내기의 HDR10 PQ/HLG 선택, WebGPU 지원 GPU·드라이버, HEVC Main10 WebCodecs 인코더
- 증상: `WebGPU가 필요`, `HEVC Main10을 지원하지 않음` 오류로 출력이 중단되거나, 생성 파일은 10-bit/PQ·HLG로 판독되지만 카메라 HDR 원본의 강한 하이라이트가 기대보다 평평하다.
- 원인: HDR 모드는 실제 10-bit YUV 평면과 Main10 인코더를 요구하므로 지원하지 않는 장비에서 8-bit 파일에 HDR 태그만 붙이지 않는다. PQ/HLG·BT.2020 I420P10 단일/복수 원본은 리사이즈·위치·크기·회전·불투명도·기본 그레이딩·기본 Look/LUT·사용자 곡선·HSL Qualifier·최대 16개 색상 노드 DAG·크롭·비네트·블러·그림자·추적 얼굴 모자이크·합성 모드·다중 자유 마스크·크로마키·검정 전환과 레이어 합성을 WebGPU RGBA16F 선형 광에서 처리한다. 마스크는 알파만 8-bit texture이며 HDR RGB는 변환하지 않는다. 이미지·비 HDR 영상·자막은 투명 Canvas 레이어만 203-nit PQ 또는 75% HLG diffuse white로 변환하므로 아래 HDR 원본의 정밀도는 유지되지만, 해당 SDR 레이어 자체에 원래 없던 HDR 하이라이트를 만들지는 않는다. 같은 색상/블러/LUT/노드 조정 레이어는 RGBA16F 프로그램에 직접 적용한다. 입력이 raw 조건과 맞지 않거나 별도 흰색 전환 표현을 쓰면 해당 레이어만 Canvas로 처리한다.
- 해결: 최신 GPU 드라이버와 WebGPU/HEVC Main10 지원 환경을 사용한다. 효과가 있는 PQ/HLG 원본이 예상보다 평평하면 먼저 입력이 BT.2020-NCL limited-range I420P10으로 디코딩되는지, 원본과 출력 transfer가 같은지, 회전 메타데이터 또는 흰색 전환 때문에 레이어 대체 경로가 선택됐는지 확인한다. 기본 Look/LUT·곡선·Qualifier·색상 노드·블러·그림자·추적 얼굴 모자이크·다중 자유 마스크·크로마키는 raw HDR 경로에서 사용할 수 있다.
- 판정: 지원 장비에서 결과 비디오가 HEVC Main10, 10-bit 4:2:0, BT.2020 primaries, PQ 또는 HLG transfer, BT.2020-NCL limited range로 판독되어야 한다. 30초 체크포인트 경계의 decoder configuration이 다르면 결합을 차단해야 한다.
- 남은 범위: 현재 raw HDR 효과 모델의 RGBA16F 소스 이식은 완료했다. 남은 것은 지원 GPU별 셰이더 실행, PQ/HLG 기준 클립의 효과 전후 휘도·색상·마스크·모자이크 위치, 30초 체크포인트 결합, HEVC Main10 재생 호환성의 사용자 실장비 검증이다. HDR10 PQ 데스크톱 출력의 mastering display·MaxCLL·MaxFALL은 최종 MP4의 `mdcv`·`clli`로 보존하지만 원본에 없는 값은 생성하지 않는다.

## 2026-08-09 — HDR10 출력 뒤 메타데이터 기록 단계에서 실패함

- 상태: MP4 `mdcv`·`clli` 삽입과 atom/chunk offset 보정 구현 완료, 사용자 원본 검증 필요
- 환경: HDR10 PQ 데스크톱 출력, HEVC Main10 MP4, SMPTE ST 2086 또는 Content Light Level 메타데이터가 있는 원본
- 증상: 렌더가 99% 이상 진행된 뒤 `HDR10 정적 메타데이터 기록` 단계에서 실패하거나 완성 파일에 mastering display·MaxCLL·MaxFALL이 표시되지 않는다.
- 원인: 메타데이터 기록기는 최종 MP4에서 `hvc1` 또는 `hev1` sample entry를 찾아야 한다. 인코더가 비표준 MP4 구조를 만들었거나 파일 교체 권한이 없으면 안전하게 중단한다. 원본 메타데이터가 일부만 있으면 `mdcv`는 생략하며, MaxCLL·MaxFALL 중 존재하는 값만 `clli`에 기록한다.
- 해결: 출력 파일을 사용 중인 플레이어를 닫고 쓰기 가능한 폴더에 다시 출력한다. 실패한 단일 렌더의 완결 구간 체크포인트는 삭제하지 않으므로 같은 렌더 작업의 재시도로 최종 결합과 메타데이터 기록만 다시 수행할 수 있다. 메타데이터가 비어 있는 경우 먼저 미디어 상세 정보에서 원본 값이 실제 판독됐는지 확인한다.

## 2026-08-09 — LAN 검토 링크가 다른 기기에서 열리지 않음

- 상태: 토큰형 LAN HTTP 서버·MP4 Range 스트리밍·코멘트 양방향 병합 구현 완료, 사용자 네트워크 검증 필요
- 환경: EditWeave 데스크톱 앱과 검토 기기가 같은 Wi-Fi/유선 LAN, 검토용 MP4 선택
- 증상: 편집자 PC에서는 링크가 보이지만 휴대폰·다른 PC에서 페이지가 열리지 않거나 영상 탐색이 멈춘다.
- 원인: 운영체제 방화벽이 EditWeave의 임의 로컬 수신 포트를 차단했거나 두 기기가 게스트 Wi-Fi/AP isolation으로 서로 격리됐을 수 있다. 링크는 공용 인터넷용이 아니며 앱이나 세션을 종료하면 즉시 닫힌다. MP4가 fast-start 구조가 아니어도 Range 요청은 지원하지만 브라우저 코덱 지원이 없으면 재생되지 않는다.
- 해결: 두 기기를 같은 사설 네트워크에 연결하고 Windows 방화벽에서 EditWeave의 개인 네트워크 수신만 허용한다. 링크의 IP가 `127.0.0.1`이면 네트워크 경로 판별에 실패한 것이므로 다른 기기에서는 열 수 없다. 검토 영상은 H.264/AAC MP4가 가장 호환성이 높으며, 링크를 종료한 뒤 다시 시작하면 새 토큰을 발급한다.
- 보안: 링크 토큰을 아는 동일 LAN 사용자는 영상과 코멘트에 접근할 수 있다. 공용·게스트 네트워크에서는 사용하지 말고 검토가 끝나면 반드시 `종료`를 누른다. 인터넷 공유에는 아직 외부 TLS 릴레이를 사용하지 않는다.

## 2026-08-09 — 과거 버전을 복원하거나 새 분기로 가져온 뒤 미디어가 오프라인임

- 상태: 자동 저장 stable ID 미디어 연결 보존·source/nested/ADR 의존성 분기 복사 구현 완료, 사용자 프로젝트 검증 필요
- 증상: 버전 기록의 `전체 복원` 또는 `새 분기로 가져오기` 뒤 기존에 정상 재생되던 클립이 오프라인이거나, 과거 버전에만 있던 일부 클립이 오프라인으로 표시된다.
- 원인: 같은 프로젝트에서 현재에도 존재하는 stable asset ID는 현재 장비의 열린 원본과 프록시 연결을 그대로 유지한다. 과거 버전에만 존재해 현재 앱에 File 핸들이 없는 미디어, 실제 경로가 이동·삭제된 미디어는 프로젝트 JSON에 파일 자체가 들어 있지 않으므로 오프라인이 정상이다. 버전 비교의 클립 수는 구조 차이를 빠르게 확인하는 요약이다.
- 해결: 기존 현재 미디어까지 오프라인이 됐다면 이름과 ID가 바뀌는 외부 프로젝트 편집이 있었는지 확인한다. 과거 버전에만 있던 항목은 미디어 패널에서 동일 원본 파일을 다시 연결한다. 가져온 분기의 이름에는 체크포인트 시각이 붙고 연결된 중첩/source 시퀀스에는 `연결 버전`이 붙으므로, 비교 후 필요 없는 분기는 별도로 정리한다. 전체 프로젝트를 과거 상태로 바꾸려는 경우에만 `전체 복원`을 사용한다.

## 2026-08-09 — Creator Pack 설치가 거부되거나 템플릿 수가 늘지 않음

- 상태: 선언형 Pack 내보내기·검증·중복 제거·실시간 목록 갱신 구현 완료, 사용자 Pack 검증 필요
- 증상: `.editweave-pack.json`을 선택했지만 권한 또는 안전 제한 오류가 나거나 설치 완료 메시지의 추가 개수가 0이다.
- 원인: EditWeave은 `editweave-creator-pack-v1`만 받고 manifest의 실행 코드·네트워크·파일시스템 권한이 모두 `false`여야 한다. 2MB 또는 범주별 개수 제한을 넘거나 내부 템플릿 값이 기존 스키마와 다르면 전체 Pack을 거부한다. 이름과 실제 설정이 이미 같은 항목은 ID가 달라도 중복 설치하지 않아 추가 개수가 0일 수 있다.
- 해결: 신뢰할 수 있는 EditWeave에서 다시 내보낸 Pack을 사용한다. JSON을 직접 편집했다면 `security` 세 필드와 각 템플릿의 version/필수 값을 확인한다. 임의 JavaScript·바이너리 효과는 Pack으로 설치할 수 없으며, 현재는 설정 기반 모션·속도·오디오·출력 확장만 지원한다.

## 2026-08-09 — 충돌 보고 동의 후에도 서버에 리포트가 없음

- 상태: 로컬 큐와 `services/crash-collector` 수집 서버 구현 완료, 운영 HTTPS 배포·사용자 확인 필요
- 증상: 복구 화면에서 익명 오류 전송에 동의했지만 운영 대시보드에 즉시 나타나지 않거나 같은 리포트가 로컬 JSON에 남아 있다.
- 원인: `VITE_EDITWEAVE_CRASH_ENDPOINT`가 빌드 시 설정되지 않았거나 HTTPS가 아니거나 URL 자격 증명·query·hash를 포함하면 전송하지 않는다. 수집 서버의 `EDITWEAVE_CRASH_ALLOWED_ORIGINS`에 현재 Tauri origin이 없으면 403, 분당 한도를 넘으면 429, 32KB·스키마·UUID·시각 검증에 실패하면 4xx, 볼륨 상한을 비우지 못하면 507을 반환한다. 네트워크 또는 서버 실패 시 앱은 큐를 보존하고 30초, 1분, 2분 순으로 최대 24시간까지 재시도한다.
- 해결: `services/crash-collector/README.md`에 따라 서버를 운영자 전용 볼륨과 HTTPS reverse proxy 뒤에 배치하고 `/healthz`가 `ok`인지 확인한다. 앱 빌드의 endpoint를 `/api/editweave/crashes`에 맞추고 허용 origin, 보존 일수와 용량을 설정한다. 개발 환경은 localhost만 HTTP를 허용한다. 복구 화면의 `오류 보고서 저장`으로 현재 큐를 확인할 수 있으며, 동의를 끄면 이후 자동 전송은 즉시 중단되지만 로컬 큐는 수동 진단을 위해 유지된다.
- 개인정보: 오류는 로컬 큐에 넣기 전과 서버 수신 뒤에 로컬/네트워크 경로·URL query·이메일·UUID·따옴표 안 동적 값을 다시 제거한다. 서버 JSONL과 로그에는 IP·쿠키·요청 헤더를 저장하지 않는다.

## 2026-08-09 — 프로젝트가 다른 편집자에게 잠겼거나 저장 중 잠금을 잃음

- 상태: 공유 폴더 lock·15초 heartbeat·45초 만료·명시적 인수 구현 완료, 사용자 NAS/SMB 검증 필요
- 증상: 프로젝트 열기/첫 저장 시 `사용자@장비에서 편집 중` 경고가 나오거나, 작업 중 `공유 프로젝트 잠금을 잃었습니다`가 표시되고 다음 저장에서 경로를 다시 묻는다.
- 원인: 같은 프로젝트 파일 옆의 숨김 `.editweave-lock`을 다른 EditWeave 인스턴스가 소유한다. 강제 인수, 앱 비정상 종료, 공유 폴더 지연 또는 동기화 서비스의 충돌 복사로 heartbeat 소유권이 바뀔 수 있다. 45초 이상 갱신되지 않은 잠금도 데이터 손실을 피하기 위해 자동 인수하지 않는다.
- 해결: 먼저 표시된 사용자에게 편집 종료 여부를 확인한다. 비정상 종료가 확실할 때만 강제 인수한다. 작업 중 잠금을 잃은 앱에서는 기존 공유 파일을 덮어쓰지 말고 새 이름으로 저장한 뒤 `프로젝트 버전 기록` 비교·새 분기 가져오기로 변경을 병합한다.
- 한계: 일부 클라우드 동기화 폴더는 원자적 `create_new` 대신 충돌 사본을 만들 수 있다. 팀 작업에는 SMB/NAS처럼 파일 생성 원자성을 제공하는 공유 저장소를 권장하며, 실시간 공동 편집과 클립 단위 미디어 checkout은 운영 서버 이후 범위다.

## 2026-08-09 — 피크 정규화가 고정 증폭처럼 들리거나 클리핑됨

- 상태: 고정 1.12배 임시 처리 제거, source-wide sample peak 기반 처리 구현 완료, 실제 원본 청취·출력 사용자 검증 필요
- 증상: 서로 다른 원본에 정규화를 켰는데 음량 변화가 모두 같거나, 이미 큰 원본이 더 커져 클리핑되는 것처럼 들린다. 또는 정규화를 켜도 변화가 없다.
- 원인: 이전 임시 구현은 분석값 없이 1.12배를 사용했다. 현재는 디코딩한 모든 채널·모든 PCM sample의 최대 절댓값을 저장하고 -1 dBFS 목표 gain을 계산한다. 이 값은 intersample True Peak가 아니며 선택한 트림 구간만이 아니라 원본 전체 기준이다. 분석값 누락·무음은 안전하게 원본 gain을 유지한다.
- 해결: 인스펙터의 `소스 sample peak · 정규화 gain · 예상 peak` 안내를 확인한다. 분석값 누락 안내가 나오면 원본을 다시 연결·분석한다. 매우 조용한 소스는 노이즈까지 과도하게 커지지 않도록 +24 dB에서 제한되며, 최종 음량은 트랙 믹서와 완성 MP4 EBU R128 리포트로 조절한다.
- 판정: 서로 다른 peak의 원본에는 서로 다른 정규화 gain이 표시되어야 한다. 같은 클립의 미리보기·프리 Aux·최종 출력과 Delivery Guard는 동일 gain을 사용해야 하며, 완성 파일의 실제 True Peak는 별도 EBU R128 측정값으로 확인한다.

## 2026-08-09 — 노이즈 감소를 켜면 미리보기 전체가 작아지거나 출력과 다름

- 상태: 미리보기·Aux·최종 출력·Delivery Guard 공통 soft-open 게이트 구현 완료, 실제 대화/환경음 청취 사용자 검증 필요
- 증상: 노이즈 감소를 올릴수록 말소리를 포함한 전체 미리보기 음량이 작아지거나 저음이 사라지지만, 최종 출력에서는 배경음만 끊겨 두 결과가 다르게 들린다. 강한 설정에서는 말 끝이 딱딱하게 끊길 수 있다.
- 원인: 이전 미리보기는 실제 게이트 대신 클립 전체 gain을 `1 - amount/500`으로 줄이고 하이패스 주파수도 함께 올렸지만 출력은 0~0.035 amplitude의 하드 임계값을 사용했다. 현재 두 경로는 같은 임계값과 그 위 6 dB smoothstep 개방 곡선을 사용하며 하이패스는 별도 설정으로만 동작한다.
- 해결: 인스펙터에 표시된 게이트 dBFS가 말끝·호흡보다 낮도록 `노이즈 감소`를 조절한다. 지속적인 에어컨·룸톤처럼 말소리와 겹치는 소음은 게이트만으로 완전히 제거되지 않으므로 하이패스·EQ를 함께 사용하거나 낮은 강도를 유지한다.
- 판정: 노이즈 감소 0%에서는 sample이 변하지 않아야 한다. 같은 구간의 미리보기와 최종 출력에서 게이트가 열리고 닫히는 위치가 같아야 하며, 프리 Aux에서도 원본 전체 감쇠가 아니라 같은 저레벨 억제가 들려야 한다.

## 2026-08-09 — 여러 클립이 겹치면 출력에서만 버스 컴프레서·리미터가 강하게 걸림

- 상태: 공유 역할 버스 summing과 공통 dB Dynamics curve 구현 완료, 다중 클립·Aux 실제 청취 사용자 검증 필요
- 증상: 대화 클립 하나씩 들을 때는 미리보기와 출력이 비슷하지만 두 클립 또는 primary+Aux가 겹치는 구간에서 최종 파일만 더 눌리거나 limiter가 작동한다. 버스 makeup과 master 포화도 다르게 들릴 수 있다.
- 원인: 이전 미리보기는 AudioLayer마다 별도 AudioContext와 역할 버스를 만들었으므로 버스 compressor/limiter가 합산 신호가 아니라 클립별 신호에 적용됐다. 클립 compressor도 미리보기는 브라우저 DynamicsCompressor, 출력은 amplitude-domain 수식을 사용했다.
- 해결: 현재는 프로그램의 모든 활성 클립과 Aux가 하나의 역할별 summing input에 연결된다. 버스 삽입 체인과 safety ceiling은 합산 뒤 한 번만 적용되며 클립·버스 compressor/limiter는 출력과 같은 dB transfer curve를 사용한다. 버스 설정을 바꾸면 공유 엔진이 새 설정으로 재구성된다.
- 판정: 같은 역할의 두 동일 신호를 겹치면 하나일 때보다 버스 compressor/limiter가 더 작동해야 하며 그 변화가 미리보기와 출력에서 같은 방향·ceiling으로 나타나야 한다. 프리 Aux는 클립/트랙 fader 전, 포스트 Aux는 후 신호여야 한다. 최종 LUFS/True Peak 수치는 완성 MP4 EBU R128 리포트를 기준으로 한다.

## 2026-08-09 — EQ·음성 강조가 미리보기와 출력에서 다르게 들림

- 상태: 클립·버스 RBJ Biquad PCM 처리와 청크 연속 state 구현 완료, 실제 음성·음악 주파수 sweep 사용자 검증 필요
- 증상: 저역/중역/고역 EQ 또는 음성 강조를 조절하면 미리보기에서는 특정 대역이 선명하게 변하지만 최종 파일은 더 넓은 대역이 움직이거나 고역이 거칠다. 10초 부근마다 순간적으로 tone이 초기화되는 느낌이 날 수 있다.
- 원인: 이전 출력은 Web Audio Biquad가 아니라 1차 high-pass와 단순 대역 차분을 사용했고, 음성 강조는 이전 sample과의 차이를 22% 더하는 방식이었다. 필터 state도 렌더 청크마다 새로 만들어졌다.
- 해결: 현재 출력은 미리보기와 같은 high-pass Q, shelf/peaking 중심 주파수, Q와 gain으로 RBJ Biquad 계수를 계산한다. 음성 강조도 2.4kHz/Q 0.8/+4.5dB peaking으로 통일했고 state를 클립·버스 전체 출력 동안 유지한다. 과도한 +18dB EQ는 후단 compressor/limiter를 강하게 작동시킬 수 있으므로 headroom을 함께 확인한다.
- 판정: 220Hz 이하, 1.2kHz 주변, 4.2kHz 이상의 시험 신호에서 해당 밴드만 같은 방향으로 변해야 한다. 음성 강조 전후의 2.4kHz 중심 변화와 2초/10초 청크 경계의 연속성이 미리보기·출력에서 일치해야 한다.

## 2026-08-09 — 스테레오 원본이 출력에서 3dB 작거나 pan 시 채널이 사라짐

- 상태: Web Audio StereoPanner mono/stereo 공식과 Quad·5.0/5.1 downmix 구현 완료, 실제 채널 식별 신호 사용자 검증 필요
- 증상: stereo 음악을 center pan으로 출력하면 미리보기보다 좌우가 각각 약 3dB 작고 폭이 달라진다. 한쪽으로 pan하면 반대쪽 채널 내용이 단순히 작아져 중요한 소리가 사라질 수 있다. 5.1 원본에서는 센터 대사가 빠질 수 있다.
- 원인: 이전 출력은 stereo L/R에도 mono equal-power gain을 독립 적용했고 첫 두 채널 외에는 읽지 않았다. Web Audio StereoPanner는 stereo center에서 unity이며 방향 pan은 반대 채널을 목표 채널에 crossfeed한다.
- 해결: 현재 디코더는 mono/stereo를 구분하고 Quad·5.0/5.1을 speaker layout 규칙으로 stereo downmix한 뒤 공식 StereoPanner 계산을 적용한다. 미리보기도 3채널 이상은 게이트·EQ·Dynamics 전에 speaker stereo로 정규화한다. LFE는 stereo downmix에서 제외한다. 7.1 이상 경고가 있으면 rear/height가 중요한 납품은 외부에서 승인된 stereo/5.1 mix로 먼저 변환한다.
- 판정: stereo L-only/R-only 식별 신호는 center에서 unity를 유지해야 한다. pan -100에서 두 stereo 채널이 L로, +100에서 R로 합쳐지고 반대 출력은 0이어야 한다. mono center는 L/R 각각 약 -3.01dB지만 합산 power는 유지되어야 한다. 5.1 center 대사는 L/R에 각각 √0.5로 포함되어야 한다.

## 2026-08-09 — 프로그램 오디오 미터가 실제 소리와 다르거나 계속 빨간색임

- 상태: 파형 평균 임시 미터 제거, 공유 pre/post-master 실제 stereo analyser 구현 완료, 사용자 신호 검증 필요
- 증상: EQ·pan·Aux·버스 gain을 바꿔도 미터가 같아 보이거나 소리가 멈춘 뒤에도 계속 움직인다. 또는 출력은 찌그러지지 않는데 미터 외곽이 잠시 빨갛게 보인다.
- 원인: 이전 미터는 실제 Web Audio graph가 아니라 활성 미디어의 분석 파형 평균을 좌우 막대에 표시했다. 현재 막대와 숫자는 master soft clip 이후 L/R peak이며, 빨간 외곽은 최종 sample clipping이 아니라 soft clip에 들어가기 전 합산 peak가 1.0 이상이었던 사실을 1.5초 hold한 것이다.
- 해결: 역할 버스·Aux·gain을 낮춰 pre-master 과부하 표시가 반복되지 않게 하고, 최종 납품 peak는 완성 MP4의 EBU R128 True Peak 리포트로 확인한다. 일시정지 뒤에는 감쇠 시간이 지나 막대가 최저점과 `−∞`로 돌아와야 한다.
- 판정: L-only/R-only 식별 신호는 해당 막대만 올라가야 한다. 동일 역할 클립을 겹치거나 post Aux를 추가하면 처리 전 합산과 버스 Dynamics 결과에 따라 미터가 바뀌어야 하며, 미터 tap 때문에 실제 음량이 커져서는 안 된다.

## 2026-08-09 — 한 프레임 이동이 화면에 반영되지 않거나 장시간 재생 후 A/V가 어긋남

- 상태: 단일 monotonic 재생 시계·프레임 정확 정지 seek·재생 drift 보정 구현 완료, 실제 장시간 사용자 검증 필요
- 증상: 일시정지 상태에서 좌우 키로 한 프레임을 이동해도 영상이 그대로이거나, 재생 헤드를 가까운 위치로 클릭했는데 이전 프레임이 남는다. 오래 재생하면 영상과 오디오 또는 재생 헤드가 서로 늦을 수 있다.
- 원인: 이전 공통 동기화 함수는 재생/정지를 구분하지 않고 180ms보다 큰 차이만 seek했으므로 30fps 기준 약 5프레임의 오차를 허용했다. 각 HTML media element도 자체 시계를 사용하므로 디코더 지연이 누적될 수 있다.
- 해결: 현재 정지·역재생·정지 프레임은 1/240초 이상 차이를 즉시 seek한다. 재생 헤드는 monotonic 기준 시계와 외부 탐색 재기준화를 사용하고, audio는 75ms hard correction, video는 1프레임 이상 최대 ±2% 보정과 120ms hard correction을 적용한다. metadata 전 탐색은 준비 직후 마지막 위치를 적용한다.
- 판정: 30fps에서 좌우 키를 한 번 누를 때마다 서로 다른 인접 프레임이 보여야 한다. 10/30/60분 원본을 재생하며 시작·중간·끝의 clap/입모양 기준 오차를 기록하고, 속도 램프·ADR loop·탭 비활성화 후 복귀·재생 중 탐색에서도 재생 헤드와 소리가 같은 위치로 복귀해야 한다.
- 역재생 오디오: 현재는 출력과 같은 source-time을 사용하는 부분 PCM 예약 경로를 사용한다. 역재생 클립 진입 직후 잠깐 무음이거나 프록시 안내가 나타나면 아래 역재생 오디오 항목을 확인한다.

## 2026-08-09 — 역재생 영상은 보이지만 오디오가 없거나 프록시 경고가 나타남

- 상태: 범위 PCM 디코딩·가변 속도 역방향 표본화·공유 DSP rolling scheduler 구현 완료, 실제 코덱 사용자 검증 필요
- 증상: 역재생 클립 진입 직후 오디오가 잠시 없거나 `속도 매핑 PCM 디코딩이 실시간 재생 속도를 따라가지 못했습니다` 경고가 표시된다. 탐색 또는 ADR 반복 경계에서 짧은 gap/click이 들릴 수 있다.
- 원인: 브라우저 HTMLMediaElement는 음의 playbackRate로 신뢰성 있게 재생할 수 없어 EditWeave이 250ms 소스 범위를 별도 디코딩해 역방향 PCM을 만든다. 긴 GOP·고압축 오디오·400% 같은 고배속은 200ms lead와 0.9초 예약 horizon 안에 디코딩하지 못할 수 있다. 첫 진입은 디코더 준비를 위해 최대 200ms lead를 둔다.
- 해결: 해당 원본의 30fps 편집 프록시를 생성·사용하고 지나치게 촘촘하거나 높은 속도 지점을 줄인다. 경고가 뜨면 잘못된 시각의 소리를 계속 내지 않고 해당 time-map scheduler만 멈추므로 다른 클립과 최종 출력 경로는 유지된다.
- 판정: 25%·100%·400% 역재생에서 말소리/클랩의 방향과 영상 프레임이 일치해야 한다. 재생 중 다른 위치 탐색과 ADR loop 뒤 이전 구간의 예약 오디오가 남지 않아야 하며, clip gate/EQ/Dynamics/normalize/fade/pan, pre/post Aux와 역할 버스 변화가 역재생에도 적용되어야 한다. 최종 MP4와 같은 경계·채널·레벨인지 비교한다.

## 2026-08-09 — 공동 작업 병합 후 예상과 다른 클립이 남거나 충돌 분기가 생김

- 상태: 공통 체크포인트 기반 ID 단위 3-way 병합·상대 충돌 분기·타임라인 검토 마커 소스 구현 완료, 두 장비 프로젝트 사용자 검증 필요
- 증상: 상대 프로젝트를 병합했는데 일부 클립은 현재 편집대로 남고 `병합 충돌 (상대)` 시퀀스가 생긴다. 또는 자동 반영될 것으로 예상한 변경이 충돌로 분류된다.
- 원인: 병합은 선택한 자동 저장 체크포인트를 두 편집본의 공통 기준으로 간주한다. 같은 clip/track/sequence ID를 기준 이후 양쪽에서 서로 다르게 바꾸면 어느 쪽이 맞는지 추정하지 않고 현재 값을 유지한다. 상대 값은 새 ID 분기에 보존한다. 실제 공통 기준이 아닌 체크포인트를 선택하면 한쪽만 변경한 값도 양쪽 변경처럼 보이거나 삭제 의도가 잘못 해석될 수 있다.
- 해결: 병합을 적용하기 전에 두 편집자가 복제했던 마지막 공통 자동 저장 시각과 프로젝트 ID를 확인한다. 충돌 뒤에는 주황색 `EditWeave 병합` 검토 마커로 이동해 현재 시퀀스와 `병합 충돌 (상대)` 분기를 비교하고 필요한 클립만 수동 반영한 뒤 마커를 해결한다. 해결 전에는 Delivery Guard에 `공동 작업 충돌 미확인` 경고가 남는다. 공통 기준을 잘못 골랐다면 자동 저장 기록에서 병합 전 버전을 전체 복원하고 올바른 체크포인트로 다시 병합한다.
- 직접 해결: 프로젝트 버전 기록의 미해결 목록에서 `현재 위치`와 `상대 분기`를 오가며 비교한다. `현재 유지`는 병합 당시 현재 값을 승인하고, 종류별 `상대 … 적용/삭제`는 저장된 상대 값을 시퀀스 설정·트랙·클립·대본·제안·마커·오디오 버스·ADR·미디어·사전에 반영한다. 상대 클립은 분기의 트림·효과·속도와 nested/ADR/group/link 참조를 원본 clip ID에 적용한다. 미디어는 디스크 파일이 아니라 프로젝트 항목만 선택하며, 같은 원본이면 현재 장비 연결을 유지하고 다른 원본이면 오프라인 재연결 상태가 된다. 한쪽에서 시퀀스 전체를 삭제한 충돌은 의존 시퀀스를 안전하게 자동 교체할 수 없어 상대 분기를 확인한 뒤 현재 유지로 닫는다.
- 기록: 결정 상태·시각과 병합 당시 현재/상대 값, 삭제 여부, 현재 클립/트랙 사본은 `.editweave.json`에 저장된다. 모든 충돌을 해결하면 상대 분기는 `병합 기록 (해결됨)`으로 바뀌지만, 상대 클립이 그 분기의 nested 시퀀스나 ADR 테이크를 참조할 수 있으므로 자동 삭제하지 않는다. 해결된 병합 기록에서 현재 유지/상대 적용 개수를 확인하고 종류별 결정을 다시 전환할 수 있다.
- 상대 적용 오류: `상대 충돌 분기에서 적용할 클립 또는 트랙을 찾을 수 없습니다`가 나오면 병합 뒤 프로젝트 JSON을 외부에서 수정했거나 상대 분기/클립 참조가 손상된 상태다. 현재 결정을 덮어쓰지 않으므로 자동 저장에서 분기와 세션이 함께 남은 버전을 복원하거나 `현재 유지`로 닫고 상대 프로젝트를 다시 병합한다.
- ADR 상대 적용 오류: 상대 ADR 큐의 시퀀스·미디어·트랙·클립 참조 중 하나가 현재 프로젝트에 없으면 자동 적용을 중단한다. 먼저 연관된 시퀀스/클립/미디어 충돌을 해결하거나 상대 분기에서 필요한 테이크를 확인한 뒤 다시 결정한다.
- 미디어 상대 적용/삭제: 상대 미디어를 적용한 직후 `원본 미디어 오프라인`이 표시되면 상대 장비의 sourcePath가 현재 장비에 없거나 서로 다른 파일로 판정된 정상적인 안전 상태다. 미디어 패널에서 현재 장비의 원본을 다시 연결한다. `이 미디어는 클립 …개와 ADR 테이크 …개에서 사용 중` 오류가 나오면 해당 asset ID를 참조하는 클립 또는 ADR 결정을 먼저 삭제/교체한 뒤 미디어 삭제를 다시 선택한다. 같은 파일인데 오프라인이 되면 두 장비 파일의 이름·종류·확장자·바이트 크기·분석 길이가 같은지 확인한다.
- 보존 규칙: 서로 다른 clip ID의 추가, 한쪽만 수정한 클립, 양쪽이 똑같이 수정한 값은 자동 결합한다. 같은 클립의 수정/수정, 수정/삭제, 서로 다른 트랙 이동은 현재 작업을 덮어쓰지 않으며 상대 source/nested 시퀀스와 ADR 참조까지 충돌 분기에 복사한다. 프로젝트 ID가 다르거나 stable ID가 중복되거나 클립의 track 참조가 손상됐으면 병합 자체를 거부한다.
- 판정: 공통 기준을 복제한 A/B 프로젝트에서 A는 클립 1, B는 클립 2를 트림하면 결과에 둘 다 반영되어야 한다. 양쪽이 클립 1을 다르게 트림하면 현재 값, 상대 충돌 분기, 해당 시간 검토 마커가 모두 남아야 한다. 결과 저장·재열기 뒤에도 분기 ID와 nested/ADR 참조가 유효해야 한다.

## 2026-08-09 — 시퀀스 삭제 버튼이 비활성화되거나 복제본의 ADR이 손상됨

- 상태: 시퀀스 이름 변경·ID 안전 복제·참조 보호 삭제 소스 구현 완료, 실제 프로젝트 사용자 검증 필요
- 증상: 시퀀스 관리에서 삭제 버튼이 비활성화되고 아래에 중첩·파생·병합·렌더·LAN·ADR 사용 이유가 표시된다. 또는 복제 시 `ADR 큐의 선택 테이크/컴프/트랙 또는 클립 참조가 손상되어 복제할 수 없습니다`가 나온다.
- 원인: 시퀀스를 제거하면 다른 nested clip, 롱폼에서 파생한 쇼츠, 다른 시퀀스의 ADR 큐/테이크 클립, 병합 결정 감사 기록이나 재개 가능한 렌더가 고아 참조가 될 수 있어 자동 연쇄 삭제하지 않는다. ADR/미디어 오류는 프로젝트 JSON 외부 수정 또는 이전 손상으로 clip→asset이나 cue→take→track/clip→asset 연결 중 하나가 실제 프로젝트에 없다는 뜻이다.
- 해결: 관리 창에 표시된 참조 시퀀스를 먼저 연다. 중첩 클립을 제거하거나 필요한 파생본을 별도 보존한 뒤 source 관계를 정리하고, 미완료 렌더는 렌더 작업 창에서 완료·취소·제거하며 LAN 검토와 ADR 창을 닫는다. 병합 감사 기록이 참조하는 시퀀스는 충돌 결정 재전환에 필요하므로 삭제하지 않는다. ADR 참조 손상은 자동 저장 기록에서 정상 큐·트랙·클립이 함께 있던 버전을 복원한 뒤 다시 복제한다.
- 삭제 범위: 허용된 시퀀스 삭제는 그 시퀀스의 ADR 큐 메타데이터와 타임라인만 제거한다. 프로젝트 미디어 항목, 녹음 asset, 프록시와 원본 디스크 파일은 삭제하지 않으므로 필요하면 미디어 패널에서 참조가 사라진 항목을 별도로 제거한다.
- 판정: 새 시퀀스는 현재 캔버스·fps와 비어 있는 고유 ID 트랙으로 열려야 한다. 복제본의 모든 trackId/clipId/group/link/ADR take·comp 참조가 복제본 내부를 가리키고 assetId만 원본과 같아야 한다. 원본과 복제본을 각각 편집해 서로 변하지 않아야 하며, 참조가 없는 시퀀스를 삭제한 뒤 저장·재열어도 나머지 시퀀스와 미디어가 정상이어야 한다.

## 2026-08-09 — 미디어가 선택한 트랙이 아닌 곳에 들어가거나 추가되지 않음

- 상태: 선택과 독립된 시퀀스별 V/A/T 소스 대상 구현 완료, 실제 편집 사용자 검증 필요
- 증상: 트랙을 클릭했는데 가져온 미디어가 다른 트랙에 추가되거나 `V/A/T 소스 대상이 없습니다`, `대상 트랙 잠금을 해제` 안내가 나온다.
- 원인: 트랙 클릭은 인스펙터와 클립 작업의 선택이고, 보라색 `V1/A1/T1` 버튼은 소스·생성 결과의 목적지다. 같은 종류에서 한 대상만 켤 수 있으며 켜진 버튼을 다시 누르면 해당 종류의 목적지가 모두 해제된다. 잠긴 대상은 자동으로 다른 트랙으로 우회하지 않아 의도하지 않은 편집을 막는다.
- 해결: 미디어를 놓을 트랙 머리글의 `V/A/T` 버튼을 켜고 잠금을 해제한다. 영상·이미지·조정 레이어·중첩 결과는 V 대상, 오디오 파일은 A 대상, 모션 텍스트는 T 대상으로 이동한다. 새 트랙은 자동으로 같은 종류의 새 대상이므로 이전 트랙을 계속 사용하려면 이전 대상 버튼을 다시 켠다.
- 이전 프로젝트: 대상 값이 전혀 없는 구형 프로젝트는 종류별 첫 잠금 해제 트랙을 기본 대상으로 지정한다. 사용자가 모든 대상을 꺼 저장한 프로젝트는 재열어도 자동으로 켜지지 않는 것이 정상이다. 대상이 여러 개 켜진 외부 수정 파일은 타임라인 순서상 첫 대상만 유지한다.
- 판정: 선택한 트랙과 다른 V/A/T 대상을 각각 설정한 뒤 미디어를 추가해도 대상 트랙으로 가야 한다. 대상을 모두 끄거나 잠그면 추가가 없어야 하며, 새 트랙 생성·현재 대상 삭제·시퀀스 전환·저장 후 재열기에서도 대상 표시와 실제 목적지가 일치해야 한다.

## 2026-08-09 — 삽입 편집 뒤 기존 클립이 겹치거나 대본·ADR 시간이 어긋남

- 상태: 경계 클립 분할과 프로젝트 시간 메타데이터 원자 이동 소스 구현 완료, 실제 편집 사용자 검증 필요
- 증상: 삽입 모드로 재생 헤드에 미디어를 넣었을 때 그 지점을 가로지르던 기존 클립이 분할되지 않아 새 클립과 겹치거나, 이후 마커만 이동하고 대본·편집 제안·트랙 믹스 자동화·ADR 큐/컴프가 이전 시간에 남을 수 있었다.
- 원인: 기존 `insertTimelineClip`은 `clip.start >= insertTime`인 클립의 시작점만 이동했고 App 호출부는 마커만 별도로 이동했다. 시간 범위를 가진 프로젝트 엔티티와 절대 시간 자동화, ADR take clip 참조를 하나의 편집 트랜잭션으로 다루지 않았다.
- 조치: 잠금 해제 트랙의 경계 클립을 실제 좌/우 소스 구간으로 분할하고 오른쪽 clip·group·link ID를 새로 발급한다. 속도/변형/시각/오디오 자동화와 단어 자막을 분할 구간에 맞춰 다시 샘플링하며, 이후 트랙 믹스 키프레임·ADR 컴프 범위·마커/코멘트·대본/단어·제안·ADR 큐/컴프를 같은 삽입 길이만큼 이동한다. 트랙 자동화는 삽입 시작과 끝에 같은 경계값을 남겨 공백 동안 값이 변하거나 기존 곡선이 늘어나지 않게 한다.
- ADR 보호: 삽입점이 ADR 큐 또는 테이크 클립 내부이면 분할하지 않고 큐 경계 밖으로 재생 헤드를 옮기라는 메시지를 표시한다. 이후 ADR 테이크 레인이 잠겼거나 누락돼 큐와 클립을 함께 이동할 수 없어도 삽입을 중단한다.
- Undo: ADR 큐도 편집 이력 스냅샷에 포함했으므로 한 번의 실행 취소/다시 실행이 트랙과 시간 메타데이터를 함께 복원한다.
- 판정: 2개 이상 링크된 V/A 트랙의 경계 삽입, 잠금 트랙, 속도 램프/효과 키프레임/단어 자막, 삽입점 앞·경계·뒤 마커와 제안, ADR 큐 앞/안/뒤 및 잠긴 ADR 레인을 각각 확인해야 한다.

## 2026-08-09 — 대본/AI 구간 삭제 뒤 단어·자동화·ADR 위치가 어긋나거나 ADR 참조 오류가 남음

- 상태: 공통 리플 삭제 트랜잭션과 ADR 사전 보호 소스 구현 완료, 실제 편집 사용자 검증 필요
- 증상: 대본에서 구간을 삭제하거나 침묵·군더더기 AI 제안을 적용한 뒤 영상은 당겨졌지만 단어 강조, 다른 제안, 트랙 믹스 자동화 또는 ADR 큐가 이전 시간에 남을 수 있었다. ADR 범위를 지우면 Delivery Guard에 take track/clip 또는 comp 불일치가 나타날 수 있었다.
- 원인: 기존 경로는 클립·대본·마커를 서로 다른 함수로 바꾸고 ADR을 편집 이력에 포함하지 않았다. `removeTimelineRange`도 ADR 여부를 모르고 범위와 겹친 take clip을 자르거나 새 clip ID를 발급했다. LAN 검토 comment를 범위 삭제해도 원격 삭제 ID를 기록하지 않아 다음 동기화에서 되살아날 수 있었다.
- 조치: 대본 삭제와 AI 삭제가 `commitRippleDelete`를 거쳐 하나의 범위 변환을 사용한다. 클립 소스/속도/효과/오디오/단어 자막, 트랙 자동화, 대본 단어, 제안 상태, 마커와 검토 코멘트, ADR 큐·컴프·테이크 레인을 함께 제거하거나 당긴다. 삭제 경계에는 일반 음성의 클릭 방지를 위한 최소 80ms fade를 적용한다.
- ADR 차단 메시지: `삭제 범위가 ADR 큐/테이크와 겹칩니다`가 나오면 ADR 창에서 해당 큐를 먼저 삭제·조정하거나 삭제 범위를 큐 밖으로 줄인다. `테이크 트랙/클립 참조가 손상`, `큐와 테이크 클립의 시간이 일치하지 않음`이면 자동 저장 기록에서 정상 큐·트랙·클립이 함께 있던 버전을 복원한다. `ADR 테이크 트랙 잠금을 해제`가 나오면 해당 레인의 잠금을 풀어 큐와 클립이 같이 이동할 수 있게 한다.
- AI 학습: ADR 보호로 삭제가 차단되면 제안 적용 통계는 증가하지 않는다. 성공한 제안은 삭제 지점의 0초 적용 기록으로 남고, 겹친 다른 제안은 기각 상태로 보존되며 뒤 제안은 새 시간으로 이동한다.
- LAN 검토: 범위 안 comment는 원격 삭제 대기 ID에 추가되고 Undo로 복원하면 그 ID가 취소된다. Redo하면 다시 추가되므로 3초 동기화 전에 Undo/Redo해도 최종 로컬 상태와 원격 삭제 의도가 같아야 한다.
- 판정: 범위를 완전히 포함·일부 교차·양쪽 관통·완전히 뒤에 둔 클립 조합, 링크 V/A, 잠금 트랙, 속도/효과/오디오 및 트랙 자동화, 단어 대본, 겹친 AI 제안, LAN comment, ADR 큐 앞/겹침/뒤·잠긴/누락 레인에서 삭제·Undo·Redo·저장 재열기를 확인해야 한다.

## 2026-08-09 — 리플 트림 뒤 gap/겹침이 생기거나 덮어쓰기가 ADR 테이크를 자름

- 상태: 양방향 리플 트림 공통 시간 연산과 ADR 보호 덮어쓰기 소스 구현 완료, 실제 편집 사용자 검증 필요
- 증상: 리플 트림에서 클립 시작을 움직이면 빈 공간 또는 다른 클립과 겹침이 남거나, 끝을 움직인 뒤 클립만 바뀌고 대본·마커·자동화·ADR 위치가 그대로일 수 있었다. ADR 레인을 A 소스 대상으로 둔 채 덮어쓰면 take clip이 일부만 남거나 새 ID로 갈라질 수 있었다.
- 리플 축소: 시작을 오른쪽 또는 끝을 왼쪽으로 움직이면 두 경계 사이를 공통 리플 삭제한다. 선택 클립의 원래 바깥 transition/fade는 보존하고 나머지 클립·트랙 자동화·대본/단어·제안·마커·ADR을 같은 길이만큼 당긴다. `삭제 범위가 ADR 큐/테이크와 겹칩니다`가 나오면 큐 밖으로 경계를 조정해야 한다.
- 리플 확장: 시작을 왼쪽 또는 끝을 오른쪽으로 움직이면 기존 경계에 timeline gap을 삽입하고 뒤의 모든 잠금 해제 트랙과 시간 메타데이터를 민 뒤, 같은 링크 ID와 경계를 공유하는 V/A 전체를 공통 원본 핸들 범위만큼 늘린다. 시작 확장에서는 각 링크 클립의 기존 키프레임과 단어 자막이 추가 길이만큼 뒤로 이동하는 것이 정상이다. `ADR 큐 내부에는 삽입할 수 없습니다`, `테이크 참조가 손상`, `큐와 클립 시간이 일치하지 않음`, `트랙 잠금을 해제` 메시지는 ADR 원자 이동이 불가능하다는 뜻이다.
- 덮어쓰기: 프로젝트 전체 길이와 대본/마커 시간은 바뀌지 않고 V/A 소스 대상 트랙만 교체된다. 범위를 관통한 기존 클립은 좌우로 나뉘며 오른쪽 clip/group/link ID가 새로 발급된다. `덮어쓰기 범위가 ADR 큐/테이크와 겹칩니다`가 나오면 일반 A 트랙을 소스 대상으로 바꾸거나 ADR 창에서 큐를 정리한다.
- 소스 한계: 바깥 트림은 속도 곡선으로 소비한 소스 구간과 자산 분석 길이 또는 중첩 시퀀스 길이를 비교한다. 원본 시작 전이나 실제 끝 뒤의 요청은 가장 제한적인 링크 V/A 핸들에서 멈춰야 하며, 이미지·정지 프레임·자막·조정/생성 레이어만 시간 독립적으로 확장된다.
- 판정: 시작/끝의 안쪽·바깥쪽 네 방향, 링크 V/A와 잠금 트랙, 속도 램프·변형/시각/오디오 키프레임·단어 자막·트랙 믹스 자동화, ADR 큐 경계/내부/뒤, 덮어쓰기 범위 앞/뒤/한쪽/양쪽 교차를 각각 Undo/Redo 및 저장 재열기와 함께 확인해야 한다.

## 2026-08-09 — 트림 후 링크된 영상·오디오가 어긋나거나 원본 밖으로 늘어남

- 상태: 개발 해결, 사용자 검증 대기
- 환경: V1 Development Candidate, 일반·리플·롤·슬립·슬라이드 트림
- 증상: 일반 또는 리플 트림으로 클립을 늘렸을 때 영상은 늘어나지만 링크 오디오가 뒤로 밀리거나, 슬립이 원본 길이 이후의 source offset을 만들 수 있다. 롤/슬라이드에서는 한 트랙만 바뀌거나 떨어진 이웃 클립 길이가 변할 수 있다.
- 원인: 이전 고급 트림은 선택 클립의 타임라인 길이만 제한하고 자산·중첩 시퀀스의 실제 소스 길이, 속도/역재생 소스 소비량과 링크 트랙의 공통 허용 범위를 계산하지 않았다. 롤의 이웃도 편집점 인접 여부를 확인하지 않았다.
- 해결: `trimConstraints.ts`의 공통 소스 핸들 모델과 `trimTimelineClipAdvancedResult`를 사용해 링크 클립 전체의 delta 범위를 교집합으로 계산한다. 롤/슬라이드는 모든 링크 트랙의 맞닿은 이웃을 한 번에 변경하며 잠금·ADR·중복/불완전 링크가 있으면 구체적인 이유와 함께 전체 변경을 차단한다. 리플 확장은 공통 gap 뒤 같은 편집점의 링크 V/A를 모두 늘린다.
- 회귀 테스트: 길이가 알려진 V/A를 링크하고 시작/끝에 핸들이 있는 경우와 없는 경우, 25~400% 일정/가변 속도, 역재생, 중첩 시퀀스, J/L 컷, 이미지·정지 프레임, 잠긴 상대 트랙, ADR 앞/뒤 이웃을 각각 일반·리플·롤·슬립·슬라이드로 조작한다. source offset이 음수/원본 끝을 넘지 않고 링크 경계와 Undo/Redo·저장/재열기가 일치하는지 확인한다.

## 2026-08-09 — 최신 소스 전체 검사가 TypeScript·Rust 컴파일에서 중단됨

- 상태: 개발 해결, 자동 회귀 통과, 사용자 기능 검증 대기
- 환경: Windows x64, TypeScript 7.0.2, React 19.2.8, Rust/Tauri 2.11
- 증상: `pnpm check`가 TypeScript 76개 오류로 첫 단계에서 중단됐고, 별도 `pnpm desktop:check`도 Rust 2개 오류로 실패했다. 기존 0.1.0 설치본은 오류 수정 전 소스로 생성된 상태였다.
- 원인: 새 타입 정의가 요구하는 ref 초기값·owned ArrayBuffer와 string literal narrowing이 일부 코드에 반영되지 않았고, 타임라인 오디오 조정 분할에서 필수 기본값이 optional spread로 넓어졌다. Rust에서는 `File`의 Read/Write `by_ref`가 모호했고 LAN request header가 request buffer를 빌린 채 body를 추가로 읽었다.
- 해결: ref와 typed array 반환 타입을 명시하고, audio adjustment를 기본값 위에 병합하며, keyframe/CSS/HDR/iterator 타입을 실제 runtime 구조에 맞췄다. Rust는 `Read::by_ref`를 명시하고 header 문자열을 소유하도록 변경했다.
- 회귀 테스트: `pnpm check` 전체 통과, Vitest 9개 파일·22개 테스트 통과, `pnpm desktop:build` 성공, 새 Windows EXE 8초 기본 기동 통과를 확인했다. 실제 UI 미디어 가져오기·장시간 출력·설치/업데이트·서명·macOS는 별도 사용자/실장비 검증이 필요하다.
